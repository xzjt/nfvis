# M3 人工演示记录

| 文档属性 | 内容 |
|---|---|
| 执行人 | （填写） |
| 日期 | （填写） |
| 环境 | nfvis-vm：Ubuntu 26.04 / VPP 26.06（版本以 `vppctl show version` 实测为准）/ 加速方式：（KVM 或 TCG） |
| 依据 | `docs/M3-人工演示步骤.md`、`docs/M3-任务清单.md` DoD 1/3/4 |
| 结论汇总 | 通过 __ 项 / 受限未验证 __ 项 / 失败 __ 项 |

## 结果表

| # | 演示项 | 命令要点 | 预期 | 实际 | 结论 |
|---|---|---|---|---|---|
| D1 | L2 转发与 MAC 学习 | `demo-l2`(ens224) + BVI，ping BVI；`show ... mac-table` | 通 + 学到 MAC + 计数增长 | | |
| D1b | 两端口桥接（可选，需 tap） | tap0 加入同 BD，`ping -I vpptap0` | 通（经 BD 转发） | | |
| D2 | L3/VRF/静态路由/BVI 跨网段 | `demo-l3`(ens192)+默认路由；BVI 10.10.0.1/24；宿主加路由后 ping 10.10.0.1 | 跨网段通 | | |
| D3 | ACL deny + 命中计数 | `demo-acl` deny icmp，挂 BVI acl-in | ping 拒 + 计数增长；删除后恢复 | | |
| D4 | NAT 源转换 | `demo-pool` + rule；`vppctl ping ... source 10.10.0.1` | 通 + `show nat sessions` 有会话 | | |
| D5 | SPAN 镜像 | `demo-span`（ens224 → tap1）+ tcpdump | 抓到镜像帧 | | |
| D6 | 端口限速 | `demo-limit` cir 1Mbps，绑定 ens192 | 出现丢包 + policer 计数 | | |
| D7 | LLDP | `protocols lldp enable`+接口；`show lldp neighbors` | 邻居上报（本环境受限） | | |
| D8 | VPP 重启自动重放 | `systemctl restart vpp` → 校验对象与通流 | 无需人工重配即恢复 | | |

## 证据

### D1
```
（粘贴 nfvis-cli show ... 与 vppctl 输出）
```

### D2
```

```

### D3
```

```

### D4
```

```

### D5
```

```

### D6
```

```

### D7
```

```

### D8
```

```

## 环境限制与偏差说明（必填）

- 加速方式（KVM/TCG）：
- tap 插件是否可用（影响 D1b/D5）：
- D5 是否做了抓包实证：
- D7 LLDP 受限原因与后续回归计划：
- 与契约文档不一致之处（若有，属契约漂移，需开修复任务）：

## 结论与签字

- DoD 1（任务完成）：☐ 满足 ☐ 不满足
- DoD 3（现场演示）：☐ 满足 ☐ 部分（受限项：______）☐ 不满足
- DoD 4（VPP 重启重放）：☐ 满足 ☐ 不满足
- 执行人：_______  复核人：_______  日期：_______
