# V1 收尾待办与交接（新会话入口）

| 文档属性 | 内容 |
|---|---|
| 用途 | M1~M5 已全部交付并合并（PR #1~#57）；本文件列 **V1 收尾待办**，供新会话零摩擦接手 |
| 状态基线 | main `d83b46f`（PR #57 内核基线托管已合并）；工作区干净；决策记录 **67 项**（AGENTS 已同步，`make docscheck` 守护） |
| 环境 | nfvis-vm：VPP 26.06 运行中、nfvisd 未运行、ens192/224 绑 vfio-pci、内核基线未托管（无片段）。**引导镜像 `/var/lib/nfvis/images/alpine.qcow2` 是集成测试依赖，勿删** |

## A. 验收收口（优先级最高）

| # | 事项 | 说明 / 落点 |
|---|---|---|
| A-1 | **V1 验收检查表**：把规格书 FR 逐条对照证据（代码 / 守护测试 / 真机记录），每条标 **通过 / 未验 / 降级（含理由）/ 移 V2**，作为 V1 收尾签字依据 | 规格书需求表 + `docs/*-验收记录.md` + `docs/evidence/` |
| A-2 | **§10 性能口径**：响应类已实测达标（`/interfaces` p95 1ms、`/metrics` 4ms、CLI show 1ms、commit 13ms）；**吞吐类（10GbE ~14Mpps、vhost-user ≥8Gbps）与容量类（≤10VM/≤20 容器、恢复时间）未测**（`docs/M5-11-端到端与基准报告.md` §2 已如实标注）。建议：要么上真机 + 流量发生器实测，要么改口径为"设计目标，以目标硬件验收（附测量方法）" |
| A-3 | **IPv6 真机验证**：范围承诺"管理面与数据面 L3 支持 IPv4+IPv6（静态路由）"，代码/单测有 v6 分支，但**无真机证据** | 补：v6 地址 + v6 静态路由 + `vppctl ping` v6 |
| A-4 | **M4/M5 的 DoD 3/4 现场演示汇总**（M2/M3 有演示记录，M4/M5 只有验收记录） | 建议与 A-1 一并做 |
| A-5 | **M5 已标 ✅ 项的独立复核**（SSE 实时推送、`/metrics` 数值、抓包 pcap、tech-support 归档、备份→zeroize→restore、deb 升级与回退、硬件传感器、TLS 换证）——目前是开发者的真机记录 | 优先破坏性路径：备份→zeroize→restore、升级回退 |

## B. 未完成的功能项（已有登记）

| # | 事项 | 状态 |
|---|---|---|
| B-1 | M5 **T0-3 容器 memif 通流**（缺自带 memif 客户端的容器镜像） | ⏳ 未完成（`docs/M5-验收记录.md`） |
| B-2 | M5 **T0-4 console 真人 `Ctrl-]`**（需真实 TTY + 运行中 VM） | ⏳ 未完成 |
| B-3 | M5 **T0-5 快照磁盘内容级回滚**（需 guest 内写文件） | ⏳ 未完成 |
| B-4 | **产品形态待定**：整机安装目前是「Ubuntu 最小安装 + deb + 安装期基线脚本（`deploy/installer/`）」，**分区与整机镜像/装机流程未定义** | 需决策：V1 交付物是"装机即用"还是"装好系统后加装 nfvis 包" |

## C. 已知能力降级（需在验收时降级签字，勿默认通过）

| 项 | 原因 | 处置建议 |
|---|---|---|
| **ACL 命中计数** | stats 段中**无 `/acl-plugin/` 条目**（计数未开启）；非纯粹 govpp 解码问题 | 见 D-1：启用 `ACLStatsIntfCountersEnable` 后复验；仍无则正式降 V2 |
| **buffer 池统计** | statsclient（govpp v0.13.0）无法解码 26.06 的 buffer 池项类型 | 见 D-1：改用 `vpp_get_stats` 回退源（实测数值正确） |
| `show vpp runtime` | govpp runtime 未接入 | 明确提示（已实现）；或随 D-1 一并解决 |
| `storage format-data`、`password change` | 破坏性 / 需前端交互 | 已延期 V2（决策 #65） |
| SR-IOV、LLDP | 无 SR-IOV 硬件；vmxnet3 下 VPP LLDP 报 internal error 且无对端 | 环境受限（决策 #34、M3/M4 验收记录） |

## D. 技术方案（已设计，待实现）

### D-1 govpp v0.13.0 stats 限制的解决（三层，建议按顺序）

**实测事实**：govpp pinned v0.13.0（上游最新仅 v0.14.0-alpha）；statsclient 能正确解码接口计数/内存/线程，**只有 buffer 池与 ACL 受影响**；**VPP 自带 `/usr/bin/vpp_get_stats` 可用**（同版本解码保证正确，支持 `dump|ls|poll` + `machine` 格式 + 路径 pattern），实测 `/buffer-pools/default-numa-0/{used,available,cached}` 数值正确。

1. **第一层（当天可完成，推荐先做）**：在 `internal/orchestrator/network/stats_linux.go` 增加"同版本工具回退源"——statsclient 解码失败/缺项时改调 `vpp_get_stats dump machine [pattern]` 解析（每行 `value /path`）；`show vpp buffers` / `/metrics` 增加**来源标注**（`statsclient|vpp_get_stats`）与"不可用+原因"，不静默省略。先解决 buffer 池（FR-SYS-005）。
2. **第二层（ACL 计数，两步）**：① ACL 绑定时调用 binapi **`ACLStatsIntfCountersEnable`**（govpp binapi 已含该消息）开启计数；② 用 `vpp_get_stats dump machine '/acl*'` 确认真实路径后解析。**若开启后仍无条目** → ACL 命中计数正式降 V2 并改规格书（勿改为解析 `vppctl` 文本）。
3. **第三层（中期，V2 候选）**：升级 govpp（仅 alpha，牵动全部 binapi，升级前须在 nfvis-vm 跑全量 `make integration`）；或自解析 stats segment（格式自描述、跨版本稳定，~300 行 + 单测，去掉 statsclient 与子进程依赖）。

### D-2 内核基线（已完成，供参考）
`show system kernel` 三方对照 + `[edit system] kernel` + `request system kernel apply|rollback` + `deploy/installer/nfvis-baseline.sh`（PR #57、决策 #66）。**V2 候选**：一次性启动项 + 重启后超时未确认自动回退（JunOS `commit confirmed` 完整等价语义）。

## E. 项目机制速查（新会话先读）

```bash
make check            # vet + 全量测试 + 覆盖率门槛 + 依赖方向 + 决策条数 + 原型（必须全绿）
make integration      # 真机集成（nfvis-vm；无 VPP socket 自动跳过，CI 不跑）
```
- 约定与踩坑：`AGENTS.md`；历史缺陷分析：`docs/reviews/2026-09-13.md`（共六轮）
- 契约：`docs/NFViS-openapi.yaml`（87 端点）、`docs/NFViS-CLI命令树完整设计.md`；**改契约先行 + 附录 A 追加决策**
- 守护：API↔契约（`internal/api/routes_contract_test.go`）、CLI 语句↔模型（`cli_mapping_test.go`）、CLI 命令覆盖（`cli_contract_coverage_test.go`）、依赖方向（`internal/archtest`）、决策条数（`contrib/scripts/check_decisions_count.sh`）
- 真机注意：VPP 不自启（`systemctl start vpp`）；虚机重启后需重绑 DPDK（步骤见 `docs/M3-进度交接.md`）
