# NFViS 交接（新会话入口）

| 文档属性 | 内容 |
|---|---|
| 用途 | **新会话零摩擦接手的唯一入口**。M1~M5 已交付合并；本文件记录**当前真实状态、剩余待办、环境要点与踩坑** |
| 状态基线 | **`main` 最新**；工作区干净；决策 **144 项**（`make docscheck` 守护）；`make check` 全绿（⚠️ 宿主 Windows 内存紧张时并行 go test 会 `errno=1455`，用 `go test -p 1 ./...` 串行跑，见 §0 红线 10） |
| 发布 | **v1.1.29**（Web 控制台信息架构重构：资源域一级 + 对象详情二级、32 条 hash 路由、配置域 20 节表单、分级确认三档、界面覆盖 93/6/99；随版两处收口——`GET /system/backup/{file}` 提到 super-user（决策 #143）与控制台直连 fetch 的失败文案带上服务端原话）/** v1.1.28**（Web 控制面补齐：生命周期动作/串口 console/抓包/归档下载/快照/镜像导入/大表 + 界面覆盖守护；另收口 REST「停止并导出」与 VRF 路由 404、修掉镜像导入假绿；决策 #128~#136）/** v1.1.27**（CLI⇄REST 覆盖收口：VS/VM 详情统计字段、ssh host-key 重生成、core-dumps 导出、`load merge`，并修掉一处「假成功」；决策 #124~#127）/** v1.1.26**（Web 控制面增量 1~3 第一刀 + 五处收口 R37-1/R37-2/R44-1/R44-2/#19，决策 #115~#123）/** v1.1.25**（废除安装 ISO + 人工模拟全流程 6 项修复：bind 模块自理、wizard 导航/判定假绿、user-data 文件/类型/instance-id/RefreshSeed、VPP 主堆大页）/** v1.1.20**（内核基线入口收敛 + 安装 ISO——**ISO 已于 v1.1.25 废除**，决策 #111）/** v1.1.19**（内核基线四连 + 重写版手册）/** v1.1.15**（裸机收口）/** v1.1.10**（验收口径收口）/** v1.1.9**（真机反馈三条）/** v1.1.8**（文本去内部引用 + deb 可复现）/** v1.1.7 / v1.1.5 / v1.1.4 / v1.1.3 / v1.1.2 / v1.1.1 / v1.1.0**（详见 Releases 与 §1.2 轮次表）——**跳过 v1.1.6**（已撤回）；**1.1.11~1.1.14、1.1.16~1.1.18、1.1.21~1.1.24 为内部验证构建、从未发布** |
| 验收口径 | 规格书 **109 条 FR**：**通过 101 / 未验 4 / 降级 2 / 移 V2 2**（明细见 `docs/V1-验收检查表.md`；处置口径已于 2026-09-18 确认，**正式签字待落**） |
| 最新发布件 | **v1.1.29** `nfvis_1.1.29_amd64.deb` sha256 `080269540f64a3750d91b349c87d9b7074d433c2f68a1486249beda8d97a4536`（**构建端 / 上传件 / 回下载件 三处一致**；main `dccd007` + `SOURCE_DATE_EPOCH=1790219512`，**连打两次哈希一致**，7,918,732 字节）；发布校验全过（round48 口径逐项）——元数据/内容/解包、**随包文档是当前版本**（规格书 143 条决策）、**随包前端与仓库源码逐字节一致**、升级实装（拓扑存活、config_json 未变）、本版新内容实测（归档下载 read-only/operator 403、super-user 200 且含哈希）、purge、重装（配置库逐字段一致）、拓扑收敛 + 流量复通（BVI ping 双向 10/10、宿主经 DPDK 物理口 <0.5ms）、回下载比对；证据 round66 |
| 上一发布件 | **v1.1.27** `nfvis_1.1.27_amd64.deb` sha256 `2fafd93b8f5622624b29725d30ab26dce82334601518fad88bf16a7695bffec5`（**构建端 / 上传件 / 回下载件 三处一致**；main `163e4d6` + `SOURCE_DATE_EPOCH=1790128941`，**连打两次哈希一致**）；发布校验全过——元数据/内容/解包、**随包文档是当前版本**、升级实装（**既有 VNF 拓扑存活**、配置库逐字段未变）、本版新内容实测（statistics 与 CLI 同源、core-dumps 导出接收端真的收到清单、`X-NFVIS-Merge` 保留未提及字段、ssh host-key 端点 403 边界）、purge（SSH 撤销自动生效）、重装（**配置库逐字段一致**）、重装后拓扑收敛 + 流量复通（BVI ping 双向 5/5、宿主经 DPDK 物理口 <1ms）；证据 round50 |
| 上一发布件 | **v1.1.26** `nfvis_1.1.26_amd64.deb` sha256 `1f8799ffbeef142a73ba9ec555afed1509204621bdead897ed661e4ed9a35022`（**构建端 / 上传件 / 回下载件 三处一致**；main `46f0180` + `SOURCE_DATE_EPOCH=1790088035`，**连打两次哈希一致**；**发布当日按 round29 先例重切过一次**——首切件 `f06539d4…` 的随包手册仍写「只读视图」，两次构建**逐文件对照只有用户手册不同、二进制逐字节相同**）；发布校验全过——元数据/内容/解包、升级实装（**既有 VNF 拓扑存活**）、REST/Web 内容实测（`/ui/` 与源码逐字节一致、`/configuration` 无口令哈希、ping 未通 502、clear-statistics 204 后计数可再增）、purge（SSH 撤销自动生效）、重装（**配置库逐字段一致**）、重装后拓扑收敛 + 流量复通（BVI ping 双向 5/5、宿主经 DPDK 物理口 0.33~0.47ms）；证据 round48（§7 为重切记录） |
| 环境 | nfvis-vm（**就是目标机** `D:\Virtual Machines\nfvis`，VMware、vmrun 可控；round32/34 两度被恢复为「干净」快照后重建）：nfvis **1.1.29** active、口令 `WBF81vOA4M8GM28f@Aa1`（**2026-09-24 round66 按 §3.1 哈希比对法核对有效**；它是最后一次首启的一次性口令，取法与「口令会随快照恢复而变」的提醒见 §3.1）；VPP 26.06-release（本地 debs `/root/vpp-v26.06-deb`，**主堆已用 2M 大页**——决策 #114）；cmdline `default_hugepagesz=1G hugepagesz=1G hugepages=2 hugepagesz=2M hugepages=768 isolcpus=2-5 intel_iommu=on iommu=pt`（IOMMU 真启用）；ens192/ens224 交 vfio-pci 且在 VPP；**vs-vnf 拓扑与 vnf-a/vnf-b 在跑（round66 复验：BVI ping 双向 10/10、宿主经 DPDK 物理口 0.37~0.48ms）**；镜像 `debian-12-generic-amd64.qcow2` 与 `alpine.qcow2`（round36 重补，集成与冒烟阶段 3 可用）；`ens160` 是管理口（红线）；细节与「复跑前须知」见 §3.1 与 `docs/evidence/v1-closeout-round48-release-1.1.26.txt` §4 |

> **接手第一步**：先 `git log --oneline -3` 确认实际 HEAD（上表 SHA 仅为编写时快照，不随每次合并更新）。
> 然后读 `AGENTS.md`（不可违反的规则）+ 本文档；规格书是需求真源，其
> **附录 A（决策 1~117）**回答"该怎么做"；`docs/NFViS-openapi.yaml` 与 `docs/NFViS-CLI命令树完整设计.md` 是**契约**。
>
> **若本轮要动 Web 控制面 / 做 UI 的可视验收**：本机**已启用 Browser Use 插件**（技能 `control-browser`、
> `web-gui-tester`，浏览器操作经 `mcp__node_repl__js` 的 `js` 工具）——它**在会话启动时加载**，
> 所以要在**新开的会话**里才可用（老会话看不到这两个技能与工具）。round37 的只读总览已于
> **round39 做完可视验收**（决策 #117，收口三处缺陷；用法备忘见 §0 第 6 条），
> 后续增量（配置读写/console 终端/抓包日志视图）交付时**同样要先过可视验收再算完**。

---

## 0. 下一步（**新会话从这里开始**）

> ## ⭐ round67 刚落（2026-09-24）：**CLI 运行期 class 判定与命令树声明对齐**（决策 #144）
>
> **一句话**：把上一轮登记的那条**权限判定缺陷**修掉了——命令树把 `Su()`/`Op()` 标在**子节点**上，
> 而执行器把**域节点**（`request system` = O）交给判定 ⇒ 声明为 super-user 的命令在运行期对 operator **全部放行**
> （`zeroize`/`reboot`/`software add`/`configuration backup`…）；**更严重的是 `request vpp`/`interfaces`/`sriov`/`alarms`
> 四个子域连判定都没有**（read-only 也能 `request vpp restart`、`bind-dpdk`）。修法：新增
> `allowTokens`（按 `schema.Match` **匹配到的最深节点**判定）+ `request` 域**入口处判定一次**（不再可能漏域），
> `show` 域同口径。**契约文档未改**（声明本来就写 S/O，本次是让运行期与声明一致）。
> 守护：新增 `cli_class_gate_test.go`——**穷举操作树 200+ 节点**断言「判定 == 声明」+ 端到端 13 条 S / 4 条 O 命令
> 双向断言（含防收紧过头）；**变异验证**两条（退回域节点、去掉入口判定）都会让它报 ✗。
> 真机复验：operator/read-only 被拒、`ping`(O)/`show`(R)/super-user 正例仍可用。
> 证据 `docs/evidence/v1-closeout-round67-cli-class-gate.txt`。
> **本轮后仍未裁**：① 控制台未按角色隐藏写入口（设计 §8）；② `request system configuration backup to <path>`
> 的目标路径仍无校验（已不是提权路径，但仍可改进）；③ `GET /system/tech-support/{file}` 同类口径不一致。
>
> ## ⭐ round65~66 刚落（2026-09-24）：**三项待办全部关闭 + 发布 v1.1.29**
>
> **一句话**：① 高危动作的**"真执行"浏览器验收**补做（建/删用户经闸门真改到底座，逐项对照 CLI/REST/配置库/哈希复算/真登录）；
> ② `GET /system/backup/{file}` 提到 **super-user**（决策 #143——归档含全部口令哈希，恢复所必需故不脱敏，属**权限**问题）；
> ③ **刀 5 收尾 + 发布 v1.1.29**（随包二进制 + 生产库副本做全套浏览器验收；发布校验按 round48 口径逐项过，回下载件与构建端逐字节一致）。
> 发布件 sha256 `080269540f64a3750d91b349c87d9b7074d433c2f68a1486249beda8d97a4536`。
> 证据：`docs/evidence/v1-closeout-round65-web-console-highrisk-exec.txt`、
> `docs/evidence/v1-closeout-round66-web-console-final-acceptance-and-release.txt`。
> （round65~66 建议"下一轮先裁"的那条权限判定缺陷，已在 **round67 / 决策 #144** 修掉。）
>
> ## ⭐ round57~64 刚落（2026-09-24）：**Web 控制台信息架构重构**（决策 #141，14 个 PR 全部合并）
>
> **一句话**：控制台从"单页 16 张卡"重构成"**资源域一级 + 对象详情二级**"的图形控制台，**界面覆盖达到设计判据
> ——已接 93 / 未接 6 / 契约 99**（剩 6 条全是非界面读物：`/metrics`、`/openapi.json`、`/ui`、`/ui/`、
> `/cli/execute`、`/cli/candidates`）。
>
> **设计与计划（先读这三份）**：`docs/superpowers/specs/2026-09-23-web-console-ia-design.md`（决策 #141：
> 定位＝CLI 的图形等价物、全图形鼠标操作、维持免构建、分级确认三档、资源域 IA）、
> `docs/superpowers/plans/2026-09-23-web-console-{skeleton,detail}.md`、证据 `docs/evidence/v1-closeout-round{57..64}-*.txt`。
>
> **交付顺序**：#185 骨架（路由表 `ui/routes.json` + hash 路由 + 每页按需取数 + 总览瘦身）→ #186 详情页（8 条参数化路由 +
> VM 四页签 + 自动断串口）→ **#187 配置历史端点 + CLI（决策 #142，schema v2→v3 补提交者列）** → #188 历史/回滚/会话页
> （**两段式回滚**：取历史快照为 candidate，不直接生效）→ #189 表单网络+计算域（3→13 节）→ #191 表单系统+vpp 段（→20 节）
> → **#195 分级确认三档**（低/中/高危；高危＝确认词 + 10 秒倒计时双闸门；`window.confirm` 归零）→
> #196 低风险端点（硬件健康/LLDP/MAC 表/NTP）→ **#198 危险动作接入 + 收口到 6**。
>
> **同批三次纠错（都是"先证伪再落笔"）**：#190 更正 cross-connect 不是模型缺口（bool 是有意设计，端口身份＝成员端口）；
> #193 判定口令字段**假绿**（明文 HTTP 下报"已保存"而改动被静默丢掉）→ #194 修复（派生失败即整次保存显式失败 +
> **桩式自校准进 CI**，并修掉"异步派生 + 每键 input 导致写入后缀哈希"的第二个真缺陷）；
> **`GET /system` 泄露口令哈希原文（严重）已修**（#196，真机复验 0 次、非敏感字段保留），守护扩到 23 条配置出口 + 兜底扫全 GET 路由。
>
> **⚠️ 三项待办（新会话从这里接）——三项均已完成（round65/66，2026-09-24）**：
> 1. ✅ **已完成（round65）：高危动作"真执行"的浏览器验收**——经界面走完**建用户**与**删用户**两次真执行，
>    并与独立事实源逐项对照：CLI `show users`、REST `GET /system/login-users`、配置库 `rev 55→56→57` /
>    `audit 104→105→106` / `cfgsha` 变化与**回退后逐字节回到原值**、**用界面填的口令对库里的哈希复算 = MATCH**、
>    用新账号真登录成功、删除后原口令登录失败；对当前登录用户的删除被服务端以 **409** 拒绝且界面如实回显
>    `{"code":"CONFLICT","message":"不能删除当前登录用户"}`、被拒后配置库未变。证据 `docs/evidence/v1-closeout-round65-web-console-highrisk-exec.txt`。
> 2. ✅ **已完成（决策 #143）：`GET /system/backup/{file}` 已提到 `ClassSuperUser`**——归档含全部口令哈希（恢复所必需，故不脱敏），
>    与生成端（`POST /system/backup` 本就是 S）及命令树的**声明**口径对齐；列表（只回元数据）保持 read-only。**核实更正**：
>    CLI `request system configuration backup` 的声明等级**确为 S**（`Su()` 标在父节点 `configuration` 上、叶子继承；`node_test.go` 只抽样列了 restore），
>    但**执行器的运行期判定取的是 `request system` 域节点（O 级）→ operator 实测能过**，且 `… backup to <path>` 的目标路径**无校验**、
>    nfvisd 以 root 运行 ⇒ operator 可把任意文件覆盖成归档 JSON（拒绝服务）——**两条缺陷已登记、本轮未修**（决策 #143 文末 ③④）。
> 3. ✅ **已完成（round66）：刀 5 收尾 + 发布 v1.1.29**——**全套浏览器操作验收**用**随包二进制 + 生产库副本**做：
>    25 条具体路由逐条巡检（可见页/面包屑/只请求自己声明的端点/无 JS 错）+ 负例如实报错 + 深链/刷新/前进后退 +
>    低危动作真执行（对照审计行与 0600 落盘文件）+ super-user 归档下载 + 只读角色实测；
>    发布校验按 round48 口径**逐项过**（可复现两次哈希一致、**随包前端与仓库源码逐字节一致**、升级/purge/重装后
>    配置库逐字段一致、重装后拓扑 + 流量 10/10、**回下载件与构建端逐字节一致**）。
>    发布件 `nfvis_1.1.29_amd64.deb` sha256 `080269540f64a3750d91b349c87d9b7074d433c2f68a1486249beda8d97a4536`，
>    Release https://github.com/xzjt/nfvis/releases/tag/v1.1.29。证据 `docs/evidence/v1-closeout-round66-web-console-final-acceptance-and-release.txt`。
>
> **⚠️ 本轮新登记、需下一轮裁决的两条**（① 已在 round67/决策 #144 关闭；都写在 round66 证据 §5）：
> ① ✅ **已修（round67，决策 #144）**：`request system …` 的运行期 class 判定比命令树声明松——原来执行器取
>    `request system` **域节点**（O 级）⇒ operator 能过 `zeroize`/`reboot`/`software`/`configuration backup`（声明为 S），
>    且 `request vpp`/`interfaces`/`sriov`/`alarms` **连判定都没有**；现按"匹配到的最深节点"判定 + `request` 入口判一次。
>    连带的 `… backup to <path>` 任意文件覆盖**不再是低权限路径**（该命令已收回到 super-user）；"目标路径不校验"仍登记在 §2.3。
> ② **控制台未按角色隐藏写入口**（设计 §8 / 骨架计划 Step 2 的验收项未实现）：read-only 账号能看到各页写按钮，
>    服务端判定是真边界（点下去被如实拒绝、无越权），但体验上是"点下去才被拒"——修它要跨 ~20 个页面做入口分类 + 浏览器复验。
> ③ `GET /system/tech-support/{file}` 同为 read-only 而诊断归档内含 `config.json`（含口令哈希），收紧会挡住
>    「operator 生成诊断包 → 下载送支持」的现场流程，属产品口径变更。
>
> **另有两处如实登记的缺口**：① `POST /system/kernel:apply|:rollback` **契约声明但服务端从未注册（404）**——
> 同类幽灵端点，界面如实报错不谎报（待办 §2.3）；② §7 要求的"高危审计记意图+结果两条"**服务端未实现**（只写一条结果行）。
>
> **环境（2026-09-24 round66 收尾）**：目标机装 **1.1.29** active、配置库 **rev 55 / audit 104**（cfgsha `084c3a64…`，
> 本轮全程未变）、`vnf-a`/`vnf-b` 在跑、BD 6252701 在、`vppctl ping` 双向 10/10；**无开发态实例**（8443 无监听，
> 浏览器里开着的标签页指向的临时实例已按惯例停掉，是死页）；口令取法见 §3.1（哈希比对，别反复试登录）。
>
> **新会话第一步**：`git log --oneline -3` 确认 HEAD（本轮末见本文件顶部表；发布点 `dccd007`）→ 读 AGENTS.md + 本文档
> → 从上面「需下一轮裁决的三条」接着做（建议先裁第 ② 条）。


**最近十三轮（round35~48，2026-09-20/22）做完了什么**：

1. **发布 v1.1.25**（round35）：升级实装 / purge / 重装全过，重装后拓扑收敛 + 流量复通。
2. **三件套复跑**（round36）：`make integration` 全绿（238 PASS / 0 FAIL / 0 SKIP）；冒烟对已装实例
   171/26/3（26 条**全是前置级联**）、对干净开发态基线 **194/1/5**（唯一失败 = 已登记的 `show vpp runtime`）；
   语义校验修掉 3 处**工具假红**后 **11/0/0**。**未改产品代码**。
   证据 `docs/evidence/v1-closeout-round36-three-suites.txt`。
3. **V2 Web 控制面增量 1（只读总览）**（round37，**决策 #115**）：内嵌进 nfvisd（`internal/api/ui/`，
   Go `embed`）**同源**托管于 `GET /api/v1/ui/`（**不需要 CORS**、deb 里不需要额外部署件），
   前端**免构建**（原生 HTML/CSS/JS，无 npm）。副产物：登记 R37-1（契约↔实现响应形状不符）。
   证据 `docs/evidence/v1-closeout-round37-web-console.txt`。
4. **R37-1 收口**（round38，**决策 #116**）：查清是**两类**根因——契约 `VppStatus` **重复定义**
   （`yaml.safe_load` 静默取最后一个 ⇒ 发布的规范带过时形状）+ 实现未落地声明字段；
   删重复定义、补齐 `/system/status`（cpu/memory/storage/hugepages）与 `/interfaces`（运行态字段）
   与 `/vpp/status`（config_revision），**数据源与 CLI 同源**；新增两道守护（契约**重复键查重**入
   `docscheck`、**响应形状核对**含白名单理由）。**另登记 R37-2**：`/system/version` 是桩（6 个组件版本空串）。
   证据 `docs/evidence/v1-closeout-round38-contract-shape.txt`。
5. **Web 控制台可视验收 + 三处缺陷收口**（round39，**决策 #117**）：用 Browser Use 插件黑盒验收
   （截图 + 只读 DOM 双向印证）——**首次有人眼看过页面**。发现 CSS `hidden` 被作者样式覆盖
   （登录后登录卡片盖住总览页）、`/login` 响应 `user` 未按契约回对象（顶栏 undefined）、
   `uptime_seconds` 取守护进程而非主机运行时长；修复 + 两道新守护 + `make check` 全绿 +
   真机浏览器全套复跑通过 + 环境复原。证据 `docs/evidence/v1-closeout-round39-web-console-visual.txt`。
6. **R37-2 收口：组件版本探测**（round40，**决策 #118**）：`/system/version` 与 CLI
   `show version` 从桩改为真探测（ubuntu/libvirt/qemu/docker 经各自来源，vpp 与
   `/vpp/status` 同源）；**DPDK 无可靠来源，有意不汇报**（不冒充）。形状守护白名单
   6 条缩到 1 条（其余经注入假源真校）。证据 `docs/evidence/v1-closeout-round40-system-version.txt`。
7. **前端简化 + 组件版本口径**（round41，净减代码）：app.js 删掉手写 `/metrics` 文本解析，
   系统数字全走 `/system/status`（每次刷新少一个端点）；守卫覆盖迁移到字段名守护（嵌套键）。
   证据 `docs/evidence/v1-closeout-round41-web-system-card-rest.txt`。
8. **CLI⇄REST 覆盖核查**（round42）：258 个命令形态 ⇄ 91 个端点逐族对照，**225 覆盖 /
   14 缺口 / 19 CLI-only by design**（round43 后 226/13/19、round47 后 231/8/19、**round49 后 233/6/19**）；产出
   `docs/CLI-REST覆盖核查.md` 与 `cli_rest_coverage_test.go` 守护（映射过期/新命令漏归类
   两道断言，变异验证过）。
9. **增量 2 的 API 侧收口**（round43，**决策 #119**）：新增 `GET /configuration`（committed
   全量读取，ClassReadOnly + 脱敏）、`GET /configuration/candidate` 契约按实现形状对齐并
   收进形状守护、**已知缺陷 #19 收口**（`POST /logout` 释放 candidate 锁，与 CLI Teardown
   对齐）。四条新用例（含变异验证）；真机复验通过。证据
   `docs/evidence/v1-closeout-round43-config-api.txt`。
10. **增量 2 第二刀：控制台配置读写页 + R44-1 收口**（round44，**决策 #120**）：控制台可改
   配置（candidate → 差异 → 提交/丢弃，提交即释放会话锁）；浏览器黑盒验收全流程通过。
   **验收抓到并修复 R44-1**：配置视图移除口令哈希（决策 #25），而"以视图为底稿整文档回写"
   会把哈希抹掉、提交即锁死该账号——引擎对入参缺失的敏感叶子从 committed 继承 + 校验兜底
   拦无口令用户；判据是"提交后原口令仍可登录"的端到端用例（变异验证过）。**登记观察项
   R44-2**：api 来源改管理口不需 commit confirmed（FR-CFG-012 原文只约束 SSH 会话），
   建议评估是否纳入 api/console。证据 `docs/evidence/v1-closeout-round44-web-config-page.txt`。
11. **R44-2 裁决：管理口自锁保护扩到「经网络接入的会话」**（round45，**决策 #121**）：判据从
   "来源是不是 ssh"改为"会话是否经网络接入"——ssh 与 api（REST/Web 控制台）都要求
   `commit confirmed`，本地串口豁免。契约补 400 `CONFIRM_REQUIRED` 说明；原固化旧口径的
   测试改写 + 新增 API 级用例（变异验证过）；浏览器复验 confirmed 全流程（被拒 → 待确认 →
   确认生效）走通。证据 `docs/evidence/v1-closeout-round45-mgmt-confirm-scope.txt`。
12. **配置页向导化 + `commit check` 预校验**（round46，**决策 #122**）：新增
   `POST /configuration/check`（CLI `commit check` 的 REST 等价物：同一份校验、不落库不下发、
   问题仍返回 200 并在 `{ok, errors[]}` 里）；配置页表单扩到三节（系统/接口/资源池，
   隔离核支持 `2-5,7` 文本）+「预校验」按钮。浏览器验收：三节表单改值 → 预校验正反例 →
   提交，CLI 逐项对照一致。证据 `docs/evidence/v1-closeout-round46-config-forms-check.txt`。
13. **增量 3 第一刀：诊断视图**（round47，**决策 #123**）：4 个新端点——`GET /system/logs`
   （与 `show log system` 同源）、`POST /diagnostics/ping`（**未通即 502**，与 CLI 同口径）、
   `POST /diagnostics/traceroute`、`POST /interfaces:clear-statistics`；控制台新增「诊断」卡
   （ping 表单 + 日志按需拉取）。真机与 CLI 逐字对照一致；覆盖核查缺口 13→8。
   证据 `docs/evidence/v1-closeout-round47-diagnostics.txt`。
14. **发布 v1.1.26**（round48）：round37~47 的改动首次进入发布件（Web 控制面增量 1~3 第一刀 +
   五处收口）；发布校验复刻 round29/33/35 口径全过（升级实装拓扑存活 / REST-Web 内容实测 /
   purge / 重装配置库逐字段一致 / 拓扑收敛 + 流量复通 / 回下载 sha 一致）；**连打两次哈希一致**。
   登记 R48-1~R48-5；其中 **R48-5（随包手册仍写「只读视图」）触发发布件重切**（round29 先例，
   见轮次日志第 28 条与证据 §7）。证据 `docs/evidence/v1-closeout-round48-release-1.1.26.txt`。

15. **CLI⇄REST 覆盖收口**（round49，**决策 #124~#127**）：round42 核查里 4 项可做功能补全部补齐——
    VS/VM 详情 statistics 响应字段（#124）、`ssh host-key regenerate` 端点（#125）、
    **core-dumps 导出真实现 + 端点（#126，并修掉 CLI 侧只打印「已受理…将导出」的假成功）**、
    `load merge` 补 REST 面（#127，`X-NFVIS-Merge: true`）。覆盖 226→**236** / 缺口 13→**3**；
    每条都有单测 + 变异验证 + 真机验证（统计字段与 CLI/vppctl 三方一致、core-dumps 导出接收端真的
    收到清单、merge 与 CLI 同结果）。
16. **发布 v1.1.27**（round50，main `163e4d6`）：发布件 `nfvis_1.1.27_amd64.deb`（sha256 `2fafd93b…`，7,839,554 字节，
    `SOURCE_DATE_EPOCH=1790128941`；**连打两次哈希一致**）。内容是 **CLI⇄REST 覆盖收口**——round42 覆盖核查里
    4 项可做功能补（VS/VM 详情 statistics、ssh host-key regenerate、core-dumps 导出、`load merge`，决策 #124~#127），
    并**修掉一处「假成功」**（`request system core-dumps export` 此前只打印「已受理…将导出」而没发请求）。
    覆盖 226→**236** / 缺口 13→**3**。校验复刻 round29/33/35/48 口径全过（含**随包文档是当前版本**一项）；
    登记 R50-1（校验脚本误改生产库 → 按 §3.9 快照恢复）与 R50-2（脚本 stdin/heredoc 坑）。
    证据 `docs/evidence/v1-closeout-round50-release-1.1.27.txt`。

**当前下一步（按优先级）**：

> **round51~55 刚落（2026-09-23，main `e4ad9ac`）**——新会话先读这一段：
>
> **做完了什么**：① **Web 控制面补齐**（决策 #128~#138，13 个 PR #167~#179）：界面覆盖 **20 → 70 条路径**
> （新增 VM/容器生命周期动作、串口 console 终端、抓包视图、归档下载、VM 快照、镜像导入三模式、
> 大表按需拉取、网络对象与容器/镜像详情），并新增**界面覆盖守护**（`internal/api/ui_coverage_test.go`：
> 已接由前端源码机器提取、未接的逐条写理由，**新增端点忘记归类即红**）；同批收口 REST「停止并导出」
> （`?export=true`）、VRF 路由对不存在 VRF **404**、修掉**镜像导入假绿**（2xx 即报完成而服务端是 failed）、
> 实现幽灵端点 `GET /system/kernel`（并修 `KernelDesired` 无 JSON tag 导致的形状不一致）、清掉幽灵路径
> `/vpp:restart`。② **发布 v1.1.28**（round52，sha256 `75f17cdb…`，三处一致）。③ **收口 #22：seed 改 virtio 交付**
> （决策 #139，PR #180）——SATA 光盘 → **virtio 磁盘**，让缺 `ahci` 的 Debian cloud / Alpine virt 内核也能看到 seed。
>
> **round56 刚落（2026-09-23）——seed 端到端取证：结论是「#139 改动正确，但 #22 未收口」**：
>
> **做了什么**：按上一轮留下的「下一步」把 seed 端到端取证做完（产品路径两次运行 + `qemu-nbd` 挂盘副本离线读
> guest 的 `/etc/hostname`、`/var/lib/cloud`、`/var/log/cloud-init.log`，**并先用 Debian guest 做阳性对照自校准**）。
> **三条结论**：① **#139 生效**——seed 现以 `device=disk/bus=virtio` 挂 `vdb`，宿主 `domblklist` 与 guest
> `virtio_blk virtio1: [vdb]` 双向印证；② **但 alpine.qcow2 上 cloud-init 仍不消费 seed**；③ **#22 记的
> 「Alpine 缺 `ahci`」是把 genericcloud 的结论错误外推**——实测 alpine 内核 `ahci` 是 builtin、**旧 SATA 光盘也可见**
> （guest 日志 `sr 0:0:0:0: [sr0]` + `Attempting to use data from /dev/sr0`），失败在**挂载**这一步。
> **根因（guest 侧，有源码 + guest 内实测矩阵）**：cloud-init 只接受 `TYPE ∈ {vfat, iso9660}` 且带 cidata 卷标的设备，
> 且固定用 `mount -o ro -t auto`；而 Alpine 的 `/bin/mount` 是 **busybox**，其 `-t auto` **只认 `/proc/filesystems`
> 里已加载的类型**（那一刻只有 ext3/ext4＝根盘驱动），`isofs`/`vfat` 都还没 load → `Invalid argument`；
> **先 `modprobe isofs` 再 `-t auto` 立刻成功** ⇒ 唯一原因就是「驱动未加载」。**宿主侧无解**（产品只控制域 XML）。
> **处置**：**决策 #140**（#139 保留；#22 改记「部分收口」；alpine 类限制如实登记不追）+ 随包文档更正
> （手册 §9.2③/§11 排查表/§12 限制表）+ **新增附录 A 表格结构守护**（见下条）。
> 证据 `docs/evidence/v1-closeout-round56-seed-e2e.txt`（含 4 条登记 R56-1~R56-4）。
>
> **顺手修掉的两个文档/工具缺陷**：① 附录 A 的 **#130 行自 PR #170 起被劈成两半**（行内写的是**真 CR 字符**，
> 后来被规范成换行 → 行尾断在「（补 `」、尾巴掉到表尾），**v1.1.28 的随包规格书带此缺陷**，而条数守护照样全绿——
> 已按 git 原文（`a369896`）接回，并给 `check_decisions_count.sh` **加两道结构检查**（决策行须以 `|`/`。` 收尾；
> 附录 A 内不得有孤立行），红-绿验证过；② **REST 建 VM 时未知字段被静默忽略**（上一轮用的 `cloud_init.user_data_file`
> 不在契约里、被丢掉，是那轮「user-data 没生效」的一半原因）——只登记未改（改解码严格性属新行为，需契约先行）。
>
> **环境**：目标机装 **1.1.28**（v1.1.28 之后的 5 刀尚未发布）；配置库 **rev 55 / audit 104**
> （config_json 未变，本轮两次开发态实例各用全新 DB）；VPP 里只剩 BD 6252701；两台 VNF 在跑；开发态实例已退出。
> 口令见 §3.1（哈希比对法核对）。收尾后另做了一次**浏览器黑盒复核**（已装 1.1.28 二进制 + 生产库副本起明文
> 开发态实例，走不了自签证书那条路——内置浏览器拒绝加载）：控制台登录、卡片数据与 CLI/配置库逐项一致
> （rev 55 / vnf-a、vnf-b running / BD 6252701），截图 + DOM 双向印证，收尾指纹与备份逐项一致——证据 §8。
>
> **V2 控制台信息架构重构：设计已定、待过目**（2026-09-23，决策 #141）：控制台从「单页 16 张卡」改为
> **资源域一级 + 对象详情二级**（总览/计算/网络/系统/配置/运维六页，详情页内 Tab 取代共享浮层）；定位 = **CLI 的图形等价物**、
> **全图形鼠标操作**（不出现语句）、**维持免构建**（原生 ES modules + hash 路由 + 产物入库）、**分级确认**（低/中/高危）；
> 设计文档 `docs/superpowers/specs/2026-09-23-web-console-ia-design.md`（含分五刀的落地计划与机器判据：`uiNotWired` 28→6；
> 另发现一处**契约缺口**——CLI 有 `rollback [n]` 但没有「列出历史快照」，需新增 `GET /configuration/history` + CLI 对应命令）。
> **验收口径（本决策起生效）**：**每个交付的功能都要在浏览器里真的操作一遍**（Browser Use 黑盒：操作前后 DOM 快照 +
> 关键步骤截图 + **与独立事实源对照**——界面显示对了不算完，要证明它真的改到了底座）。AGENTS 已同步。
>
> **round57 刚落（2026-09-23）：控制台 IA 骨架刀已交付**（决策 #141 的第 1 刀）——
> 单页 16 张卡 → **资源域一级 + 对象详情二级**：`routes.json` 路由表（13 条，前端与 Go 守护共用一份）、
> `router.js` hash 路由（地址即页面：可分享/刷新保持/前进后退）、每页按路由声明取数（总览不再全量刷新、
> **破坏性动作移出总览**）；新增 `ui_routes_test.go` 与"路由表端点在契约里"断言，覆盖守护改从路由表提取。
> **浏览器操作验收**（决策 #141 口径）13 页逐页过、取数范围用页面内 `performance` 资源清单验证、
> 深链/刷新/前进后退/未知地址全过，并抓到并修掉 3 个"没人眼看发现不了"的缺陷（R57-1 取数失败抛错、
> R57-2 提示被立刻抹掉、R57-3 导航层级读不出）。证据 `docs/evidence/v1-closeout-round57-web-console-skeleton.txt`。
> **下一步**：刀 2 详情页 → 刀 3 配置域表单（含新增 `GET /configuration/history`）→ 刀 4 分级确认 + 22 条接入 →
> 刀 5 收尾（`uiNotWired` 28→6、手册、全套操作验收）。
>
> **round58 刚落（2026-09-23）：控制台详情刀已交付**（决策 #141 第 2 刀）——对象详情页取代共享浮层：
> 8 条参数化详情路由、VM 详情 4 个 Tab（概览/接口/快照/串口，串口与快照 DOM 从浮层原样搬入并复用既有实现）、
> 容器/镜像/网络对象详情页、列表行可点、面包屑可点、**离开详情页自动断开串口**。浏览器操作验收过（点行进详情、
> 串口真连真断、快照列表、运行中创建被正确拒绝、深链直达、镜像详情、容器不存在时如实报错）。
> 顺带纠正：`/qos/policies/{name}` 与 `/port-mirroring/{name}` **契约里只有 DELETE**（旧「详情」按钮是 405），
> 已按实情移入 `uiNotWired` 并加守护「路由表端点必须在契约里有 GET」——**覆盖数字如实变化：已接 70→68 / 未接 28→30**。
> **未验如实登记**：容器详情正例（本机无容器）、关机态下的快照创建/删除/回滚（本机 VNF 在跑，只验到拒绝路径）。
> 证据 `docs/evidence/v1-closeout-round58-web-console-detail.txt`。**下一步**：刀 3 配置域表单（含 `GET /configuration/history`）
> → 刀 4 分级确认 + 22 条接入（`uiNotWired` 收口）→ 刀 5 收尾。
>
> **round59 刚落（2026-09-23）：控制台刀 3a（配置历史/回滚 + 持锁会话）已交付**——两条新路由、
> 回滚**两段式**（取历史快照为 candidate，不直接生效；中危确认框列影响面 + 标红 + 只读回显）、
> 会话页只读；实测坑已避开：`rollback/{n}` 的 **n 是相对最新提交的偏移**、不是 Rev 号。
> 浏览器操作验收走通全流程（取候选 → 配置页显示真实差异 → 会话页持锁 → 丢弃 → 锁释放），
> **没有提交**（运行配置与 VPP 未触碰）。覆盖：已接 68→**71** / 未接 30→**28**。
> 证据 `docs/evidence/v1-closeout-round59-web-console-config-history.txt`。
> **下一步**：刀 3b（128 条 set/delete 的域表单扩展）→ 刀 4（分级确认组件 + 28 条接入，`uiNotWired` 收到 6）
> → 刀 5（收尾 + 发 v1.1.29）。
>
> **round60 刚落（2026-09-23）：控制台刀 3b 第一段（配置页表单扩到网络域 + 计算域）**——3 节 → **13 节**、
> 108 个控件，覆盖虚拟交换机/VRF·路由/ACL/NAT/QoS/端口镜像/聚合/LLDP/虚拟机/容器；走既有 candidate →
> 预校验 → 差异 → 提交/丢弃 流程。浏览器验收：改 hostname → 保存 → **差异显示 `[edit system] + hostname …`**
> → 预校验通过 → 丢弃（未提交）。未覆盖如实登记：SR-IOV vf-count（无硬件）、`cross-connect` 的**端口身份**
> （**该结论已被 round60 追记证伪**：bool 是有意设计、端口身份＝该交换机的成员端口；仅遗留"表单文案应写明配对关系"）。
> 证据 `docs/evidence/v1-closeout-round60-web-console-config-forms.txt`。
> **下一步**：刀 3b 第二段（系统段其余字段 + 模型缺口是否补）→ 刀 4（分级确认 + 28 条接入）→ 刀 5（收尾 + v1.1.29）。
>
> **round61 刚落（2026-09-23）：控制台刀 3b 第二段（配置页扩到系统段 + vpp 段）**——配置页 **20 节 / 163 控件**
> （系统段 7 节 + VPP 节）；口令在**浏览器内**派生成与 CLI 同格式的 PBKDF2 哈希、**明文 HTTP 下拒绝而非退化成明文**；
> 顺带修掉两个真缺陷（`vpp.dpdk.per_dev` 定位器漏一层导致静默丢数据、DNS/NTP 整表替换丢第 3 条）；
> cross-connect 文案已写明"配对＝该交换机的两个成员端口"（round60 那条待办**已关闭**）。
> 浏览器验收：20 节渲染、改 VPP 节 main-core → 差异 `[edit vpp cpu] - main-core 5; + main-core 3;` → 丢弃（未提交）。
> **未在浏览器验到**：口令字段的"明文 HTTP 拒绝"口径（探针没定位到控件；只有引擎级证据）——**下一刀开头补**。
> 证据 `docs/evidence/v1-closeout-round61-web-console-config-system-forms.txt`。
> **待查项已判定并修复（2026-09-24）**：明文 HTTP 下填口令保存会**假绿**（报「已保存」而口令改动被静默丢掉；
> 无明文泄露）。**⚠️ 判定时的判据已被更正**：`GET /configuration/candidate` 会脱敏、`password_hash` 本来就不返回，
> 不能用它当判据——真凭据是「候选/差异是否变化」与「客户端候选文本里的 `pbkdf2$…`」。
> **修复**（`fcd12b0`）：闸门放到 `cfgSave()` 入口，派生失败即**整次保存显式失败**（不写 candidate、消息给 HTTPS 提示、
> 保留控件内容便于重试）；**顺带修掉同路径第二个真缺陷**——异步派生 + 每键 `input` 会把半截口令清掉，
> 写进候选的是**后缀**的哈希（修复前实测只剩 `23!x`）。**桩式自校准进 CI**（`web-console-config-selftest.sh`，
> 接进 `make check` toolcheck，红-绿验过）。**浏览器复验两条路径都过**（拒绝路径 + 经隧道 localhost 的正例），
> 独立源 `GET /configuration/diff` 出现 `password-hash «已隐藏»` 的增删行。证据 round61 §5/§6。
>
> **下一步**：判定上面这条 → 刀 4（分级确认组件 + 28 条接入，`uiNotWired` 收到 6）→ 刀 5（收尾 + v1.1.29）。
>
> **round62 刚落（2026-09-24）：控制台刀 4a（分级确认组件）**——低/中/高危三档落地（高危＝确认词 +
> 10 秒倒计时双闸门、失败关闭），**9 处 `window.confirm`（27 个入口）全部升级、源码里归零**；桩式自校准
> 进 CI（293 断言 + 红-绿）。浏览器验收：低危（生成配置备份）与中危（重启主机，4 条影响面 + 标红）形态与
> "取消即不执行"都对；**高危档未在浏览器验**（本段没有已接的高危动作，属刀 4b）——如实登记，刀 4b 接第一个
> 真高危动作时必须按 §11.2 补做。证据 `docs/evidence/v1-closeout-round62-web-console-confirm-tiers.txt`。
> **下一步**：刀 4b（把 28 条"有意不接"逐个接入，高危动作走三档确认；`uiNotWired` 收到 6 条）→ 刀 5（收尾 + v1.1.29）。
>
> **round63 刚落（2026-09-24）：控制台刀 4b 第一段（低风险端点接入）+ 两处服务端修复**——接入硬件健康/阈值、
> LLDP、MAC 表（按需）、NTP 同步，覆盖 **已接 71→76 / 未接 28→23**；顺带修掉 **① `GET /system` 泄露口令哈希原文**
> （严重，安全；真机复验 password_hash/pbkdf2 均 0 次、非敏感字段保留）与 **② LLDP 邻居字段名契约/实现不一致**
> （含"守护运行态未注入即恒跳过"的漏洞）。**⚠️ 刀 4b 第一段的浏览器验收**已补做**（硬件健康页含空态如实显示、LLDP 页控件数 0 且指路配置页、
> **MAC 表 4 条与 `vppctl show l2fib` 的 4 entries 逐条对上**、NTP 低危档形态正确且真同步 `chronyc makestep: 200 OK`）；
> **仍未做**：MAC 表上限/换对象清空两个边界、NTP 审计行核对（看最近 3 条未出现 `system.ntp.sync`，未穷尽——
> 低风险显示逻辑，桩式自校准已覆盖前者）
> **新登记（需决策）**：`GET /system/backup/{file}` 只需 ClassReadOnly 就能下载含全部口令哈希的完整配置归档，
> 而 CLI 同能力是 S（super-user）——权限口径不一致，建议提到 ClassSuperUser（契约先行）。
> 证据 `docs/evidence/v1-closeout-round63-web-console-4b1-and-secret-fix.txt`。
> **下一步**：补验刀 4b 第一段的浏览器操作 → 刀 4b 第二段（危险动作接入 + 三档确认，`uiNotWired` 收到 6）→
> 刀 5（收尾 + v1.1.29）。
>
> **round64 刚落（2026-09-24）：控制台刀 4b 第二段（危险动作接入 + 收口到 6）**——系统域/运维域的危险动作
> 全部接入并走三档确认（用户管理/证书上传/软件升级/恢复配置/恢复出厂＝高危，词分别是用户名/`install-cert`/
> `upgrade`/归档名/`zeroize`；内核基线/DPDK/SR-IOV＝中危）；**覆盖达到设计目标：已接 93 / 未接 6**（只剩非界面读物）。
> 顺带查出 **`POST /system/kernel:apply|:rollback` 契约声明但服务端从未注册（404）**——同类幽灵端点，界面如实报错不谎报。
> **浏览器验收**：高危闸门全过（错词/倒计时未完都不放行、词对且倒计时走完才可点、取消未执行且审计无痕）；
> **未验两项**：① 高危动作的**真执行**没跑成（探测没打开建用户模态框 + 上下文耗尽）——精确步骤已备，下一刀开头补；
> ② §7 的"高危审计记意图+结果两条"服务端未实现。证据 `docs/evidence/v1-closeout-round64-web-console-dangerous-actions.txt`。
> **下一步**：补高危真执行 → 裁 `GET /system/backup/{file}` 权限口径（ClassReadOnly vs CLI 的 S）→ 刀 5（收尾 + v1.1.29）。
>
> **下一轮的候选（按价值）**：① 内核基线 **apply/rollback 进界面**（含确认强度设计，别用裸 confirm()）；
> ② 待办 §2 里 ③ 类低风险 5 条（`/system/configuration/sessions`、`/system/hardware`、
> `/configuration/rollback/{n}`、`/system/ntp:sync`、`/system/health/thresholds`）；
> ③ 发 **v1.1.29**（把 1.1.28 之后的 5 刀 + 本轮文档更正送进发布件——**注意随包文档已改**，照 round48 口径核对）。
> **有意不做**：用户管理/恢复出厂/软件升级/DPDK 绑定/SR-IOV/证书上传/内核 apply 之外的破坏性动作
> （界面不提供，CLI 有守卫与二次确认——理由见 `uiNotWired`）。

1. ✅ 已完成（round40，决策 #118）**R37-2 收口**（组件版本探测）——见概览第 6 条与证据
   `docs/evidence/v1-closeout-round40-system-version.txt`。
2. ✅ 已完成（round41）**前端简化**（系统数字改读 `/system/status`）——见概览第 7 条与证据
   `docs/evidence/v1-closeout-round41-web-system-card-rest.txt`。
3. **Web 控制面后续增量**（决策 #115）：配置读写（candidate/commit + 向导式交互，
   用 `X-NFVIS-Auto-Commit`）、console 终端（WS + 一次性 ticket）、抓包与日志视图。
   ✅ **增量 2 已完成**（round43 API 侧 + round44 配置读写页 + round45 R44-2 裁决 +
   round46 向导化/预校验，决策 #119~#122）：控制台可改配置（candidate → 差异 → 预校验 →
   提交/丢弃），管理口变更走 confirmed 全流程，接口/资源池有表单，已过浏览器可视验收。
   ✅ 增量 3 第一刀（诊断视图：日志/ping/traceroute/清零统计）已完成（round47，决策 #123）。
   ✅ **④ 发版 v1.1.26 已完成**（round48）：round37~47 的改动（增量 1/2/3 第一刀 + 五处收口）
   首次进入发布件；发布校验复刻 round29/33/35 口径全过，证据
   `docs/evidence/v1-closeout-round48-release-1.1.26.txt`。
   **下一刀**（均属「不新增能力」类）：token revoke / format-data 两项已登记延期，`show vpp runtime`（govpp runtime 解码受限，两边都未接）。
   **round42 覆盖核查结论**（`docs/CLI-REST覆盖核查.md`）：增量 3（诊断） = 日志 +
   ping/traceroute + 两处 statistics 响应字段 + clear counters + ssh host-key +
   core-dumps 导出——**日志/ping/traceroute/clear counters 已于 round47 收口**、**增量 3 剩余缺口里 4 项可做功能补已全部收口**（两处 statistics 响应字段、`ssh host-key regenerate`、
   `core-dumps export`（**同时修掉 CLI 侧的假成功**）、`load merge`；决策 #124~#127）
   （覆盖 226→**236** / 缺口 13→**3** / 例外 19；机器真源见 `cli_rest_coverage_test.go`）。
   余 3 项：token revoke（延期 V2）、format-data（有意延期）、`show vpp runtime`（两边都未接）。
   ✅ **这批收口已随 v1.1.27 发布**（round50）——覆盖 226→236 / 缺口 13→3。
4. **登记未修的 4 项**（round34/35 证据 §3/§5）：CLI 引号内 `|` 被当管道切分、多行引号值只取首行
   （多行 user-data 须走文件路径，手册已写明）；`request vpp restart` 返回后立即查询偶发 broken pipe；
   1G 大页池「无主占用」未做回收。
5. **环境受限项**（如实登记，别当待办追）：见本文 §5 与 `V1-验收检查表.md` §5.1。
6. ✅ 已完成（round39，决策 #117）**Web 控制台可视验收**（三处缺陷收口）——见概览第 5 条
   与证据 `docs/evidence/v1-closeout-round39-web-console-visual.txt`；**Browser Use 插件用法备忘**：
   每个 `js` 调用都要先 bootstrap（`ZCODE_PLUGIN_ROOT` 下 `scripts/browser-client.mjs`），
   浏览器绑定不跨调用续命；`tabs.list()` 与动作后的观察要分开调用；截图必须与
   `nodeRepl.emitImage(await tab.screenshot())` 同一格；`tabs.new()` 不要重复建
   （会多出 about:blank 空白页，先 list 再 get）。

**轮次日志**（第 12~15 条为最近四轮；细节见 §1.2 轮次表与各轮证据）：

1. **裸机测试暴露的 16 条发现里，可修的 12 条全部修完并真机复验**：
   #10 自签重签自毁管理面（决策 #99）、#8 业务口进 VPP 的产品路径（#100）、#7 管理口守卫（#101，口径 A）、
   #13 解绑在用口会卡死 CLI 通道（#102）、#12(a) 管理口自锁补齐首次声明（#103）、
   #9 冒烟假红、#14 空态文案、#1 purge 残留、#3 里程碑编号+守护、#4 `| compare` 接线、
   #11 `show vpp` 可见性、#2/#5/#6 手册与工具脚本。每条都有单测（关键行为做了**变异验证**）与真机证据。
2. **发版 v1.1.15**（决策 #99~#103 的收口；sha256 四处一致、可复现两次相同、真机安装实测）。
3. **`make integration` 的前置全部就绪并首跑**：镜像（产品路径 + sha256 校验）、`docker pull alpine:3.20`（经代理）、
   `cloud-localds`、隔离核池、大页布局。首跑 **211 个用例通过**，失败从 6 条降到 3 条。
   证据 `docs/evidence/v1-closeout-round23-integration-prep.txt`。
4. **（第二十四轮）`make integration` 跑绿**：3 条 guest 类用例的根因是「产品 seed 挂 SATA 光盘而 guest 内核
   缺 `ahci` → cloud-init 静默自禁」与「测试把 guest 网卡写死成 `eth0`（实际 `enp1s0`）」；修法全在测试侧与
   Makefile，**未改产品**。全套 **0 FAIL**。同时**更正了上一轮 #17 的结论**（genericcloud 不可能通过）、
   关掉 #18，并新登记需决策的 #22/#23。证据 `docs/evidence/v1-closeout-round24-integration-green.txt`。
5. **（第二十五轮）内核基线的真机补全与护栏**（决策 #104）：`EnrichDesired` 在安装器与 CLI 两条写路径统一补全
   vendor 参数（intel/amd + `iommu=pt`）、`irqaffinity=<非隔离核>`，内核无 nohz_full 支持时省略 `nohz_full/rcu_nocbs`；
   `ValidateDesired` 拦「隔离核越界 / 非隔离核不足 2 个 / 逃生口里的 isolcpus=」——被拒不落盘不跑 update-grub。
   **CLI 命令面不变**。真机已验（print 补全、护栏五类拒绝、apply/rollback、恢复原状）；**重启生效未测**（机器不重启）。
   证据 `docs/evidence/v1-closeout-round25-kernel-baseline-guard.txt`。**下一步的候选**：低延迟参数组做成单独确认的
   可选 profile（每项都有代价，不做默认）、双大页池（涉及契约，单列）、安装期/CLI 交互式向导（若要，放 CLI 而非 postinst）。
6. **（第二十六轮）低延迟 profile + 装机一遍补掉 round25 未验**（决策 #105）：`set system kernel low-latency <bool>`
   与脚本 `--low-latency`（显式可选，代价在输出里明说）；机型无关五项由生成器产出，`idle=poll/tsc=reliable`
   **虚拟机自动省略**（EnrichDesired 探测 hypervisor 标志/DMI）；Compare 增代表项使「待重启生效」不漏报。
   **重启已实测**：cmdline 全参数生效（含 #104 的 vendor 组）；CLI apply/rollback/护栏（isolated 0-5 被拒、片段不动）真机通过；
   vendor 补全让 VMware 的 vIOMMU 真启用（不再需要 unsafe noiommu 绑 vfio）；数据面经产品路径恢复（两口 up）。
   证据 `docs/evidence/v1-closeout-round26-lowlatency-profile.txt`。**剩余候选**：双大页池（契约改动，单列）、
   交互式向导（若要，放 CLI 而非 postinst）。
7. **（第二十七轮）双大页池进内核基线**（决策 #106）：committed 多池此前只托管一种页大小，2M 池只能
   运行期手工 echo、重启即失。现 1G+2M 两组连写进 cmdline（`hugepagesz=1G hugepages=2 hugepagesz=2M hugepages=768`），
   ReadActual/Compare/show 按尺寸各读各的（sysfs 优先），**重启后 2M 池开机即 768、无需手工 echo**；
   round26 的手工预留步骤从流程消失。观察项 #27：启动期 1G nr 曾无主上浮到 3（缩页试验成功、无持有者）。
   证据 `docs/evidence/v1-closeout-round27-dual-hugepage-pools.txt`。
8. **（第二十八轮）`wizard` 初始化向导**（决策 #107）：CLI 端交互编排（monitor 先例，无 API 端点）——
   问答规划隔离核/VPP 线程/大页/低延迟，展示语句清单确认后经既有语句提交并 apply；非 TTY 拒绝不挂起；
   不绕过任何校验。命名从 setup 改 **wizard**：oper 无歧义前缀规整会把 `set` 展开成 `setup*`（真机单测抓到）。
   新增 /metrics 采集 `nfvis_system_cpu_online_count`。pty 全流程/非 TTY/前缀回归三向验证。
   证据 `docs/evidence/v1-closeout-round28-setup-wizard.txt`。
9. **（第二十九轮）发布 v1.1.19**：发布校验的 purge 环节抓到发现 #1 的修复**从未在真实 purge 下生效**
   （postrm 依赖包内脚本，dpkg 先删文件再跑 postrm → 撤销被静默跳过；决策 #108 修复内联 + 红-绿验证）；
   实装+purge+重装全过、配置库逐字节一致；待办 §0 第 3 项随此完成。
   证据 `docs/evidence/v1-closeout-round29-release-1.1.19.txt`。 **发布当日因手册重写（第 10 条）重切一次**：删原 release/tag 重建，sha 0489bc13… → 7081c88c…（构建提交 f12ffcc → ddbe55b，二进制无差异、仅随包文档），回下载一致；详情见 round29 §5。
10. **（第三十轮）用户手册重写**（945 → 1359 行）：新增快速上手/平面划分/CLI 基础/wizard 向导/
    内核基线详解/重启后动作清单/故障排查分组扩表；**命令准确性机器验证**——39 条 show 真机执行
    0 失败、101 条 set/delete schema 验证 0 错误。证据 `docs/evidence/v1-closeout-round30-manual-rewrite.txt`。
11. **（第三十一轮·Part A）内核基线脚本降级为内部件**（决策 #109）：
    操作者入口收敛为 `wizard` + `request system kernel apply/rollback`；手册删「安装器脚本」路径，
    脚本仅保留 postinst 首装引导与「无守护进程救急」两场景（wizard 三条件不满足，无法取代）；
    postinst 大页指引改指 wizard。Part B（ISO）已于 round32 完成，见 §0.1。**剩余候选**：交互式向导（若要，放 CLI 而非 postinst）。
12. **（第三十二轮）NFViS 安装 ISO（Part B，决策 #110）**：Ubuntu 26.04.1 官方 ISO 按自报配方全树重建
    （EFI El Torito 指向附加分区字节区间，indev/outdev keep 编址不了）；离线闭包 243 包（USTC）+
    VPP 26.06（9）+ nfvis deb 随盘，装机期以**本地 file 仓库 apt 解析**安装（dpkg 直灌会被候选组
    展开出的互斥变体族坑死——-hwe 双变体、cron/bcron/systemd-cron 等，两轮装机实证）；
    嵌套 KVM 气隙（-nic none）装机全链路通过：GRUB opt-in → autoinstall → 重启后 nfvisd active、
    journal 取一次性口令登录 CLI `show version` → NFViS 1.1.20、wizard 非 TTY 即返、串口控制台
    （装机与装好系统同口径）、vpp disabled；装后逐项验证 **19/19 PASS**。手册增「方式 C」（§2.4，
    原 §2.4~2.6 顺延）；证据 `docs/evidence/v1-closeout-round32-install-iso.txt`。
    ⚠️ 期间 nfvis-vm 被误恢复为干净快照，现场凭仓库脚本全量重建（USTC 源/工具/闭包/ISO 校验）；
    qemu -m 4096 曾疑致宿主 OOM 冻结，装机验证用 **-m 3072**。
13. **（第三十三轮）发布 v1.1.20**：发布件 `nfvis_1.1.20_amd64.deb`（sha256 `7bc77cce…`，main `b138700`
    + SOURCE_DATE_EPOCH=1789830771）；校验复刻 round29 口径全过——元数据/内容/解包 -version、
    实装（首装路径：服务 active、一次性口令登录 `show version`、wizard 非 TTY 即返）、
    purge（决策 #108 内联撤销在真实 purge 下生效、GRUB/fstab 手工指引、数据保留）、
    重装（SSH 强化回归、**配置库逐字段一致**：audit/rev/config_json sha）、回下载 sha 一致。
    **安装 ISO 同轮交付**（3,114,321,920 字节，sha256 `19e1c492…`）——超 GitHub 单文件 2GiB 限制
    不挂 Release asset，按手册 §2.4 方式 C 本地构建；AGENTS 发布清单补记 v1.1.19。
14. **（第三十四轮）废除 ISO（决策 #111）+ bind-dpdk 模块自理（决策 #112）+ 人工模拟全流程**：
    用户裁决废除安装 ISO（理由与保留项见决策 #111：交付工程复杂度不属产品能力、首启缝隙在
    deb 路径同样存在）；`deploy/iso`、`contrib/iso`、`make iso` 移除，手册删「方式 C」并还原编号。
    bind-dpdk 改为缺则自动 modprobe、成功后持久化 `/etc/modules-load.d/nfvis-vfio-pci.conf`（决策 #112，
    单测五例 + 向导完成语/手册 §5.4/§7.2/§7.5/§11 同步）。随后按用户要求**人工模拟**「deb 装机 → wizard
    → 数据面 → VNF 流量」全流程（目标机 D:\Virtual Machines\nfvis，VMware + vmrun 操作），
    发现问题即时修复。**结果：全流程跑通**——底座→deb→postinst→一次性口令→wizard（修假绿后全绿）→
    重启基线生效（1G/2M 页 + isolcpus + IOMMU）→绑定（无需手工 modprobe）→声明→数据面起面→
    镜像导入→L2 交换机 + 两台 VNF（cloud-init 文件路径）→**流量核对：BVI ping 3/3 收、
    宿主经 DPDK 物理口直连 guest 0.35ms/0.9ms**。共修复 6 项（决策 #111~#114）：ISO 废除、
    wizard 导航/判定、bind 模块自理、user-data 文件与类型校验、instance-id 摘要 + 启动前 seed 重建、
    VPP 主堆大页；登记未修 3 项（CLI 引号内 `|`/多行值、restart 后立即查询竞态、大页池无主占用回收）。
    证据 `docs/evidence/v1-closeout-round34-manual-flow.txt`。
15. **（第三十五轮）发布 v1.1.25**：发布件 `nfvis_1.1.25_amd64.deb`（sha256 `8f623392…`，main `741801a`
    + SOURCE_DATE_EPOCH=1789876284；1.1.21~1.1.24 为 round34 内部验证构建，沿用跳号惯例）。
    校验复刻 round29/33 口径全过：元数据/内容/解包、升级实装（1.1.24→1.1.25，**升级后 VNF 拓扑存活**）、
    purge（SSH 撤销自动生效、数据保留）、重装（**配置库逐字段一致**：audit 74/rev 35/cfg sha）、
    重装后拓扑收敛 + 流量复通（宿主经 DPDK 物理口 0.47ms）、回下载 sha 一致。
    证据 `docs/evidence/v1-closeout-round35-release-1.1.25.txt`。
    证据 `docs/evidence/v1-closeout-round33-release-1.1.20.txt`。
16. **（第三十六轮）三件套复跑**（2026-09-22，main `a218d4e`）：`make integration` **全绿**
    （238 PASS 含子测试 / 0 FAIL / 0 SKIP；`test/integration` 772.404s）；256 条冒烟对**已装实例**
    171/26/3——26 条逐条判读后**全是前置级联**（根因 A：阶段 2 预块第 1 条是空操作、而 `-c` 脚本
    「任一行失败即停止」→ 其后语句全没执行；根因 B：1G 大页池被两台已声明 VNF 占满、
    产品**正确地**拒绝再分配 cli-vm），对**干净开发态基线**复跑 **194/1/5**，唯一失败是已登记的
    `show vpp runtime`，级联全部消失、被掩盖的 19 条命令全部通过；语义校验 **11/0/0**
    （先修 3 处**工具假红/假报**：`show l2fib` 非 verbose 只有汇总行、`vppctl` 输出 CRLF 未剥、
    S9 的"未列出"断言不成立；另修收尾残留 `interfaces bond0` 空壳）。
    **未改产品代码**（改动只有 `contrib/scripts/` 两个脚本 + Makefile 的 toolcheck）；
    新增 `cli-semantic-selftest.sh`（桩 vppctl 自校准，已并入 `make check`）。
    环境已恢复（配置与基线逐字节一致、拓扑在 VPP、两台 VNF 在跑、BVI ping 双向 3/3、
    宿主经 DPDK 物理口 <1ms）。证据 `docs/evidence/v1-closeout-round36-three-suites.txt`。
17. **（第三十七轮）V2 Web 控制面增量 1：只读总览**（决策 #115，2026-09-22）：内嵌进 nfvisd
    （`internal/api/ui/`，Go `embed`）**同源**托管于 `GET /api/v1/ui/`（`/ui` 302 到 `/ui/`）——
    同源换来**不需要 CORS**、deb 里**不需要额外部署件**；前端**免构建**（原生 HTML/CSS/JS，无 npm，
    产物入库）⇒ `make check`/CI/deb 可复现/离线口径全不变。静态资源**无鉴权**（与 `/metrics`、
    `/openapi.json` 同例：登录页必须先能加载），数据走既有 REST + Bearer，token 存 sessionStorage。
   只读一屏：系统（数字取自 `/metrics`）/组件版本/资源池/数据面/接口/虚拟机/容器/告警；
    实时刷新用既有 `GET /events`——**浏览器的 `EventSource` 不能自定义请求头**，故前端用
    `fetch` + 流式读取手工解析 SSE 帧（token 仍走头部，不进 URL），流断了退化 5 秒轮询。
    守护两处：UI 路由放 `/api/v1` 下以落进 `routes_contract` 契约守护；`user_text` 判据
    **扩到 `.html/.js/.css`**（UI 文本会到达操作者；口径取操作者读物那套，新增三种按行剥注释实现）。
    真机验证：静态资源/安全头/鉴权边界/SSE 真实事件（`config-committed` 帧）/页面读的 7 个指标名
    全部命中/`node --check` 语法 OK/`parseMetrics` 喂真实输出取值逐位一致。
    **副产物 R37-1**：发现 REST 资源 API 的实际响应比契约声明的薄（见 §0 当前下一步第 1 条）。
    证据 `docs/evidence/v1-closeout-round37-web-console.txt`。
18. **（第三十八轮）R37-1 收口：契约重复定义 + 实现未落地声明字段**（决策 #116，2026-09-22）：
    逐条查清后发现是**两类**根因——① **契约自身重复定义**：`components/schemas/VppStatus` 在 YAML 里
    定义了两次（一处准确、一处过时），而 `check_openapi_json_sync.sh` 用 `yaml.safe_load` 生成内嵌 JSON
    **静默保留最后一个** ⇒ **发布的规范带的是过时形状**（定位手法：按行正则必出假阳性——契约里有
    `{name}:start` 这类含冒号路径、内联 map、列表项，实测报 62 处"重复"，真重复只有 1 处；
    改用**真正的解析器**逐层查重才得到唯一答案）；② **实现未落地契约声明的字段**
    （`/system/status` 少回 cpu/memory/hugepages/storage、`/interfaces` 少回运行态）。
    **处置**：删重复定义；**补实现**（`/system/status` 的 cpu/memory/storage 取
    `metrics.HostMetrics()`——**与 CLI `show system cpu|memory|storage` 同源**，hugepages 取
    `resourcePoolView`——与 `/resource-pools` 同源；`/interfaces` 补 kind/enabled/link/speed_mbps/driver，
    来源与 `show interfaces physical` 同源；`/vpp/status` 补 config_revision）；契约侧修正
    `storage`/`hugepages` 形状（原形状无数据源/表达不了两个池）；**新增两道守护**——
    契约 YAML **重复键查重**（入 `make docscheck`）与**响应形状核对**（按契约 schema 逐字段核对，
    字符串还须非空；会合法缺席的进显式白名单并写明理由）。
    真机验证：`system/status.cpu.isolated` 与 `resource-pools.cpu.isolated_cores` 逐项相同、
    两处 `hugepages` 数组逐字段相同、`/interfaces` 与 `show interfaces physical` 的
    up/up/10G/dpdk **逐项一致**。**另登记 R37-2**：`/system/version` 是桩（6 个组件版本全空串）。
    证据 `docs/evidence/v1-closeout-round38-contract-shape.txt`。
19. **（第三十九轮）Web 控制台可视验收 + 三处缺陷收口**（2026-09-22，决策 #117）：用 Browser Use
    插件（本机已启用，会话启动时加载）对 round37 的只读总览做黑盒可视验收——**首次有人眼看过页面**。
    发现三处缺陷：CSS `hidden` 被 `.login-wrap` 的 display 覆盖（登录后登录卡片盖住总览页）、
    `/login` 响应 `user` 未按契约回对象（顶栏 "undefined（undefined）"）、`uptime_seconds`
    取守护进程而非主机运行时长。全部修复并新增两道守护（含变异验证），`make check` 全绿；
    真机重跑浏览器全套验收点（错口令/正确登录/六卡真实数据/实时通道已连接/commit 触发事件刷新/
    退出回登录页/刷新后保持登录）通过；环境复原（拓扑在跑、BVI ping 5/5、宿主到 guest <1ms）。
    证据 `docs/evidence/v1-closeout-round39-web-console-visual.txt`。
20. **（第四十轮）R37-2 收口：组件版本探测**（2026-09-22，决策 #118）：`GET /system/version`
    与 CLI `show version` 从"桩/只印 NFViS"改为**真探测**——ubuntu 读 os-release、
    libvirt/qemu/docker 经底座命令探测、vpp 与 `/vpp/status` 同源；**DPDK 无可靠来源**
    （静态链接进 VPP 插件、binary API 不含），有意不汇报。口径"只汇报探测到的键"，
    形状守护白名单 6 条缩到 1 条（其余经注入假源真校，变异验证过）。真机六键全真值、
    浏览器「组件版本」卡不再成片「—」、冒烟阶段 1 仅剩已登记的 `show vpp runtime`。
    证据 `docs/evidence/v1-closeout-round40-system-version.txt`。
21. **（第四十一轮）前端简化：系统数字改读 /system/status**（2026-09-22）：app.js 删掉
    手写 `/metrics` Prometheus 文本解析（R37-1 收口后同源数字直接可取），系统卡片全部
    改读 `/system/status`，每次刷新少一个端点。守卫覆盖迁移：删指标名守护（前端已不读
    /metrics），字段名守护扩到系统卡嵌套键（Linux 执行确认 + 变异验证）。真机事实源
    对照逐项一致、SSE 事件刷新无回归。证据
    `docs/evidence/v1-closeout-round41-web-system-card-rest.txt`。
22. **（第四十二轮）CLI⇄REST 覆盖核查**（2026-09-22）：回答"Web 控制台能否覆盖 CLI 全部功能"——
   258 个命令形态逐一对照 91 个 REST 端点：**225 个已有类型化端点、14 个真缺口、19 个
   CLI-only by design**（wizard/monitor/REPL 导航/管道渲染等形态差异；**round47 后为 231/8/19**）。配置模式 113 条
   set/delete 语句经 candidate API 一轮全覆盖。缺口集中在：committed 全量读取、系统日志、
   ping/traceroute、两处 statistics 响应字段、clear counters、ssh host-key、core-dumps 导出、
   commit check、load merge，以及两项已登记延期。产出 `docs/CLI-REST覆盖核查.md` +
   `cli_rest_coverage_test.go`（三张表 + 端点存在性 + 新命令归类断言，变异验证过）。
   附带发现：命令全表 §3 统计表的分类数字与实际行数不符（show 实为 65、其余 10、配置 128、
   合计行数 258），只登记未改。
23. **（第四十三轮）增量 2 的 API 侧收口**（2026-09-22，决策 #119）：round42 核查指出增量 2
   唯一阻塞项是 `show configuration`（committed 全量读取）无端点——本刀新增
   `GET /configuration`（`{configuration, revision}`，ClassReadOnly + 脱敏），并把
   `GET /configuration/candidate` 的契约按实现形状（`{candidate, dirty}`）对齐、收进形状守护；
   同时收口**已知缺陷 #19**（`POST /logout` 释放 candidate 锁，与 CLI Teardown 对齐）。
   四条新用例（含 #19 的变异验证）；真机复验：committed 真值、无口令哈希泄露、API 取锁登出后
   CLI 立刻可提交、与 CLI `show configuration` 同源对照。证据
   `docs/evidence/v1-closeout-round43-config-api.txt`。
24. **（第四十四轮）增量 2 第二刀：控制台配置读写页 + R44-1 收口**（2026-09-22，决策 #120）：
   配置卡（committed 版本 + JSON 预览）→「开始编辑」建 candidate → 系统节表单/原始 JSON
   编辑 → 差异预览 → 提交（成功即释放会话锁）/丢弃。浏览器黑盒验收全流程通过（每步截图 +
   只读 DOM 印证；CLI 侧对照 committed 逐项一致）。**验收抓到并修复 R44-1**：脱敏视图回写
   抹掉口令哈希 → 提交锁死账号（引擎继承 + 校验兜底；判据是"提交后原口令仍可登录"）。
   **登记 R44-2**：api 来源改管理口不需 commit confirmed（FR-CFG-012 原文只约束 SSH），
   建议评估纳入。证据 `docs/evidence/v1-closeout-round44-web-config-page.txt`。
25. **（第四十五轮）R44-2 裁决：管理口自锁保护扩到「经网络接入的会话」**（2026-09-22，决策 #121）：
   判据从"来源是不是 ssh"改为"会话是否经网络接入"——ssh 与 api（REST/Web 控制台）都要求
   `commit confirmed`，本地串口豁免；FR-CFG-012 文本、契约（400 `CONFIRM_REQUIRED`）、
   AGENTS 计数同步。原把旧口径固化的 `TestAPISessionNotGated` 改写为按新口径断言，新增 API 级
   用例（PUT 管理口变更 → 普通提交 400 → confirmed 提交 200 + 待确认 → 确认生效）；
   **变异验证**：判据改回 `== "ssh"` 时两道用例分别报红。浏览器复验 confirmed 全流程走通
   （被拒 → 待确认（含自动回滚时限）→ 确认生效）。证据
   `docs/evidence/v1-closeout-round45-mgmt-confirm-scope.txt`。
26. **（第四十六轮）配置页向导化 + `commit check` 预校验**（2026-09-22，决策 #122）：
   缺口 #10 收口——新增 `POST /configuration/check`（调 `Engine.CommitCheck`，不落库不下发；
   校验发现问题仍 200，结果在 `{ok, errors[]}`；未持 candidate 报错不谎报通过）；
   配置页表单扩到系统/接口/资源池三节（启用三态、隔离核 `2-5,7` 文本解析/格式化）。
   浏览器验收：三节表单改值 → 预校验（正例通过、反例逐条列问题）→ 提交（revision 4，
   含 resource-pools 需 reboot 的警告）；CLI 侧 `show configuration` 逐项对照一致。
   证据 `docs/evidence/v1-closeout-round46-config-forms-check.txt`。
27. **（第四十七轮）增量 3 第一刀：诊断视图**（2026-09-22，决策 #123）：缺口 #2~#4/#7 收口——
   新增 `GET /system/logs`（text/plain，`?last=n`；与 `show log system` 同源、未接入 503）、
   `POST /diagnostics/ping`（**未通即 502**，失败时原始回显逐行进 detail）、
   `POST /diagnostics/traceroute`、`POST /interfaces:clear-statistics`（204）；控制台新增
   「诊断」卡。真机与 CLI **逐字一致**（含"未通即失败"的判定文案）；浏览器验收通过；
   环境复原（含 `systemctl restart vpp` 清开发态残留 BD）。证据
   `docs/evidence/v1-closeout-round47-diagnostics.txt`。
28. **（第四十八轮）发布 v1.1.26**（2026-09-22，main `83a4c3a`）：发布件
    `nfvis_1.1.26_amd64.deb`（sha256 `f06539d4…`，7,828,928 字节，
    `SOURCE_DATE_EPOCH=1790085452`；**连打两次哈希一致**）。发布内容是 round37~47 的
    **V2 Web 控制面增量 1~3 第一刀 + 五处收口**（决策 #115~#123）——首次进入发布件。
    校验复刻 round29/33/35 口径全过：元数据/内容/解包 -version；升级实装（**既有 VNF 拓扑存活**、
    配置库逐字段未变）；**REST/Web 内容实测**（三个静态资源与仓库源码**逐字节一致**、登录回对象、
    `/system/status` 契约字段齐全且 uptime 为主机时长、`/configuration` 无口令哈希、ping 未通 502、
    `clear-statistics` 204 且计数清零后可再增）；purge（SSH 撤销自动生效、数据保留）；重装
    （**配置库逐字段一致** rev 55/audit 103/cfgsha 084c3a64…）；重装后拓扑收敛 + 流量复通
    （BVI ping 双向 5/5、宿主经 DPDK 物理口 0.33~0.47ms）；回下载 sha256 一致。
    登记 R48-1~R48-5（工具假红 / SSH host key 陈旧 / 首装不自动 start 的口径 / 候选 409 未枚举 /
    **随包手册 §10.12 落后于增量 2、3**）。其中 **R48-5 触发重切**：手册随 deb 发布，
    而它仍写着「Web 界面（只读总览）」「本页面不修改任何配置」——修手册（按界面上真实有的按钮
    重写）+ 交接文档 §1.1 同步，经 PR #156 合并后**用新 main 重建并重切发布件**
    （`f06539d4…` → `1f8799ff…`，两次构建逐文件对照**只有用户手册不同、二进制逐字节相同**，
    故装机链路结论不变）。证据 `docs/evidence/v1-closeout-round48-release-1.1.26.txt`（§7 为重切记录）。
29. **（第四十九轮）CLI⇄REST 覆盖收口：增量 3 剩余缺口 4 项**（2026-09-23）：① VS/VM 详情 statistics 响应字段（决策 #124）
    ——与 CLI 同源、运行态取不到就不给、不编造；② `POST /system/ssh-host-key:regenerate`（决策 #125）——与 CLI 同一实现、
    审计同码、失败 500 不谎报；③ **core-dumps 导出真实现 + `POST /system/core-dumps:export`（决策 #126）**——顺带修掉
    CLI 侧「只打印已受理、没发请求」的**假成功**（命令全表此前标 ✅），真机用接收端证明清单真的发出；
    ④ `PUT /configuration/candidate` + `X-NFVIS-Merge: true`（决策 #127）——merge 语义进 REST。
    覆盖 226→**236** / 缺口 13→**3**；每条都做了单测 + **变异验证** + 真机验证。
30. **（第五十轮）发布 v1.1.27**（2026-09-23，main `163e4d6`）：发布件 `nfvis_1.1.27_amd64.deb`
    （sha256 `2fafd93b…`，7,839,554 字节，`SOURCE_DATE_EPOCH=1790128941`；**连打两次哈希一致**）。
    校验复刻 round29/33/35/48 口径全过，并新增一项**「随包文档是当前版本」**（round48 的 R48-5 教训）；
    **本版新内容实测**（statistics 与 CLI 同源、core-dumps 导出接收端真的收到清单、`X-NFVIS-Merge` 保留未提及字段、
    ssh host-key 端点 403 边界）；升级/purge/重装三步配置库逐字段一致；重装后拓扑收敛 + 流量复通
    （BVI ping 双向 5/5、`vppctl` 独立源 3/3、宿主经 DPDK 物理口 4/4）。
    登记 R50-1（**校验脚本误改生产库** → 按 §3.9 快照整库恢复）与 R50-2（脚本 stdin/heredoc 坑）。
    证据 `docs/evidence/v1-closeout-round50-release-1.1.27.txt`。
31. **（第五十六轮）seed 端到端取证：`#139` 改动正确、但 `#22` 未收口**（2026-09-23，main `94a3a20`；**未改产品代码**）：
    把上一轮留下的「下一步」做完——产品路径两次运行（**旧 1.1.28** 的 SATA 光盘 vs **main** 的 virtio 磁盘），
    `qemu-nbd` 挂盘副本离线读 guest 的 `/etc/hostname`、`/var/lib/cloud`、`/var/log/cloud-init.log`，
    **并先用 Debian guest 做阳性对照**（同一份 seed、同一种交付 → `Using data from /dev/vdb`、hostname/marker 均生效）
    自校准取证方法。结论：① **#139 生效**（seed 现为 `device=disk/bus=virtio` 的 `vdb`，宿主 `domblklist`
    与 guest `virtio_blk virtio1: [vdb]` 双向印证）；② **但 alpine.qcow2 上 cloud-init 仍不消费 seed**；
    ③ **#22 记的「Alpine 缺 `ahci`」是把 genericcloud 的结论错误外推**——实测 alpine 的 6.6.31-0-virt
    内核 `ahci` 是 builtin、**旧 SATA 光盘也可见**（`sr 0:0:0:0: [sr0]` + `Attempting to use data from /dev/sr0`），
    失败在**挂载**。**根因（guest 侧）**：cloud-init 只接受 `TYPE ∈ {vfat, iso9660}` 且带 cidata 卷标的设备、
    固定用 `mount -o ro -t auto`；Alpine 的 `/bin/mount` 是 **busybox**，其 `-t auto` **只认 `/proc/filesystems`
    里已加载的类型**（那一刻只有 ext3/ext4＝根盘驱动）→ `Invalid argument`；guest 内实测：**`modprobe isofs`
    之后 `-t auto` 立刻成功**（唯一原因即「驱动未加载」）。**宿主侧无解**（产品只控制域 XML）。
    处置：**决策 #140**（#139 保留 / #22 改记「部分收口」/ 限制如实登记不追）+ 随包文档更正
    （手册 §9.2③、§11 排查表、§12 限制表）+ **附录 A 表格结构守护**（`check_decisions_count.sh` 加两道检查，
    红-绿验证过——顺带修掉 **#130 行自 PR #170 起被劈成两半**：行内是真 CR 字符、后来被规范成换行，
    **v1.1.28 的随包规格书带此缺陷**而条数守护照样全绿）。登记 R56-1~R56-4
    （含 REST 未知字段静默忽略、旧二进制那组一度打到新实例上）。
    证据 `docs/evidence/v1-closeout-round56-seed-e2e.txt`。

**下一步（按优先级；每条都写清"怎么跑"）**：

### 1. ✅ 已完成（2026-09-18 第二十四轮）：`make integration` **全绿**

   231 条断言 PASS / **0 FAIL** / 0 SKIP（`test/integration` 781s；`make integration` 内已显式 `-timeout 45m`）

三条原失败用例的**根因是两条，且都不是「窗口太短」这么简单**（证据
`docs/evidence/v1-closeout-round24-integration-green.txt`）：

- **RC1：产品把 NoCloud seed 挂成 SATA 光盘，而 guest 内核缺 `ahci` → seed 不可见 → cloud-init
  `notfound=disabled` 静默自我禁用**（不是超时，是压根没跑）。四条证据：宿主 `virsh domblklist`
  里 seed 挂着 / guest `lsblk` 只有 `vda*`、无 `/dev/sr0` / guest 内核日志只有 `virtio_blk`、
  `ahci|libata` 命中 0 / guest 的 `ds-identify.log` 写着
  `FS_LABELS=` + `ISO9660_DEVS=` + `No ds found [mode=search, notfound=disabled]. Disabled cloud-init [1]`。
  镜像侧对照：`debian-12-genericcloud` 有 `ata_piix`/`isofs`/`sr_mod` 但**无 `ahci`**；
  **⚠️ 更正上一轮 #17 的结论——genericcloud 不可能通过**，可用的是**完整内核**的
  `debian-12-generic-amd64.qcow2`（已按产品路径 `request images upload` 导入，sha512 与官方一致）。
  **⚠️ 但不要把这条结论外推到 alpine（round56 更正）**：alpine.qcow2 的 6.6.31-0-virt 内核 **`ahci` 是 builtin**、
  启动日志里 ahci 正常初始化，**SATA 光盘也并非不可见**（guest 有 `sr 0:0:0:0: [sr0]` 且去挂过它）。
  alpine 的失败另有原因——cloud-init 用 `mount -o ro -t auto`，而它的 `/bin/mount` 是 **busybox**，
  `auto` 只认内核**已加载**的文件系统类型（那一刻只有 ext3/ext4）；详见决策 #140 与
  `docs/evidence/v1-closeout-round56-seed-e2e.txt`。**两条结论各自成立、不可互换**。
- **RC2：测试的 `runcmd` 把 guest 网卡写死成 `eth0`，而 guest 里实际是 `enp1s0`**（systemd 可预测网名；
  产品模型里的 `eth0` 只是 VPP 侧 socket 的逻辑名）。证据：guest 串口
  `ci-info: | enp1s0 | ...` + `Cannot find device "eth0"` + `cc_scripts_user.py[WARNING]` +
  `cloud-final FAILED`；同一条日志里的 `Datasource DataSourceNoCloud [seed=/dev/sr0]` 证明 RC1 已修好。
- 另有一层**时间基线**：guest 的地址由 cloud-init **final 阶段**配，而该阶段 `After=network-online.target`，
  本用例的 BD 上没有 DHCP → `systemd-networkd-wait-online` 约 120s 才放行 → guest 到 **uptime ~135s**
  才有地址（串口实测 `modules:final ... Up 134.15 seconds`）。原先按 alpine 定的 60s/150s 窗口必然误判。

修法全在**测试侧与 Makefile**（未改产品）：镜像选取改为「只从已确认可用的镜像里挑，否则跳过并说明要求」
（`testVMImage`/`usableVMImage`）；5 处硬编码核池 `{1,2,3}` 改为按真机事实派生（`vmIsolatedCores`，即 #18）；
`runcmd` 改为按实际存在的非 lo 网卡配地址；等待窗口按上述实测基线放大；串口诊断改为与 ping 等待**并行持续回读**、
失败时把整段 guest 串口带进错误信息。

**新登记、需决策（本轮未动产品）**：#22 seed 以 SATA 光盘交付，缺 `ahci` 的 guest 内核看不到且 cloud-init
静默自禁（**该条对 `debian-12-genericcloud` 成立**；alpine 的情况见上面的 ⚠️——它是 busybox `mount` 的问题，
round56 已查清并登记为决策 #140）；#23 模型接口逻辑名（`eth0`）
与 guest 内核网名（`enp1s0`）不一致，手册宜加一句说明。详见 §2.13。

跑法（现在可直接复跑，全套约 13 分钟）：

```bash
ssh nfvis-vm 'systemctl stop nfvis; systemctl restart vpp; sleep 8'   # 避免两个 actor 驱动 VPP（§3.6）
ssh nfvis-vm 'cd /root/src && GOPROXY=https://goproxy.cn,direct make integration'
ssh nfvis-vm 'systemctl start nfvis'                                   # 跑完把服务起回来
```
⚠️ 跑前确认 **1G 大页 free ≥ 1**（`cat /sys/kernel/mm/hugepages/hugepages-1048576kB/free_hugepages`）：
残留 VM 会占着它，导致 VM 起不来报 `Cannot allocate memory`（我为此白跑过一轮）。
⚠️ 镜像须是「guest 的 cloud-init 挂得上 seed」的那种：本机用 `debian-12-generic-amd64.qcow2`（缺省自动选中）；
`alpine.qcow2` **不可用**——原因是它用 busybox 的 `mount`（`-t auto` 只认已加载的文件系统类型），
不是缺 `ahci`（round56 实测更正，见决策 #140）；`debian-12-genericcloud-amd64.qcow2` 缺 `ahci`，
**SATA 光盘交付下不可用**，virtio 交付（决策 #139）后应可用（**未在集成测试里复验**）。
选定不可用的镜像只会得到超时。

### 2. ✅ 已完成（2026-09-22 第三十六轮）：256 条冒烟跑**整轮**（含阶段 3）

**round36 结果**：对**已装实例** 171 通过 / 26 失败 / 3 预期报错，26 条**全是前置级联**；
对**干净开发态基线**（脚本头部记载的默认目标）**194 通过 / 1 失败 / 5 预期报错**，
唯一失败 = 已登记的 `show vpp runtime`。逐条判读见 `docs/evidence/v1-closeout-round36-three-suites.txt` §3/§4。

⚠️ **两个前置在快照恢复后会丢，跑前要重补**（round32/34 快照恢复后本机就都没有了）：
- **引导镜像** `/var/lib/nfvis/images/alpine.qcow2`（阶段 3 要用）——按 round23 记录的产品路径拉：
  `request images download name alpine.qcow2 type vm-image url https://dl-cdn.alpinelinux.org/alpine/v3.20/releases/cloud/generic_alpine-3.20.0-x86_64-bios-cloudinit-r0.qcow2 sha256 900c91ed360beac704158ee99f45bab49e97858f8cb37469d0cc30e8e8e2ca60`
- **容器镜像** `alpine:3.20`（阶段 3 `docker save` 要用）——docker.io 直连不通，须给 **docker 守护进程**
  临时加宿主代理 drop-in（`HTTP(S)_PROXY=http://192.168.155.1:2333`）→ 拉完**立刻删掉 drop-in 并重启 docker**
  （避免给容器注入代理环境变量；用 `systemctl show docker -p Environment` 确认已清空）。

⚠️ **对已装实例跑时**：阶段 2 预块第 1 条 `set resource-pools hugepages page-size 1G count 2` 在"池里
本来就是 2"时是**空操作**，而 `-c` 脚本模式**任一行失败即停止** → 预块其后语句全不执行，会连累
阶段 5 的 vs-l2/vs-l3/acl-test 视图（6 条）与带 source 的 ping（1 条）。同理，若 committed 里还驻留着
两台 VNF（各占 1 个 1G 页），阶段 3 的 `cli-vm` 会因**资源池空闲 0** 被**正确拒绝**，连累阶段 4 的 12 条。
**要拿"套件本身是否全绿"这个结论，就跑干净开发态基线**（§1.5 第 2 条的口径）。

```bash
ssh nfvis-vm 'cd /root/src && SRV=https://127.0.0.1:443 CLI_BIN=/usr/bin/nfvis-cli \
    NFVIS_PASSWORD="WBF81vOA4M8GM28f@Aa1" API_PORT=443 bash contrib/scripts/cli-fulltest.sh'
```
（脚本已支持环境变量，**不必再用补丁副本 `/root/ft`**，见 #5 的修复。）
上轮只跑过阶段 1/2/4/5/6（162 通过 / 28 失败 / 3 预期报错，28 条全为已知类）；**阶段 3 需引导镜像**，现在可以补。
注意：套件会 commit 配置（含 `system api port`），跑完核对 `show configuration` 并按需清理；跑前建议先备份配置库（§3.9）。

### 3. 生命周期与卸载残留复验（P7）

`dpkg -i` 升级 → `--purge` → 重装，核对：① 配置库全程未变（§3.9 先备份再比对 revision/用户/审计）；
② **发现 #1 的三处底座残留已被 postrm 自动撤销**（SSH drop-in ✓ 自动；GRUB/fstab 打印指引）；
③ 发现 #2（手册前置）、#20（deb 未声明 qemu-img/cloud-localds 依赖）顺带核。

### 4. 未修的发现（都已在 §2.13 登记，不阻塞上面三项）

- **#19（中）**：API `POST /logout` 只吊销 token、**不释放 candidate 锁** → CLI 会被 `admin@api` 挡到空闲超时
  （我这轮被挡过一次，靠 `DELETE /configuration/candidate` + 重新提交解开）。修法：logout 时也 `discard`（与 CLI 的 Teardown 对齐）。
- **#20（低）**：deb 的 `Recommends` 补 `qemu-utils, cloud-image-utils`。
- **#21（中）**：手册「大页数量规划」补一句「1G 页要按 VPP + 每台 VM 至少 1 页规划；VPP 可用 `memory hugepage-preference 2M` 让页」。

### 5. 环境受限、非代码可补（如实登记，别当待办追）

SR-IOV（无 PF/VF）、LLDP 邻居（无对端）、console 真人 `Ctrl-]`（需运行中 VM + 真实 TTY）、
§10 吞吐/容量类基准（需流量发生器 + 10GbE 目标硬件）、FR-NET-022 容器侧 memif 通流（需自建带 memif 客户端的镜像）。

**红线（上一轮踩过，勿重蹈）**：

1. **永不拿管理路径做试验**（`ens160`）：先用非管理口验证守卫与流程，或先用内核事实确认哪个口不能碰。
2. **换二进制前先确认谁在服务**：`for p in $(pgrep -f nfvisd); do readlink -f /proc/$p/exe; done`（`pkill -x` 杀不掉改名进程）。
3. **量退出码不经管道**（`| grep` 拿到的是 grep 的退出码）；重定向到文件再读。
4. **heredoc 会吃掉反斜杠与 `\n`**（上一轮又踩三次：`\b` 变成退格字节让守护**静默失效**、两处 `\n` 变成真换行）。
   带转义或中文的逻辑**先 Write 落文件再执行**；发现"异常干净的 0 命中"要换工具复核。
5. **改完必须 `make check` + 真机复验**（涉 `show` 族/候选来源还要跑 `cli-semantic-check.sh`），再 PR 合并。
6. **`make integration` 前**：`systemctl stop nfvis`（否则守护进程与测试同时驱动 VPP）+ `systemctl restart vpp`；
   另确认 **1G 大页 `free ≥ 1`**（round34 起机器上常驻 VNF 拓扑会占页，先停 VNF 或缩池）。
7. **qemu-nbd 只读挂 guest 盘后必须 `--disconnect`**：否则 VM 再启动报 `Failed to get "write" lock`
   （round34 实测踩中；挂盘取证是排查 guest 侧问题的有效手段，用完要收干净）。
8. **cloud-init 的两条硬规矩**：shellscript 段是**直接 exec**、脚本必须带 shebang（缺了报
   `Exec format error`，且 user-data 静默无效）；user-data 有**多行或 `|`** 时走**文件路径**
   （`set cloud-init user-data /data/incoming/x.sh`）——CLI 引号内 `|` 会被当管道、多行只取首行（已登记未修）。
9. **改 user-data 后要让它生效**：cloud-init 按 instance-id 判 once-per-instance；产品现已把输入摘要并入
   instance-id 并在 `start/restart` 前重建 seed（#114），但**改文件内容后必须触发一次 start/restart**
   （或重建 VM）才会重放。
10. **宿主内存压力下 Go 测试会"假红"**：Windows 上并行 `go test ./...` 报
    `VirtualAlloc … errno=1455 / out of memory`（`make check` 随之整体红，日志里无代码级 FAIL）——
    **`go test -p 1 ./...` 串行可过**（实测 17 包全 ok）；判读先搜日志里有没有 `errno=1455`。
11. **别让自己的 grep 藏证据**：本会话多次因过滤输出把"第一次已成功"滤掉、误判为失败或无变化——
    核对状态用 `show` 原样输出、文件时间戳与计数，而不是只看过滤后的片段。
12. **第 9 条的反面同样成立（round36 实测踩中）**：**user-data 内容没变时，`start/restart` 不会重放**
    ——instance-id 摘要没变，cloud-init 就当"同一实例、已跑过"跳过；而 guest 自身启动的网络栈只拿到
    VMnet8 的 DHCP 地址，脚本里 `ip addr add` 的静态地址是运行期的、**guest 一重启就没了**。
    症状：VNF 起来了、VPP 里端口也 up，但 BVI ping 不通、宿主的 `.155.24x` 也不可达。
    复通办法：**改一次 user-data 内容**（哪怕只加一行注释）再 `restart` → instance-id 变 → 重放。
13. **判定"失败"前先确认判据本身是对的，且别只看一处**：round36 的 26 条冒烟失败**全部**是
    前置级联（池已满 / 预块首条是空操作 → `-c` 脚本"任一行失败即停止"→ 其后语句全没执行），
    而语义校验的 1 条失败是**脚本自己的 oracle 错了**（`show l2fib` 非 verbose 只有汇总行；
    `vppctl` 输出是 CRLF）。判读顺序：先看**级联关系**（后一条失败是不是前一条的结果）、
    再核对**事实源**（`vppctl` 原样输出 / `show configuration` / 配置库），最后才怀疑产品。
14. **口令/镜像这类"外部事实"不能只靠文档维护**：round32/34 两次快照恢复后，§3.1 的
    `Nfvis@Test2026` 与「阶段 3 前置已补齐」都成了过期信息（round36 两条都撞上）。
    **每次从快照重建后，重新核对并回写**：管理口令（§3.1 的取法）、`alpine.qcow2` 与
    `docker alpine:3.20` 是否存在、`rev/audit` 计数。
15. **`repr()` 分不清"真换行"与转义序列 `\n`（round47 实测被它骗过一次）**：写前端时一次
    Python 批处理把 JS 里的 `'\n'` 写成了真换行（语法错误），我用 `print(repr(seg))` "确认已修"——
    而 repr **对真换行与两字符的 `\n` 输出完全相同**，于是误判为已修、又白跑一轮。
    **教训**：判"文件里到底是哪种"要用**计数或结构化断言**（如 `s.count(chr(39)+chr(10)+chr(39))`），
    别用 repr 肉眼比对；这与"别让自己的 grep 藏证据"（红线 11）是同一类。
16. **写"改配置"的脚本前先核对**模型字段名**（round47 又一次）**：为造 ping 成功路径，我按
    直觉改 `virtual_switches[].gateway`，实际 L2 的 BVI 网关是 `gateway`、而 **L3 口在
    `vrfs[].l3_interfaces[].addresses`**（同名 vs 条目）——键名错则脚本静默什么都没改，
    看着像"配置没生效"。**先 `grep` 模型结构体再写脚本**。
17. **开发态实例的提交会在共享 VPP 里留下自己的对象**（round47）：dev 实例 apply 后数据口被
    移进它自己的 bridge domain（残留一个空 BD，宿主到 guest 的连通性随之降到 1/3）——这是
    "两个配置驱动同一 VPP"的**预期副作用**，按 §3.6 的既定做法 **`systemctl restart vpp`**
    清掉即可；**收尾核对要包含"VPP 里只剩配置声明的对象"**。
18. **字面转义被写成真控制字符，会以"文档结构"的形式藏起来**（round56）：规格书附录 A 的 #130 行
    自 PR #170 起**被劈成两半**——行里原本写的是**真 CR 字符**（作者当时就把字面 `\r` 的转义写坏了），
    后来某次编辑把它规范成换行：行尾断在「（补 `」、后半截掉到表尾成孤立行，**条数守护照样全绿**
    （它只数 `^\| N \|` 的行数），v1.1.28 的随包规格书带着这个缺陷发了出去。
    ✅ 已按 git 原文（`a369896`）接回，并给 `contrib/scripts/check_decisions_count.sh` **加两道结构检查**
    （决策行须以 `|`/`。` 收尾；附录 A 区间内不得有孤立行），红-绿验证过。
    **教训**：写含 `\n`/`\r`/`\t` 的文本后要**用计数或结构化断言**核对（红线 15），
    而且**"结构对不对"要单独守护**——数字对不代表结构对。

### 0.1 ⛔ Part B：NFViS 安装 ISO——**已废除**（决策 #111，round34；撤销 #110）

用户裁决废除安装 ISO 交付。**理由**：① ISO 的复杂度（候选组闭包裁剪、EFI 附加分区配方重建、
GRUB 菜单 opt-in 的自动化选择）全在交付工程侧，与产品能力无关；② 它暴露的首启缝隙
（vfio 模块未持久化、VPP 未起时 commit 硬失败）**在 deb 路径同样存在**——ISO 没减少问题，
只是把它们挪到装机期；③ deb 路径（手册方式 A）已覆盖生产安装。

**已移除**：`deploy/iso/`、`contrib/iso/`、`make iso` target；手册删「方式 C」并还原 §2.4~2.6 编号。
**保留（历史事实不改写）**：v1.1.20 release 及其说明；`docs/evidence/v1-closeout-round32-install-iso.txt`
（当时的气隙装机验证与踩坑记录——其中「候选组展开污染闭包」「dpkg 直灌 vs apt 解析」两条结论
对 deb/离线安装场景仍有参考价值）、round33 发布证据。


## 1. 当前状态

### 1.1 交付物

| 类别 | 内容 |
|---|---|
| 代码 | `cmd/{nfvisd,nfvis-cli}`、`internal/*`（模型/事务引擎/编排/API/AAA/system/…）、`deploy/`（systemd + deb + 安装器） |
| **用户文档** | [`NFViS-用户手册.md`](NFViS-用户手册.md)（安装→使用全流程，含故障排查）、[`NFViS-CLI命令全表.md`](NFViS-CLI命令全表.md)（256 条命令 + 逐条真机实测状态） |
| **Web 界面** | **V2 增量 1~3 第一刀（只读总览 / 配置读写 / 诊断，决策 #115~#123；随 v1.1.26 发布）**：随 nfvisd 内嵌发布，浏览器打开 `https://<管理地址>/api/v1/ui/` 即可，无需额外部署；免构建前端（`internal/api/ui/`，原生 HTML/CSS/JS），手册 §10.12 |
| 契约 | `NFViS-openapi.yaml`（REST）、`NFViS-CLI命令树完整设计.md`（CLI） |
| **覆盖核查** | [`CLI-REST覆盖核查.md`](CLI-REST覆盖核查.md)（round42：258 个命令形态 ⇄ 91 个 REST 端点逐族对照；**当前 236 覆盖 / 3 缺口 / 19 CLI-only by design**，逐轮收口见文内更新记录；`cli_rest_coverage_test.go` 机器守护） |
| 需求/验收 | 规格书（附录 A = 决策 1~91）、`V1-验收检查表.md`、`docs/evidence/`（各轮真机原始输出） |
| 工具 | 真机手动三件套（**互补；CI 都跑不了**）：`contrib/scripts/cli-fulltest.sh`（问「命令能不能用」）、`cli-semantic-check.sh`（问「**结果对不对**」，独立事实源 + 扰动判别）、`cli-pty-smoke.sh`（交互行为，必须 pty）。命令全表的实测状态由 fulltest 产出。另有两个**自校准**脚本（桩式、CI 可跑，已并入 `make check` 的 `toolcheck`）：`cli-fulltest-selftest.sh`（失败判定模式）、`cli-semantic-selftest.sh`（语义校验的 oracle，round36 新增——`SEM_SCRIPT=` 可指向改了 oracle 的副本做红-绿） |

### 1.2 V1 收尾各轮（PR #58~#79，决策 #69~#80）

| 轮次/PR | 内容 | 关键结论 |
|---|---|---|
| #58、#59 | 验收检查表（109 条 FR 逐条）；buffer 池 `vpp_get_stats` 回退源 | ACL 命中计数经实测**不可安全落地** → 降 V2（#68） |
| #60~#64 | 远程 syslog / 日志级别 / `openapi.json` / IPv6；**口令哈希泄露修复**；管理网卡身份 + 隔离强制；实机部署闭环（DPDK 接管 / 监听收敛 / 默认 HTTPS）；物理口 link 告警 | 关闭多条降级（#69~#73） |
| #67 | 列表端点分页（`api.paginate`，14 个端点） | #74 |
| #69 | **快照磁盘内容级回滚**（T0-5）：guest 内写 KNOWN→LATER→CHANGED→回滚→回读=LATER；**证伪**"运行中回滚会报错"（实为**替换 QEMU 进程**）→ create/rollback 改**要求关机态** | #75 |
| #70 | **全功能 CLI 测试**（逐条真机执行 197 通过/1 失败/2 预期报错）+ 命令全表；修复 7 处"契约声明了但 CLI 用不了" + 契约漂移 + 错误文案 | #76 |
| #72 | 用户手册 + **备份导出件权限泄露修复**（0644→0600，含口令哈希） | #77 |
| #73 | **`nfvis-cli` 缺省连接地址对齐守护进程**（缺省 `https://127.0.0.1:443`，自签证书自动固定→**零参数可连**）；`NFVIS_SERVER` 覆盖 | #78 |
| #76 | **§2.4 的 8 处 CLI 缺陷全部修复**（见 §2.4）+ 三处通用加固 | #79 |
| #79 | **首装/升级体验 6 处修复**（见 §2.6） | #80 |
| 本轮 | **CLI 交互三项缺陷修复**：raw 模式输出换行（候选列表/提示符阶梯错位）、Tab 多匹配不列候选、`?` 需回车才显示候选 | #81（含**三处验收误判**的更正：FR-CLI-001/002/003）→ 已发布 **v1.1.2** |
| 本轮 | **语句解析三处缺陷 + REPL 退出收尾**：实例名位置被误当子关键字（取值写到祖先层）、报错泄露 Go json 原文、`login user` 静默建无口令账号、交互 REPL 退出不收尾（会话残留配置模式） | #82 → 已发布 **v1.1.3** |
| 本轮 | **`<ifname>` 候选来源错误**：取自「已写进配置的接口名」而非运行态端口清单（漏掉已被 DPDK 接管的口、又列出不存在的名字）；候选按语义拆 vpp-ifnames/kernel-ifnames/ifnames 三个来源 | #83 → 已发布 **v1.1.4** |
| 本轮 | **show 族运行态口径修复**：`show virtual-switches` 列表/ports/statistics 取自配置（漏掉 VPP 中真实存在的 BD、`ports`/`statistics` 未实现）；`show interfaces physical` 缺契约明列的链接状态/速率/驱动且 Admin 取自配置；`\| display set` 四处宣称可用却未实现 | #84 → 已发布 **v1.1.5** |
| 本轮 | **操作者可见文本去内部引用**：CLI 帮助/报错/校验/告警/日志里的 `FR-xxx`、`§x`、`决策 #nn` 全部去掉（40 文件 132 行）；新增 archtest 守护；副产物是发现 5 处测试把泄漏固化成了期望 | #86 → 已发布 **v1.1.7** |
| 本轮 | **语义校验固化**：把「结果对不对」的校验落成 `contrib/scripts/cli-semantic-check.sh`（独立事实源 + 扰动判别 + round-trip）；AGENTS 自检清单与本文档 §4 同步 | #85 |
| 本轮 | **该脚本的前置自检**：CLI 不可执行时立即明确报错——发布 v1.1.7 时用默认 `CLI_BIN=/tmp/nfvis-cli`（不存在）跑出 **3/6 假失败**，实际产品正常 | #85 补丁（PR #93） |
| 本轮 | **守护扩到非 Go 文件 + deb 可复现构建**：① 操作者可见文本守护从 `.go` 扩到 `.sh`/`.service`/`Makefile` 的**非注释行**、`deploy/` 下的一切（`postinst`/`prerm`/`postrm` **没有后缀**，`dpkg -i` 直接打印其输出）与用户手册全文，清掉 **40 处**漏网的引用；② `make deb` 用 `SOURCE_DATE_EPOCH` + 暂存树 mtime 归一做成**可复现**（同源码同 epoch 连打两次逐字节一致，此前两次必不同） | #87/#88 → 已发布 **v1.1.8** |
| 本轮 | **真机三条反馈的根因修复**：① `ping` 只覆盖 VPP 数据面——原样转发 vppctl 输出且不报错，于是**一个包都没发出去却被冒烟判定算作通过**（真机实测旧版 `ping <管理口网关>` 退出码 0、判 ✓），现 0 发包即报错并点明平面归属，冒烟改为断言「判定自洽」；② 补全在「无子树参数消耗 token 后」**不回退到父层关键字**，`set system login user admin ` / `... management interface ens160 ` / `... ntp server 1.2.3.4 ` 一个候选都列不出来（`login class <name>` 因为把子节点挂在参数上恰好是对的——同一形态两种建模）；③ `set system login user <name>` 单独成句会**静默建出无口令无 class 的账号**（CLI 回 `[ok]`、commit 报成功、落库 `{"name":"tester1"}`），现报「语句不完整」并把约束写进命令树；④ `dns server secondary <ip>` 报「配置中不存在字段 dns」指错方向，现直说缺 `<ip>`；⑤ 候选标签不再把类型写死成 `（name）` | #89/#90/#91 → 已发布 **v1.1.9** |
| 本轮 | **验收口径收口（6 条决策）**：① `ping` **未通即失败**（0 发包 / 无应答都报错，`received=0` 此前算通过）；② `delete-vfs` 明说「`vf <n>` 不参与定位」；③ **FR-SEC-006 落地**——SSH 禁 root 口令登录（安装期 drop-in + 四道防锁死闸门，真机双向验证）；④ **NFR-006 落地**——审计记录带「时钟是否已同步」三态（存储 v1→v2 纯增量迁移，真机双向实测）；⑤ NFR-005 口径修正（原文「英文为主」→「中文为主」，并更正我先前一条错误建议）；⑥ 「镜像不存在」报错改成可照着排查。另：§10 补「状态与测量方法」，验收检查表 §5.2 九行过期条目按源码复核改写 | #93~#98 |
| 本轮 | **发现 #10 修复（发布级）：重签自签证书的 SAN 四处同源**（`system.ServerCertSANs` + `TLSManager.RegenerateSelfSigned`；启动 / REST / CLI / commit 钩子共用；`TlsRuntime` 不再暴露带 `ips` 的重签） | #99（证据 `docs/evidence/v1-closeout-round16-tls-san.txt`：红-绿 + 装机 + 启动期自签 + 冒烟复跑） |
| 本轮 | **验收检查表与代码对齐 + `monitor vnf` 补全**：① `V1-验收检查表.md` §5.2 有 **9 行**在对应决策关闭后没跟着改（仍写「未实现 / V2 补」），与 §0 关闭注记和 §1~§4 主表自相矛盾——按源码逐条复核后改写并给出 `文件:行` 依据；§6 更正通过数（71→100，与 §0 一致）、A-3（已完成）、D-1（已结案）；② `monitor vnf <name>` 按契约改为**真跟踪**（CLI 端轮询，Ctrl-C 退出；非 TTY 单次快照） | #92（另含 #89~#91 复核） |

---

## 1.4 最近一轮（决策 #85/#86）最该记住的三件事

1. **「命令能不能用」≠「结果对不对」**：256 条全功能冒烟 197 全绿的同时，语义校验一次跑出 4 类契约违反
   （`show virtual-switches` 取自配置、`ports`/`statistics` 没实现、接口链路列取自配置、`| display set` 从未实现）。
   改 `show` 族/候选来源后**必须**跑 `cli-semantic-check.sh`（已写进 AGENTS 自检清单）。
2. **测试会把缺陷固化成期望**：本轮发现 5 处断言写的是「消息里应含 `FR-NET-021`」这类内部编号，
   等于把泄漏锁死；此前 `TestDynamicCandidatesEndpoint` 也断言过「已配置的接口名应成为候选」（即 #83 的 bug 本身）。
   **断言要写契约语义，不要写实现细节。**
3. **工具自身出错会制造假红**：语义校验脚本先后踩了三次（终端文本解析脆弱、`l2fib` 空表按行数计得 1、
   前置条件没校验）——每次都先修脚本再采信结论。脚本头部已写明「自校准」要求。

### 1.3 本轮最有价值的三处**通用加固**（决策 #79，防的是整类缺陷）

1. **`fromJSONTree`/`validateTreeJSON` 启用 `DisallowUnknownFields`**：此前 schema 键与模型键不一致时
   `encoding/json` **静默忽略**该键，语句"看似成功却没生效"（报"未产生配置变更"而非错误）。
   现在整类"CLI 声明了但落不进模型"**第一次执行即报错**。⚠️ 这会让此前被静默忽略的**错误配置开始报错**。
2. **解析器就近向上回退 + 末位实例参数终止语句 + `SPA` 首值落数组**（`Node.parent` 本即为此回填）。
3. **口令回显脱敏**（`set … password <pw>` → `«已隐藏»`；配置只存哈希、展示层已脱敏，回显不该例外）。

---

### 1.5 本轮（决策 #87/#88）最该记住的三件事

1. **把源码规则套到 Markdown 会冒出假阳性，「残渣」类规则必须按文件类取用**：直接沿用 #86 的
   规则扫用户手册，一下子多出 4 类假红——`①②③④` 是手册的**步骤编号**、`[系统要求](#1-系统要求)`
   是**目录锚点**、`见 §4.3` 是**手册自身的导航**。故 `§x` 在手册里放行（操作者点得动），
   圈号与 `/#nn` 只对 `.go`/脚本判。**判据要按「这段文本会到达谁、他能不能解析」定，不能按「看着像」定。**
2. **验证工具自身出错会制造假红（这是第二次踩，见 §1.4 第 3 条）**：本轮两次——
   ① 检查「已安装文件还有没有编号」时，我的过滤正则写成 `^[^:]+:[0-9]+:`，而 `grep` 对**单文件**
   不输出文件名前缀，于是 7 行注释被报成「有命中」；② `cli-fulltest.sh` 连跑两次，第二次
   **192 通过 / 6 失败**——全是「语句未产生配置变更」的**脏状态假红**（第一次跑留下的配置让第二次
   的相同语句变成「值未变化」），重启 VPP + 换新 DB 后回到 **197/1/2** 的历史基线。
   **结论：报「失败」之前先确认判据本身是对的，重启到干净基线再采信。**
3. **「可复现」要证明该变量真的生效，不能只看「两次哈希一样」**：两次一样可能是巧合。
   把 `SOURCE_DATE_EPOCH` 换成 0 得到**不同**哈希，才证明它真的参与了产物——与
   `cli-semantic-check.sh` 的「扰动必须真的改变事实源」是同一条要求（决策 #85）。

## 2. 剩余待办

### 2.1 环境受限的 4 条"未验"（`V1-验收检查表.md` §5.1）

| 项 | 阻塞原因 |
|---|---|
| **FR-NET-004 / FR-NET-021** SR-IOV | nfvis-vm 是 vmxnet3，**无 PF/VF**，需目标硬件。另：`request sriov delete-vfs` **忽略 `vf <n>`**（按计数递减），登记待修 |
| **FR-NET-018** LLDP 邻居 | 环境无 LLDP 对端。启用与命令正常；标识解码缺陷已修（#62） |
| **FR-CMP-014** 真人 `Ctrl-]` | 需运行中 VM + 真实交互 TTY。**另有真缺陷（已登记于 §5.2 待决）**：console 断开时空闲阻塞在 `Read`、需按键才退出——**阻塞方尚未定论**（服务端 `io.Copy`/`Close` 还是 CLI 侧 raw 读 stdin），未猜着改 |

### 2.2 降级 2 项（`V1-验收检查表.md` §5.2，**处置口径已于 2026-09-18 确认**）

> FR-SEC-006 已**拆两半**（决策 #96）：SSH 禁 root 口令登录**已实现并真机验证 → 转通过**；
> 「底座最小化」半属镜像/安装器范畴，**不在 deb 交付范围**，已移入 `V1-验收检查表.md` §5.3。

| 项 | 现状 |
|---|---|
| **FR-NET-022** 容器侧 memif 通流 | VPP 侧 endpoint/socket 与容器生命周期真机通过；容器侧未验（需**自建带 memif 客户端的镜像**） |
| ~~**FR-SEC-006** SSH 强化~~ | ✅ **已实现**（决策 #96）：`deploy/installer/nfvis-ssh-harden.sh` 写 drop-in（`PermitRootLogin prohibit-password`），四道防锁死闸门；离线不会切断当前会话（不 reload）。原「访问风险」已由闸门消解 |
| **FR-SEC-007** 敏感信息脱敏 | 主要泄露**已修**；残留为**有意例外**（`save <file>`、备份归档均 0600 + super-user）。只差书面确认 |

### 2.3 其它待决

- ✅ **A-2 §10 口径已定**（决策 #93 同批）：§10 逐行标出**已测/未测**并给出**每项的测量方法**，明确按
  「设计目标下限 + 目标硬件验收」。**仍未做的是实测**：吞吐类（10GbE ~14Mpps、vhost-user ≥8Gbps）与容量类
  需要**流量发生器（IXIA/TRex）+ 带 10GbE DPDK 口的目标硬件**——nfvis-vm 是 vmxnet3、无测试口，测不了。
- **容器镜像目录名须等于 Docker tag**（决策 #76 §4①）——**报错侧已改好（决策 #98）**：现在校验会列出仓库现有镜像并对容器镜像点出「可用名是 tar 内嵌 tag，与上传目录名不一致时两个名字都用不了」。**仍未解决的是产品决策**：`request images upload` 的目录项名与 `docker load`
  落地的 tar 内嵌 tag 不一致时，容器镜像**用哪个名字都不可用**（用目录名 → Docker API 404 `docker: not found`；
  用 tag → 校验拒"仓库中不存在镜像"）。**需产品决策**（目录是否记录 tar 内嵌镜像引用），见规格书 §12。
- `show vpp runtime` 未接入（govpp runtime 解码受限；CLI 明确提示而非静默空值）。
- **契约已声明、服务端从未注册：`POST /system/kernel:apply` 与 `POST /system/kernel:rollback`**（2026-09-24 控制台刀 4b 第二段发现，R51-2 的**反向**形态）：
  `docs/NFViS-openapi.yaml` 声明了这两条（`cli_rest_coverage_test.go` 也把 `request system kernel apply|rollback`
  映射到它们），但 `server.go` 只注册了 `GET /system/kernel`——实测 `POST …/system/kernel:apply` 得 **404
  `404 page not found`**（`routes_contract` 只查"注册的都在契约里"，查不到这个方向）。
  命令行侧的 `request system kernel apply|rollback` 走的是 CLI 本地执行器（不经 REST），故此前没人撞上。
  **影响**：控制台「内核基线」页的写入/回退按钮会如实报 404（界面已把该 404 说明成"本机服务端没有提供这个接口，
  请在命令行执行"，不谎报成功）。**要修就得服务端补注册 + 契约/守护同步**（属服务端改动，本段按边界未动）。
- **R56-1（低-中）：REST 请求体里的未知字段被静默忽略**（round56 实测）：`POST /virtual-machine-functions`
  传契约里不存在的 `cloud_init.user_data_file` → **201 接受、字段丢掉**，VM 照建但 cloud-init 退回产品默认
  （上一轮「user-data 没生效」的观察有一半源于此）。契约只声明了 `cloud_init.user_data`，故产品与契约自洽，
  属"客户端写错字段"；但**静默**这一点与项目最忌讳的「静默无效」同类。**若要做**：给 REST 解码加
  `DisallowUnknownFields`（CLI 语句路径 `cliexec.go` 已有先例）→ 未知字段返 400，**需契约先行**
  （声明 400 语义 + 全端点一致 + 用例），故本轮只登记未改。
- ✅ **已完成：`monitor vnf <name>` 改为真正「跟踪」**（决策 #92）——契约 §1.3 与命令树写的是
  「跟踪」，此前 CLI 的 `monitorSpec` 却**明确排除 vnf**，只执行一次快照；服务端 `monitorVNF`
  的注释本就写着「由 REPL 轮询（与 monitor interfaces 同法）」，即**服务端按轮询设计、CLI 侧漏了**。
  现 `monitorSpec` 同时识别两种形态：TTY 下清屏刷新、Ctrl-C 退出，非 TTY 退化为单次快照（脚本不挂死）。
  真机差分：旧 CLI 在 pty 下刷新 **0** 次、新 CLI 同窗口刷新 **6** 次。顺带修掉一条**把缺陷锁死的测试断言**。
- ✅ **已完成：NFR-006（未同步标记）**（决策 #97）：审计记录随事件落库 `time_synced` 三态（true/false/**NULL=未知**），
  `show log audit` 未同步时带 `[时钟未同步]`；探针 `internal/system.ClockSynced()`；存储 v1→v2 纯增量迁移。
  真机双向实测（抓到 chrony 重同步窗口）。**告警侧（内存态）未加同款字段**，如需请单列。
- ✅ **已完成：NFR-005**（决策 #95）——**更正我先前的错误建议**：降级理由**不是**「文案混内部引用」（那是 #86/#87 的事），
而是**语言**：#NFR 原文要求「错误消息**英文**为主」，产品全程中文。已按产品定位把措辞改为「**中文为主**」→ 转通过。
- ✅ **已完成：`GET /system/backup/{file}` 权限口径**（决策 #143）——由 `ClassReadOnly` 提到
  `ClassSuperUser`（归档含全部口令哈希、恢复所必需故不脱敏）；列表保持 read-only，生成端本就是 S。
  契约（summary/403/description）、内嵌 `openapi.json`、用户手册 §10.7、用例（read-only/operator 403、super-user 200 且归档含哈希）
  与变异验证同步完成。**同批登记、本轮未修的两条**见下两条。
- ✅ **已完成（round67，决策 #144）：`request` 域的运行期 class 判定与命令树声明对齐**——原来执行器把**域节点**
  （`request system` = O）交给判定，把子节点上的 `Su()` 标记全部吃掉（operator 能跑 `zeroize`/`reboot`/`software add`/
  `configuration backup`）；**且 `request vpp`/`interfaces`/`sriov`/`alarms` 四个子域连判定都没有**（read-only 也能
  `request vpp restart`、`bind-dpdk`）。现改为按 `schema.Match` **匹配到的最深节点**判定，并在 `request` **入口处判定一次**
  （不再可能漏域）。守护：`cli_class_gate_test.go` 穷举操作树 200+ 节点 + 端到端双向断言 + 两条变异验证；
  真机复验 operator/read-only 被拒、`ping`(O)/`show`(R)/super-user 正例仍可用；256 条冒烟（admin）**无一条「无权限」**。
  证据 `docs/evidence/v1-closeout-round67-cli-class-gate.txt`。
- **`request system configuration backup to <path>` 的目标路径仍无校验**（round65 核实、round67 处置后**已不是提权路径**）：
  该命令现按声明收回到 super-user，而 super-user 本就有 `start shell`；但"导出目标路径不校验"本身仍可改进。
- **`GET /system/tech-support/{file}` 同为 read-only，而诊断归档内含 `config.json` = committed 配置**（同样含口令哈希，
  `cmd/nfvisd/main.go` 注入 `engine.Committed()`）——与 #143 的同类口径不一致。**未改**：收紧会挡住
  「operator 生成诊断包 → 下载送支持」的现场流程（`request system tech-support generate` 声明等级是 **O**），
  且诊断归档另有 `os.Create` 落盘（0644）问题；属**产品口径变更，需单独裁决**。
- **控制台未按角色隐藏写入口**（round66 实测，未改）：设计 §8「`read-only` 只见只读页…写按钮**隐藏**（不是禁用）」
  与骨架计划 Step 2 的验收项「只读账号看不到写入口」**均未实现**——read-only 账号登入后 21 个导航项与各页写按钮
  （含「恢复出厂」「重启主机」）**全部可见**；`internal/api/ui/app.js` 里除用户管理页外没有任何 class 判定。
  **服务端 class 判定是真边界**（点「开始编辑」被如实拒绝「当前 class 无权执行该操作」、配置库无变化），
  故影响面是**体验与误导**（点下去才被拒）而非越权。修它要跨 ~20 个页面做入口分类 + 浏览器复验，属独立一刀。
  证据 `docs/evidence/v1-closeout-round66-web-console-final-acceptance-and-release.txt` §3.5/§5。
- **V2 候选清单**：见规格书 §12。

### 2.4 ✅ 已完成：8 处「契约已声明但经 CLI 用不了」（决策 #79）

2026-09-15 全部修复并真机逐条复验（证据 `docs/evidence/v1-closeout-round10.txt`）：
`api tls cert-file/key-file`、`login user password/class`、`login class allow|deny`、`cross-connect`、
`vmf interfaces vlan`、`cloud-init ssh-key`、容器 `interfaces … virtual-switch`、容器 `env`。
根因与修法见决策 #79 与命令全表 §4⑦；12 条语句已补入 `cli_mapping_test.go:contractStatements` 守护。

### 2.5 ✅ 已完成：编写《用户手册》时发现的问题（决策 #77/#78/#80）

- ✅ 备份导出件权限泄露（#77，安全）
- ✅ `nfvis-cli` 缺省连接地址与守护进程不匹配（#78）
- ✅ `postinst` 内核基线块不可达（#80③）

### 2.6 ✅ 已完成：首装/升级体验 6 处（决策 #80）

证据 `docs/evidence/v1-closeout-round11.txt`：
① `nfvis.service` 两处 systemd 告警（中文 `Documentation=` 判 Invalid URL；`StartLimit*` 放错段致崩溃退避
静默失效）；② postinst 把 2M 默认页误报成"大页已预留"；③ 内核基线块不可达（安装期不应用）；
④ 升级后 nfvisd 一直停着（prerm 在升级时也 stop，使 postinst 的 try-restart 成空操作）；
⑤ `nfvis-cli` 无交互式口令提示（口令只能写命令行 → 进 `ps`/历史）；⑥ 首启口令含 `!` 触发 bash 历史展开
（`!Aa1: event not found`）导致复制粘贴失败。

### 2.7 待办（决策 #83~#98 的已知遗留）

| 项 | 现状 / 建议 |
|---|---|
| ✅ **已完成：非 Go 文件里的内部引用** | 守护已扩到 `.sh`/`.service`/`Makefile` 的**非注释行**、`deploy/` 下一切（`postinst`/`prerm`/`postrm` 无后缀）与用户手册全文；清掉 40 处会打印给操作者的引用（`postinst` 7、`cli-pty-smoke.sh` 2、`cli-semantic-check.sh` 4、用户手册 27）。`deploy/nfvis.service`（5 行）与 `prerm`/`postrm` 的引用**全在 `#` 注释里，按设计保留不改**（决策 #87） |
| ✅ **已完成：deb 可复现构建** | 同一源码 + 同一 `SOURCE_DATE_EPOCH` 连打两次逐字节一致（真机实测 `939a39f6…` 两次相同；改动前为 `4383575c…`/`6661edfc…` 两次不同）。⚠️ **发布须显式传** `SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)`——`git archive` 导出的工作树没有 `.git`，否则回落 0（仍可复现，但包内时间戳是 1970）。跨机器/跨工具链**不主张**一致（决策 #88） |
| `show virtual-machine-functions` 只列配置中的 VNF | libvirt 里存在而未写入配置的孤儿域不显示（决策 #84 §5 登记）。契约口径未明（VNF 是可声明资源），需一条决策 |
| `\| display set` 未实现 | 契约 §3 已更正为「未实现」（决策 #84④）。忠实实现需 model→CLI **反向映射**（`save` 导出 JSON，别名语句无法反推），前提是把别名表做成双向 |
| `request interfaces <n>` 候选取并集 | 同层 enable/disable（VPP 口）与 bind-dpdk/unbind-dpdk（内核口/PCI 地址）语义不同，而参数位置在动作之前，故取并集（#83）。要按动作区分需改语句树结构 |
| `vpp-ifnames` 只剔 `local0` | 子接口（`ens192.100`）、vhost-user/memif/tap 仍会作为候选出现；按类型过滤需给 `SwIfInfo` 补 `IfType`（该结构体被告警路径共用） |
| `set interfaces <不存在>` 的取值校验 | 仍在 commit 时才失败（有补偿回滚，报错明确）。是否前移到 `commit check` 需决策：「先声明端口、绑定后再提交」是被支持的流程（决策 #72） |
| SR-IOV 相关 | 本环境无 PF/VF 未真机验证（与 §2.1 既有登记一致） |

### 2.8 2026-09-18 裸机全流程测试：**第一批发现**（#1~#7，含一次自锁门外；续篇见 §2.9）

**背景**：把 nfvis-vm 还原为**全新 Ubuntu Server 26.04（无任何软件）**后，按「环境 → 按需装底座 → 安装 → 首启/首登 → CLI → 数据面」走了一遍。底座按需装了
Go 1.26.0 / libvirt 12.0.0 / qemu 10.2.1 / docker 29.1.3 / VPP 26.06-rc2（FD.io 2606 源，**noble 构建装在 resolute 上**）。
**这套版本矩阵比产品验证过的基线新**，本身即一次兼容性抽查。

**已验证通过的（上一批修复在新机器/新版本上复验）**：deb 可复现（双打同哈希 `0f30150c…`）、安装干净、首启建库 schema v2、
**零参数即可连**、明文 HTTP 被拒、`show \| display json` 正常、`\| display set` 明示未实现、
用户报过的三条（`ping` 报「未发出任何报文」/`dns server secondary` 与 `login user` 报「语句不完整」）全部按新版文案工作、
`monitor vnf` 非 TTY 退化为单次快照、审计带时钟状态（NTP 已同步故无标记）、`postinst` **真实触发**了
「未检测到 1G 大页池」分支（此前只能用 shim 模拟）。

| # | 发现 | 性质/严重度 | 建议 |
|---|---|---|---|
| 1 ✅ | **`dpkg --purge` 留下三处底座副作用**：`/etc/ssh/sshd_config.d/99-nfvis.conf`（卸载后 SSH 仍被限制）、`/etc/default/grub.d/99-nfvis.cfg`、`/etc/fstab` 大页行。数据目录保留是 postrm 明示的有意行为，不算 | 卸载残留（中）→ ✅ **已修** | postrm 在 `purge` 时调 `nfvis-ssh-harden.sh --revert`（自动），GRUB 片段与 fstab 大页行**打印可照做的撤销步骤**（含 `nfvis-baseline.sh --rollback`），不自动改系统文件。真机验证：用探针配置路径跑 `postrm purge` → 探针被撤销、真实 drop-in 未动、指引正确 |
| 2 ✅ | 手册 §2.1 写「任意 Linux + **Go ≥1.26**」即可 `make deb`，但**全新 Ubuntu Server 无 `make`** → `make: command not found` | 首装/构建前置漏列（低）→ ✅ **已修** | 手册 §2.1 已写明「任意 Linux + Go ≥1.26；**还需要 make** 与 dpkg-deb」，并注明全新 Ubuntu Server 不带 make、构建不需要 gcc |
| 3 ✅ | **操作者可见文本仍含内部术语**（字符串字面量，非注释）：`show version` 的 `（M2：配置事务可用，网络底座 M3+ 接入）`；`导入见 M4-8`；`SR-IOV 接入见 M4-4`；`属 M4`；`（…附录 B）`×2。守护 `refPatterns` 只有 `FR-xxx`/`§n`/`决策 #n`/`附录 A #n`，**拦不住里程碑编号与无编号的「附录 X」** | 同类未清 + **守护缺口**（中） | 清这 6 处并把守护扩到 `M[1-9](-\d+)?` 与 `附录 [A-Z]`（注意排除行尾注释——注释是合法落点） |
| 4 ✅ | **契约 §3 声明的两种 compare 写法都没有 CLI 入口**：`show \| compare`（candidate ⇄ committed）与 `show configuration \| compare rollback <n>` 均报 `%% 未知管道: compare`。**能力其实都已实现**——`Engine.CompareCandidate()`（注释就写着「配置模式 `show \| compare`」）已接到 `GET /configuration/diff`，`Compare(n)` 也已接到**无管道**的 `show configuration compare rollback <n>`；**只差管道解析器不认 `compare`**（`PipeKeywords` 只有 match/except/count/last/begin/display）。后果：操作者在 commit 前**看不到自己改了什么**（JunOS 事务模型的核心动作） | **契约违反 + 覆盖缺口**（高）→ ✅ **已修** | `compare` 已接入管道（`\| compare` → `CompareCandidate()`；`\| compare rollback <n>` → `Compare(n)`），并进 `schema.PipeKeywords`（Tab 候选同源）；新增守护 `TestPipeKeywordsMatchParser`（Tab 候选与解析器必须同源）+ 4 组语义用例（含越界序号、未进入配置模式、后接 `count`）。**变异验证**：解析器不认 compare → 测试报红 |
| 5 ✅ | **语义校验脚本对「默认 HTTPS 实例」不兼容**：脚本用 `curl` 登录取 token，不信任自签证书（`exit 60`）。文档说「产品装在系统上时须给 `CLI_BIN`」，实际还差 `CURL_CA_BUNDLE=/var/lib/nfvis/tls/server.crt`（或脚本支持 `--cacert`/`-k`）。**发布前门槛在真实安装上跑不了** | 工具与默认配置不兼容（中）→ ✅ **已修** | 两个脚本都支持环境变量：语义校验在 HTTPS 下**自动读产品证书**（`NFVIS_CA=` 可指定，仅实验室可 `NFVIS_INSECURE=1`）；冒烟的 `SRV`/`CLI_BIN`/`NFVIS_PASSWORD`/`API_PORT` 全部可覆盖，**已装实例不必再打补丁副本**。真机复核：语义校验 11/0、冒烟阶段 1 得 40/1（仅 `show vpp runtime`） |
| 6 ✅ | **全新系统 `vfio-pci` 未加载**，`request interfaces <n> bind-dpdk` 直接失败（`stat /sys/bus/pci/drivers/vfio-pci: no such file or directory`）。而手册把它标为「**方式 A：经 CLI（推荐，产品内建）**」，`modprobe vfio-pci`（+ 无 IOMMU 时的 `enable_unsafe_noiommu_mode`）却只写在「**方式 B：带外手工（CLI 不可用时用）**」里 | 首装阻塞 + 报错不可操作（中）→ ✅ **已修（手册侧）** | 手册 §3.2 方式 A 已把 `modprobe vfio-pci`（+ 无 IOMMU 的 `enable_unsafe_noiommu_mode`）提为**前置条件**并给出报错样例。**产品侧未做自动 modprobe**（改内核模块加载属安装期行为，需单列口径）——错误文案已含确切命令，故按"文档前置"收口 |
| 7 ✅ | **未声明管理口时，「绑定管理口」的自锁守卫无效**：全新装、未声明 `system.management.interface` 时，`request interfaces ens160 bind-dpdk --yes` **照做**，把承载 SSH/API 的网卡交给了 DPDK → **当场失联**（命令 hang、SSH 超时）。手册说「管理口保持内核驱动」听上去像有守卫，但守卫只能按 **committed 里声明的**管理口判断，未声明就无从判断；手册也没说「先声明管理口」这个顺序依赖 → ✅ **已修复**（决策 #101，口径 A：默认拒绝、无 force 出口；真机复验见 `docs/evidence/v1-closeout-round18-mgmt-guard.txt`）| 落地：判据取**默认路由 / 守护进程监听地址 / 配置声明的管理口**三条高置信度事实（同网段这类低置信度线索不作拒绝理由）；手册 §3.2 增「管理口会被产品直接拒绝」+ 排障表一行；**「已声明时是否也会被拒」已澄清**——配置路径本来就有 `checkManagementIsolation` 拦，运行态动作这条路本次补齐 |

**测试中断点与后续**：P1~P4 已完成（环境/安装/首启/首登/CLI/事务/报告过的命令）；P5~P7（真机三件套对已装实例、数据面、生命周期与卸载残留复验）被 #7 自锁中断。
恢复 VM 后需补：语义校验（加 `CURL_CA_BUNDLE`）、256 条冒烟与 pty 冒烟（对已装实例需脚本支持 HTTPS）、引导镜像 + 集成测试（**需重启开 1G 大页**）、升级/purge/重装与 #1 的残留复验。

### 2.9 2026-09-18 续：裸机全流程测试的**第二批发现**（3 条，含 1 条发布级缺陷）

**本轮新增环境**：VM 已由用户还原为**全新 Ubuntu Server 26.04**（无任何软件）。按需装了
Go 1.26.0 / libvirt 12.0.0 / qemu 10.2.1 / docker 29.1.3 / VPP 26.06-rc2（FD.io 2606 源，**noble 构建装在 resolute 上**）。
**1G 大页需重启生效**（运行期写 `nr_hugepages` 无效），装完后 postinst 写的基线在重启后生效（实测 `total=2`）。

| # | 发现 | 严重度 | 建议 |
|---|---|---|---|
| **10** ✅ | **`request system api tls regenerate` 之后 CLI 彻底连不上**（本次 256 条冒烟 **50 条失败**的直接原因）。根因是**同一功能两条实现**：启动期 `EnsureSelfSigned(host, system.ListenSANs(*listen))` 传入 `127.0.0.1`/`::1` 等 SAN；而 **CLI 执行器** `internal/api/cli_compute_oper.go:836` 调 `Regenerate(host, **nil**)` → 重签出的证书**没有 IP SAN** → CLI 的「零配置固定本机证书」（决策 #78）当场失效，报 `x509: cannot validate certificate for 127.0.0.1 because it doesn't contain any IP SANs`。**REST 路径 `internal/api/tls.go:84` 传的是 `localIPs()`**（两条实现不一致）。实测：套件跑到阶段 2/4 执行这条语句后，**其后所有 CLI 调用全部登录失败**；删证书让启动路径重签后立即恢复（SAN 回到 `DNS:nfvis, DNS:localhost, IP:127.0.0.1, IP:::1`） | **发布级**（管理路径自毁）→ ✅ **已修复（决策 #99，2026-09-18）** | 已落地：四处**共用** `system.ServerCertSANs`，`TlsRuntime` 不再暴露带 `ips` 的重签（调用方没有机会漏传）；**两处补记**：① 无条件复现的是运维语句 `request system api tls regenerate`（配置语句只在无证书时重签）；② **REST 路径原先也不对**（`localIPs()` 只收非回环地址，重签后同样不含 `127.0.0.1`）。真机红-绿与装机复验见 `docs/evidence/v1-closeout-round16-tls-san.txt` |
| **8** ✅ | **业务口交给 VPP 这条路，在新装环境下经 CLI 走不通；且产品一旦重生成 startup.conf 就会掉口**。三处约束互相打架：① `set interfaces <n>` 的 **apply 要求端口已在 VPP**（`network/services.go:178` 硬失败 + 整体回滚）；② `set vpp dpdk dev <X>` 的**校验要求 X 已在 `set interfaces` 里声明**（`model/validate.go:748` 查 `ifaceNames`）；③ startup.conf 生成**按名字解析 PCI**（`pci_sysfs.go` 读 `/sys/class/net/<n>/device/uevent`），而**绑定后该 netdev 已不存在** → 端口被 DPDK 接管后任何重生成都失败。实测路径：`bind-dpdk` ✓ → `set vpp dpdk dev ens192` ✗（「不是 DPDK 接管的物理口」）→ 两者同 commit ✗（apply 失败「接口在 VPP 中不存在（**是否未由 DPDK 接管？**）」——**它其实已由 DPDK 接管，报错指错方向**）→ `set interfaces 0000:0b:00.0`（想用 PCI 绕开）✗（「接口名称不合法」）→ **只有带外手写 startup.conf 的 `dpdk{}` 段 + `systemctl restart vpp` 才能把口送进 VPP**；此后 commit 能成功，但 `request vpp restart` 报「解析 "ens192" 的 PCI 地址失败」并**把口从 VPP 掉出** | **高**（主功能在新装环境不可用） | (a) **绑定时持久化 ifname→PCI**（`Bind()` 已拿到 PCI）：写进配置或独立记录，生成 startup.conf 时优先用已记录的 PCI；(b) 接口 apply 对「已声明为 dpdk dev 但尚未进 VPP」的端口**宽容**（延后到 VPP 重启后收敛，而不是整体回滚）——这正是决策 #72「先声明端口、绑定后再提交」的含义；(c) 修正报错文案与手册 §3.3（把「第一步是 `set vpp dpdk dev`」写清，并说明首装的带外播种现状） |
| **9** ✅ | **`cli-fulltest.sh` 对 `show log audit` 的判定会被环境历史误判**：判定用 `_is_fail`（`grep -qE '^%\|校验失败'`）扫**输出全文**，而审计记录里若含历史条目（例如先前失败的 commit，其 detail 就是「校验失败: …」），该用例被判失败——**同一条命令在不同审计历史下结论不同**。本次即因此多 1 条假红 | 中（工具假红，与"假绿"相反方向）→ ✅ **已修复** | 已把 `校验失败` **锚定行首**（`cli-fulltest-lib.sh` 两处），并新增**判定模式自校准**脚本 `contrib/scripts/cli-fulltest-selftest.sh`（已知正确→PASS、已知错误→FAIL、含历史回显→PASS），已纳入 `make check`（`toolcheck`）——这类"工具自身出错制造的假红"不再靠人肉发现（决策 #85 的同类）。真机复核：阶段 1 的 `show log audit` 假红消失 |

**256 条冒烟在裸机上的失败分解（87 条）**：50 条 = #10（证书重签后 CLI 全断）；
约 16 条 = **引导镜像缺失的级联**（VM/容器声明后 start 报「不存在」）；13 条 = 阶段 2/3/6 的 configure 块
（含 1–2 条「语句未产生配置变更」——我在前面手工 commit 过同样取值造成的脏状态，属已知用例受限类）；
2 条 = `/data/incoming` 缺文件（同上游）；3 条 = ping 判定「报错但不像未通」（#10 级联）；
其余 3 条 = `show vpp runtime`（已知未接入）、`show log audit`（#9）、等。
**即：扣掉 #10 与"前置未满足"，套件本身没有暴露出新的功能性缺陷。**
> **2026-09-18 修复 #10 后复跑（阶段 1/2/4/5/6）**：**162 通过 / 28 失败 / 3 预期报错**，
> 上述 50 条级联**已消失**；28 条按同样口径分解为 23 条引导镜像级联 + 3 条脏状态 + 2 条已知项。见 §0 第 3 条。

**本轮通过的**（同一台裸机上）：语义校验 **11/0**（对已装 HTTPS 实例，需 `CURL_CA_BUNDLE` 变通 → 见 #5）、
pty 冒烟 **10/0**、以及 v1.1.9/v1.1.10 那批修复全部复验通过。

### 2.10 2026-09-18 第三批发现（1 条，修 #8 时顺带发现；**已修**）

| # | 发现 | 严重度 | 建议 |
|---|---|---|---|
| **11** ✅ | **`show vpp` 的可见性缺口**：契约（`NFViS-CLI命令全表.md`）把它描述为「数据面概览：**版本**/线程/buffer/内存」，而 CLI 只打印 threads/buffers/memory——**版本没打印**；更要紧的是 **`pending_restart` 在 CLI 侧完全不可见**（只有 `GET /vpp/status` 有）。而 #8 修复的收尾动作恰恰是「看有没有待生效的 vpp 变更 → `request vpp restart`」：操作者目前只能从 **commit 自身的警告**（`警告: vpp 变更需 request vpp restart … 后生效`）得知，`show vpp` 看不出来 | 中（契约缺口 + 操作者看不到下一步）→ ✅ **已修** | `cliExecutor` 接 `VppController`（`setVppCtl`，装配于 `api.New`），`show vpp` 概览补 `version`/`connected`/`pending_restart`（+ `last_error`）；**stats 运行态不可用时这三项仍给得出**（它们来自连接管理器，不依赖 stats 段）。命令全表该行同步（并注明后发现 #4 使管道计数 7→9、合计 249→258） |

### 2.11 2026-09-18 第四批发现（修 #7 时产生/发现；**已修**）

| # | 发现 | 严重度 | 建议 |
|---|---|---|---|
| **13** ✅ | **对「正在被数据面使用」的口执行 `unbind-dpdk` 会把 CLI 通道卡死，并把网卡留在无驱动状态**（本轮亲历）。复现：口在 VPP 中时执行 `request interfaces <该口PCI> unbind-dpdk to-driver vmxnet3 --yes` → ① 请求永不返回，客户端报「请求超时（1m30s）：操作可能已在服务端完成，请用 show 确认（如 `show virtual-machine-functions <name>`）」——**hint 指向虚拟机命令，与场景无关**；② 设备停在**无驱动**（已从 vfio-pci 摘下，内核 vmxnet3 绑定写阻塞在 VPP 持有的 vfio group 上）；③ **`cliExecutor.Execute` 全程持 `x.mu`**，卡住的 goroutine 握着锁 → 此后**任何** CLI 命令都 90s 超时（REST 其它端点正常：/vpp/status 200、/login 正常 → 是「执行器被占用」而非进程挂）。恢复：`systemctl stop vpp`（释放 vfio group，阻塞的写随即完成）→ `systemctl restart nfvis` → `request vpp restart`。**而手册 §3.2 的「解绑」示例正是这条命令**（未提示该口须先移出数据面）| **高** → ✅ **已修（口径取 (a) 拒绝，与 #7 同：拒绝 + 告知正确顺序、不提供 force）** | 决策 #102：`CheckUnbindAllowed` 在解绑前判「该口是否在数据面」，命中即拒（探测不到不拦）；**另加 sysfs 写超时**（20s，超时给可读错误而不是把执行器拖死）+ 超时 hint 改成与命令无关的通用措辞。真机：把**上次卡死的那条命令**（名称与 PCI 两种形式）跑回来——**0 秒内被拒**，设备与数据面纹丝不动、CLI 正常 |
| **14** ✅ | **`show interfaces management` 的「未配置」用 `%%` 前缀输出**（修 #9 时顺带发现）：`system.management` 是**空对象**（配置过又删除的残留）时走的是「打印表格 + `%% 提示：未指定管理网卡…`」，而**从未配置**时走的是普通空态句「（未配置管理口：…）」。同一种"没有"两种口吻，且 `%%` 会被真机冒烟按**失败**计（同一条命令在不同配置状态下结论不同）——与 #9 属同一类。已修：空对象与「从未配置」给同一句普通提示，只配了地址没配口名时也去掉了 `%%`（`show` 的「没有」是空态不是错误，同「（无 core dump）」口径） | 低 | 已修（`internal/api/cli_iface_vpp_show.go` + `TestShowManagementInterfaceEmptyStates` 锁三种空态）；**顺带记录**：本机 committed 因此曾残留 `system.management={}`，已用产品路径清掉（配置模式 `save` → 摘键 → `load override` → `commit confirmed`），此后 `show interfaces management` 回到普通空态 |
| **12** ✅ | 两处管理口配置的小缺口：**(a)** 管理口**首次**声明不触发 `commit confirmed`——`sysMgmtChanged` 在 `old.Management == nil` 时直接返回 false，而错误文案写的是「管理口地址/网关变更必须以 commit confirmed 提交」；运行期没有应用管理地址的实现（只警告 + 下次启动收敛监听），故目前无实际风险，但规则与实现不一致。**(b)** `delete system management interface` 删掉最后一个子键后留下**空对象**（`show configuration` 显示 `management;`），而 `delete system management` 整段删除**未映射**（报「语句未产生配置变更」）——本机因此残留 `system.management = {}`（见环境 §3.1） | 低 → ✅ **两半都已修** | **(b)** `applyStatement`/`deleteStatement` 合流处回收空壳（`pruneEmptySingleton`）。**(a)** 决策 #103：首次声明也须 `commit confirmed`——FR-CFG-012 未豁免首次，且风险相同。**影响面已核对而非假定**：冒烟里 `set system management interface ens160` 只在阶段 2 逐条循环作用于候选，`-c` 退出时 `Session.Teardown` 会 `discard`，从不进入后续 commit → **套件用例无需改动**。真机：null 的机器上普通 commit 被拒 → confirmed 后生效 → 删除回 null；无关字段提交仍不需要 confirmed |

### 2.12 2026-09-18 第五批发现（修 #1/#3/#4/#11 时顺带观察；**已修**）

| # | 发现 | 严重度 | 建议 |
|---|---|---|---|
| **15** ✅ | **语义校验脚本的扰动恢复不彻底**：它把 `interfaces[0].description` 改成 `semcheck-<mark>` / `sem-rt-<mark>` 做扰动，清理时是**删掉该字段**而不是恢复原值（真机 rev 29→32 的差异：`cli-pre → semcheck-21790 → sem-rt-21790 → <删除>`）。脚本头部已声明"尽力恢复"且在结尾提示 `restart vpp`，故影响很小（一个描述字段），但「恢复」应恢复**原值** | 低 → ✅ **已修** | 扰动前记下该接口原有描述，收尾**还回原值**（原本就没有才删字段），并打印做了哪种。真机：先把被旧脚本抹掉的 `cli-pre` 写回，再跑脚本 → 打印「已恢复接口 ens192 的原有描述（cli-pre）」且配置保留原值 |
| **16** ✅ | **`rollback 0` 与「丢弃 candidate」的语义容易被误用**：契约写的是 `rollback [n]`（取历史快照为 candidate，n≥1），丢弃 candidate 是 `discard`；而 `rollback 0` 报「配置快照不存在」，操作者（本轮的我）第一反应会以为它等价于"回滚到当前 committed" | 低 → ✅ **已修** | `rollback 0` 直接回「快照编号从 1 起（0 不存在）；要丢弃未提交改动请用 discard」（纯文案）。真机验证 |

### 2.13 2026-09-18 第六批发现（准备并首跑 `make integration` 时发现）

| # | 发现 | 严重度 | 建议 |
|---|---|---|---|
| **17** ✅ | **集成测试需要的云镜像没有记录，而文档里唯一记录的那份用不了**。⚠️ **结论已更正（round24）**：原记「换 Debian genericcloud 后同一条用例通过 ✓」**不成立**——genericcloud 的内核 `6.1.0-53-cloud-amd64` **缺 `ahci`**，而产品把 seed 挂成 q35 机器的 SATA 光盘，故 guest 里 `/dev/sr0` 根本不出现、cloud-init 按 `notfound=disabled` **静默自我禁用**（详见 #22）。round23 记的那次「通过」很可能发生在「seed 改 vfat + virtio 磁盘」的实验代码还在树里时（该实验已回退）。**可用的是完整内核的 `debian-12-generic-amd64.qcow2`**（sha512 与官方一致，已按产品路径导入） | 中（测试前置不可复现）→ ✅ **已修** | 已落地：`testVMImage`/`usableVMImage` 把「镜像要求」写进测试（缺省只从已确认可用的镜像里挑，都没有就跳过并说明 ahci+iso9660 的理由），不再回落到注定静默失效的 alpine；证据与 URL/sha512 见 `docs/evidence/v1-closeout-round24-integration-green.txt` |
| **18** ✅ | **集成测试硬编码的 VM 核池不扣减 VPP 保留核**：`m4_main_chain`/`m4_vnic`/`m5_snapshot_content`/`m4_console` 自建配置 `IsolatedCores: []int{1,2,3}` 且**不含 `vpp` 段** → 账本扣不到 VPP 保留核 → VM 的 vCPU 被钉到 VPP 正在轮询的核上 → **guest 饿死** | 中（测试前提假设机器布局）→ ✅ **已修** | 新增 `vmIsolatedCores(t)`：绑核池 = 真机在线核 − 核 0 − **VPP 实际占用的核**（读 `/etc/vpp/startup.conf`，解析逻辑与 `hostFacts` 共用 `hostVPPCores()`），5 处硬编码全部替换。本机解析结果 `{1,2,3}`（6 核 − 核 0 − VPP 的 4/5）与旧值一致故行为不变；换到 VPP 占 1-3 的机器上会自动避开 |
| **19** ✅ | **API `POST /logout` 不释放 candidate 锁**：只吊销 token（`aaa.Logout`），不动会话的 candidate；而会话锁按 `user@source` 保留 → 一个 API 客户端写过 candidate 后登出，CLI（`admin@ssh`）随后报 `candidate 会话锁被占用: 由 admin@api 持有`，直到**空闲超时**才释放。CLI 侧的 `Teardown` 是 `discard → exit → logout`（更完整） | 中（运维可见的锁滞留） | 让 `logout` 也做 `discard`（与 CLI Teardown 对齐）；或在 `GET /system/configuration/sessions` 输出里点明「谁持锁、怎么释放」（现在只能靠猜）——✅ **已收口（决策 #119）**：`POST /logout` 除吊销 token 外**同时丢弃本会话 candidate 并释放锁**，与 CLI 的 `Teardown` 对齐；真机复验「API 取锁 → 登出 → CLI 立刻可提交」 |
| **20** | **deb 未声明 VM 功能的运行时依赖**：`Recommends` 只有 `vpp, libvirt-daemon-system, docker.io`，而 VM 功能还依赖 `qemu-utils`（qemu-img）、`cloud-image-utils`（cloud-localds，缺了 `DefineVM` 直接失败/测试跳过） | 低 | `Recommends` 补 `qemu-utils, cloud-image-utils`；手册的安装前置也列出来 |
| **21** ✅ | **大页规划需保证 VPP 之外至少留 1 个 1G 页给 VM**：本机 1G 池仅 2 页，VPP 默认按 1G 页分配时**两页全吃**→ VM 一律 `Cannot allocate memory` 起不来（6 条集成用例失败）。解法：VPP 改用 2M 页（`vpp memory hugepage-preference 2M` + 2M 池），或把 1G 池调大 | 中（首装易踩） | 手册「大页数量规划」补一句：**1G 页要按「VPP + 每台 VM 至少 1 页」规划**；并说明 VPP 可用 `memory hugepage-preference 2M` 把 1G 页让给 VM——✅ **已补**：手册 §6.5「规划速查」与 §8.2「资源池与 VPP 运行时」均已写明 |
| **22** | **seed 交付方式导致 guest 看不到/挂不上 seed，cloud-init 静默失效**（round24 定位；**round56 已查清并部分收口**）。**原记录（round24，对 `debian-12-genericcloud` 成立）**：产品把 seed 挂成 SATA 光盘，而该内核缺 `ahci` → seed 不可见 → cloud-init 按 `notfound=disabled` **静默自禁**（VM 起来了、user-data 没生效、全程无报错；guest 里 `/var/lib/cloud` 与 `/var/log/cloud-init*` 都不存在）。**round56 更正与收口**：① **决策 #139 已把 seed 改为 virtio 磁盘**（`device=disk/bus=virtio`，挂 `vdb`），该改动**真机生效**（宿主 `domblklist` 与 guest `virtio_blk virtio1: [vdb]` 双向印证），对 genericcloud 一类（缺 `ahci`、但用 util-linux 的 `mount`）是真修复；② **但 alpine.qcow2 仍不消费 seed**，且根因**不是缺 `ahci`**（实测其 6.6.31-0-virt 内核 `ahci` 是 builtin、SATA 光盘也可见）——而是 **cloud-init 用 `mount -o ro -t auto`，busybox 的 `mount` 只认内核已加载的文件系统类型**（那一刻只有 ext3/ext4），`isofs`/`vfat` 都还没 load → `Invalid argument`；**`modprobe isofs` 后 `-t auto` 立刻成功**（guest 内实测矩阵见证据）。**宿主侧无解**（产品只控制域 XML）。属项目最在意的「静默无效」类 | **中**（对 busybox 类镜像仍不可用；**宿主侧无解**） | **已部分收口**（决策 #139 + #140）：virtio 交付保留；alpine 类限制**如实登记、不追**，改镜像（装 util-linux 或把 `isofs` 加进 `/etc/modules`）才能解；手册 §9.2③/§11/§12 已写明。证据 `docs/evidence/v1-closeout-round24-integration-green.txt` §1【RC1】与 `docs/evidence/v1-closeout-round56-seed-e2e.txt` |
| **23** ✅ | **产品模型的接口逻辑名与 guest 内核网卡名不一致**：模型/CLI 里是 `eth0`（VPP 侧 socket 名 `vh-<vm>-eth0`），而 guest 内是 `enp1s0`（systemd 可预测网名；产品不写 `net.ifnames=0`）。操作者若照模型的 `eth0` 在 user-data 里配网卡，会得到 `Cannot find device "eth0"`，cloud-final 随之失败 | 低 | 手册 VM/cloud-init 一节加一句：**guest 内的网卡名由 guest 的命名策略决定**，产品模型的 `eth0` 是逻辑名（VPP 侧接口/socket 名），配网卡前先在 guest 里 `ip -o link` 确认。✅ **已补**：手册 §9.2「VM VNF 全流程」已写明 guest 内网卡名由 guest 命名策略决定、产品模型的 `eth0` 只是 VPP 侧逻辑名，配网卡前先在 guest 里 `ip -o link` 确认 |
| **24** ✅ | `test/integration` 里 5 处硬编码核池（= #18 的实现）与「串口诊断阻塞 60s 后只打 800 字符、看不出 cloud-init 的结论」 | 低 → ✅ **已修** | 核池见 #18；诊断改为**与 ping 等待并行持续回读**，失败时把整段 guest 串口（含 `Cannot find device` 一类原文）带进错误信息，另支持 `NFVIS_SERIAL_DUMP=<path>` 落盘。m4_console 的超时也补上「已读回多少字节 + 末尾」 |

---

## 3. 环境要点（真机前必读）

### 3.1 当前状态（**2026-09-24 round66 发布 v1.1.29 后：拓扑与两台 VNF 在跑、流量已复通**）

nfvis-vm（**就是目标机** `D:\Virtual Machines\nfvis`，VMware、vmrun 可控；6 vCPU / 8G /
3×vmxnet3 NAT / 嵌套虚拟化 vhv 已开）。round32 与 round34 两度被恢复为「干净」快照，
现状态（快照恢复会清场——重建路径见 `docs/evidence/v1-closeout-round32-install-iso.txt` §4e）：

- **nfvis 1.1.29 已装**（round66 发布件；服务 active；round66 校验里跑过 升级(1.1.28→1.1.29)→purge→重装，
  末态是"purge 后重装 + `systemctl start nfvis`"；配置库全程未变；另在 1.1.29 上做了随包二进制的浏览器全套验收）
- **管理口令 `WBF81vOA4M8GM28f@Aa1`**（round66 按下面的哈希比对法核对有效）——
  ⚠️ **口令随快照恢复而变，别照抄旧值**：它是"最后一次首启的一次性口令"，取法：
  `journalctl -u nfvis --no-pager | grep 一次性口令 | tail -1`（随机生成、只打印一次，且**此后一直是
  该口令**直到被改）。**验证某个口令对不对，别反复试登录**（连续失败 5 次会锁定账号）：
  直接读配置库里的哈希比对即可——`system.login.users[0].password_hash` 形如
  `pbkdf2$sha256$600000$<b64salt>$<b64hash>`，用 PBKDF2-SHA256/600000 轮、`base64.RawStdEncoding`
  复算（与 `internal/aaa/service.go` 的 `HashPassword` 同格式）。
  ⚠️ 文档里出现过 `Nfvis@Test2026`——那是 **rounds 26~28 的旧值**，round32/34 恢复快照后已失效。
  ⚠️ 快照重建**会换掉 SSH host key**（round48 实测：VM 的 host key 于当日 01:27 重建），
  本机 known_hosts 随之陈旧——因 `~/.ssh/config` 里 `StrictHostKeyChecking no`，表现为"只警告不失败"；
  按 §3.8 的既定做法 `ssh-keygen -R 192.168.155.129` 后重取即可，**不是产品行为**。
  ⚠️ 另外 **round49 验证 `ssh host-key regenerate` 端点时按命令本意重生成过一次**（决策 #125，三把 key 全变、
  `sshd -t` 通过、当时会话不受影响）；**从快照恢复的机器上若 ssh 提示 host key 变了，先按 §3.8 刷新再判断**。
- 配置库计数（round52 末）：**rev 55 / audit 104**（audit 比 round50 多 1 条：round52 的校验里做了一次 core-dumps 导出，该动作自身入审计）（round50 全程未变——升级/purge/重装/首启四步
  与发布前备份逐字段一致，`config_json` sha256 `084c3a64e4cc0d43a16cbced28cb0742aaa0ef1783c75065eb7966b091eba726`；
  ⚠️ 校验途中我的脚本曾误改生产库（建 read-only 用户），已按 §3.9 快照**整库恢复**并核对回该指纹——见 round50 证据 R50-1）
- 底座：**VPP 26.06**（本地 debs `/root/vpp-v26.06-deb`，**主堆已用 2M 大页**——决策 #114）、
  libvirt/qemu/docker/cloud-image-utils 齐；源码树 `/root/src`（`git archive`，**无 `.git`**，
  构建须显式传 `SOURCE_DATE_EPOCH`）；开发态实例与已装服务可并存（`:18443`/`/tmp/*.db` vs `:443`/`/var/lib/nfvis/nfvis.db`，
  跑开发态前先 `systemctl stop nfvis`）；**`nodejs` v22.22.1**（round37 装，仅用于
  `node --check internal/api/ui/app.js` 校验前端语法——前端本身与 CI 都**不依赖** node）
- cmdline（round34 wizard 规划并重启生效）：`default_hugepagesz=1G hugepagesz=1G hugepages=2
  hugepagesz=2M hugepages=768 isolcpus=2-5 intel_iommu=on iommu=pt`；IOMMU 真启用（10 groups，
  vfio 绑定不需要 unsafe noiommu 模式）
- 数据面：ens192/ens224 交 DPDK（vfio-pci；`/etc/modules-load.d/nfvis-vfio-pci.conf` 已持久化）；
  **vs-vnf 拓扑在跑**——L2 + BVI `192.168.100.1/24` + vnf-a/vnf-b（各 1 vCPU / 1G 大页 / vhost-user，
  cloud-init 文件路径 user-data 在 `/data/incoming/vnf-{a,b}-ud.sh`）；
  **流量 round50 复验通过**：`ping 192.168.100.10/11 source 192.168.100.1 count 5` 双向 **5/5 0% 丢包**
  （独立事实源 `vppctl ping … source bvi0` 交叉核对 3/3），宿主经 DPDK 物理口直连 guest
  （`ping 192.168.155.240/.241`）各 4/4、**<1.1ms**
- 镜像：`debian-12-generic-amd64.qcow2`（集成测试可用）+ **`alpine.qcow2`**（round36 按 round23 记录重补，
  sha256 `900c91ed…`；**冒烟阶段 3 依赖**）+ 容器镜像 `alpine:3.20`（round36 经宿主代理重拉，
  阶段 3 的 `docker save` 依赖）。⚠️ **这两个前置在快照恢复后会丢**，重补方法见 §0 第 2 条。
- ⚠️ 跑 `make integration` 前：`systemctl stop nfvis` + **停掉 VNF 腾 1G 大页**（两台各占 1 页，free 需 ≥1）
  + `systemctl restart vpp`（见 §0 红线 6）
- ⚠️ 1G 池「无主占用」现象仍在（nr 可能高于 cmdline 声明：现 nr=3 而声明 2；VPP 改吃 2M 后已显著收敛，
  未做回收）——`show system kernel` 因此长期报「大页 1G：期望 2，实际 3」，属已知项
- ⚠️ **停/起 VNF 之后，guest 内的网络配置不会自己回来**（round36 实测，见 §0 红线 12）：
  user-data 内容没变 → instance-id 没变 → cloud-init 按 once-per-instance **不重放**，
  而 guest 自身启动时的网络栈只拿到 VMnet8 的 DHCP 地址；脚本里 `ip addr add` 的静态地址是运行期的、
  guest 一重启就没了。**要复通就改一次 user-data 内容再 `restart`**（或重建 VM）。
- 本轮完整现场与发现见 `docs/evidence/v1-closeout-round36-three-suites.txt`（§6 为终态核对、
  §7 为本轮新发现）；round34 的现场见 `docs/evidence/v1-closeout-round34-manual-flow.txt` §4

```bash
systemctl status nfvis        # active
/usr/bin/nfvisd -version      # 1.1.26
cat /proc/cmdline             # 见上
```

### 3.2 大页与内核基线的历史记录（**已由 §3.1 取代；机制说明仍有效**）

> 基线参数的**自动补全与护栏**见决策 #104（vendor/irqaffinity/nohz_full 探测、非隔离核 ≥2）；
> **低延迟 profile** 见决策 #105（`set system kernel low-latency` / 脚本 `--low-latency`，显式可选，
> VM 上自动省略 idle=poll/tsc=reliable）。历史（1G=2 来自旧机器 postinst 基线）：

`postinst` 在无基线时按机器规格写 `/etc/default/grub.d/99-nfvis.cfg`（RAM 5G → 1G 大页 1 页）、
维护 fstab 大页行并跑 `update-grub`；**重启后生效**。实测重启后：

```bash
cat /sys/kernel/mm/hugepages/hugepages-1048576kB/nr_hugepages   # 2
grep -E 'hugepages|isolcpus' /proc/cmdline                      # default_hugepagesz=1G hugepagesz=1G hugepages=1（isolcpus 仍缺）
```

⚠️ **运行期写 `nr_hugepages` 对 1G 尺寸无效**（必须带 `hugepagesz=1G` 重启）——这正是 `postinst`
提示「需重启生效」的含义。⚠️ 若将来 `systemctl reboot`：**DPDK 绑定会丢**（网卡回到 vmxnet3），
且 **VPP 里的端口需要重新播种 startup.conf**（发现 #8）。

> 历史（已作废）：2026-09-15 曾有「带外设置 1G = 3 页、无需重启」的记录——那台机已被还原，该记录不再适用。

### 3.3 常用命令（**2026-09-20 round35 校正**）

```bash
# ── 同步源码 + 构建（VM 直连 proxy.golang.org 不通，须 GOPROXY）──
git archive --format=tar.gz -o /tmp/nfvis.tgz HEAD
scp -q /tmp/nfvis.tgz nfvis-vm:/tmp/
ssh nfvis-vm 'export GOPROXY=https://goproxy.cn,direct GOSUMDB=off;   rm -rf /root/src && mkdir -p /root/src && tar -xzf /tmp/nfvis.tgz -C /root/src && cd /root/src && go build ./...'

# ── 打 deb：⚠️ 全新 Ubuntu Server **不带 make**（apt-get install -y make 先装上）──
EPOCH=$(git log -1 --format=%ct)
ssh nfvis-vm "apt-get install -y make; cd /root/src && GOPROXY=https://goproxy.cn,direct   make deb VERSION=1.1.26 SOURCE_DATE_EPOCH=$EPOCH"
#   git archive 的树没有 .git → 必须显式给 SOURCE_DATE_EPOCH（否则回落 0，包内时间戳 1970）

# ── 装机 / 首启 / 取一次性口令 ──
ssh nfvis-vm 'dpkg -i /root/src/build/nfvis_1.1.26_amd64.deb && systemctl start nfvis'
ssh nfvis-vm 'journalctl -u nfvis --no-pager | grep 一次性口令 | tail -1'

# ── 开发态实例（明文；三件套默认就打这一台）──
ssh nfvis-vm 'cd /root/src && go build -o /tmp/nfvisd ./cmd/nfvisd && go build -o /tmp/nfvis-cli ./cmd/nfvis-cli
  systemctl stop nfvis; pkill -x nfvisd; sleep 1; rm -f /tmp/nfvis.db*
  setsid /tmp/nfvisd -db /tmp/nfvis.db -listen 127.0.0.1:18443 -init-admin-password "Admin@12345" -allow-plaintext >/root/nfvisd.out 2>&1 </dev/null &
  sleep 4; pgrep -x nfvisd >/dev/null && echo up'   # ⚠️ pkill -x 杀不掉改名后的进程（§5）

# ── 真机集成（**跑前：systemctl stop nfvis + systemctl restart vpp + 停掉 VNF 腾 1G 大页**；CI 不跑）──
#   Makefile 内已显式 `-timeout 45m`（guest 类用例单条就要 2~7 分钟，缺省 10m 会被顶穿）
ssh nfvis-vm 'cd /root/src && GOPROXY=https://goproxy.cn,direct make integration'
#   ⚠️ 镜像由测试自动挑（判据是「guest 的 cloud-init 挂得上 seed」，本机为
#      debian-12-generic-amd64.qcow2）；也可用 NFVIS_TEST_VM_IMAGE 指定。选 alpine
#      只会得到超时——原因是它的 busybox mount 挂不上 seed（**不是缺 ahci**，round56 更正，
#      见决策 #140）；debian-12-genericcloud 缺 ahci、SATA 交付下同样超时（virtio 交付后未复验）

# ── 三件套对**已装实例**跑（发现 #5 已修：两个脚本都支持环境变量，**不必再打补丁副本**）──
#   口令取自 §3.1（随快照恢复而变，别照抄旧值）：
#     PW=$(ssh nfvis-vm 'journalctl -u nfvis --no-pager | grep 一次性口令 | tail -1' | awk '{print $NF}')
#   语义校验：HTTPS 下自动用产品证书 /var/lib/nfvis/tls/server.crt（也可 NFVIS_CA=… 指定；
#             仅实验室可 NFVIS_INSECURE=1 = curl -k，不默认开启）
ssh nfvis-vm 'cd /root/src && SRV=https://127.0.0.1:443 CLI_BIN=/usr/bin/nfvis-cli   NFVIS_PASSWORD="WBF81vOA4M8GM28f@Aa1" bash contrib/scripts/cli-semantic-check.sh'
#   256 条冒烟：目标与凭据同样全部经环境变量；对已装实例跑时另给 API_PORT（阶段 2 会声明它）
#   ⚠️ 对已装实例跑会因**前置**（池已满 / 预块首条为空操作）产生成片级联失败——判读见 §0 第 2 条
ssh nfvis-vm 'cd /root/src && SRV=https://127.0.0.1:443 CLI_BIN=/usr/bin/nfvis-cli   NFVIS_PASSWORD="WBF81vOA4M8GM28f@Aa1" API_PORT=443 bash contrib/scripts/cli-fulltest.sh'

# ── 三件套对**干净开发态基线**跑（要拿"套件本身是否全绿"这个结论就跑这一台）──
ssh nfvis-vm 'cd /root/src && SRV=http://127.0.0.1:18443 CLI_BIN=/tmp/nfvis-cli \
    NFVIS_PASSWORD=Admin@12345 bash contrib/scripts/cli-fulltest.sh'
#   ⚠️ 需要 1G 大页有空闲（阶段 4 会真起一台 cli-vm）：先停已装服务与 VNF，或把池调大
```

**两条环境提示（round34/35 实测）**：
- apt 源用 **USTC**（`mirrors.ustc.edu.cn`；aliyun 实测几乎不可用）——切换要覆盖
  `/etc/apt/sources.list.d/*.sources`（deb822），只改 `*.list` 无效；
- 宿主 Windows 内存紧张时并行 `go test ./...` 会 `errno=1455` 崩溃（`make check` 整体红）——
  用 **`go test -p 1 ./...`** 串行跑（实测 17 包全 ok）；VPP 现在用 `/root/vpp-v26.06-deb` 的
  **本地 9 个 deb** 安装（`dpkg -i`），不再走 packagecloud。

**底座是按需装的（2026-09-18 备忘，全新 Ubuntu 上没有）**：

```bash
apt-get install -y golang-go                      # Go 1.26（apt 就有，与 go.mod 要求一致）
apt-get install -y libvirt-daemon-system libvirt-clients qemu-system-x86 qemu-utils docker.io
# VPP **不在 Ubuntu 源里**，走 FD.io 的 2606 源；**没有 resolute 套件，只能用 noble 构建**
echo "deb [trusted=yes] https://packagecloud.io/fdio/2606/ubuntu noble main" > /etc/apt/sources.list.d/fdio-2606.list
apt-get update && apt-get install -y vpp vpp-plugin-core vpp-plugin-dpdk vpp-drivers
#   [trusted=yes]：packagecloud 的 gpgkey 端点取不到（测试机做法，非生产）
# 业务口交 DPDK 前**必须先** modprobe vfio-pci（+ 无 IOMMU 时 enable_unsafe_noiommu_mode）——发现 #6
# 端口进 VPP 走**产品路径**（2026-09-18 决策 #100 修好发现 #8，此前只能带外手写 startup.conf）：
#   configure → set interfaces ens192 / set interfaces ens224
#             → set vpp dpdk dev ens192 / set vpp dpdk dev ens224 → commit
#   → request vpp restart      # 按 committed 重生成 startup.conf，口名→PCI 由绑定记录解析
# 若曾带外手写 startup.conf：产品启动时会把其中的 dev 映射导入
#   /var/lib/nfvis/dpdk-bindings.json（见 §3.4），故存量安装不必重绑即可转由产品维护。
```

### 3.4 网卡 DPDK 绑定（决策 #72；重启后会丢）

> ⚠️ **动手前必读**（2026-09-18 自锁门外的教训）：
> 1. **`ens160` 是管理口**（承载默认路由与 SSH）——`bind-dpdk` **绝不能碰它**。
>    `internal/orchestrator/network/dpdkbind.go` 的 `Bind/Unbind` **没有任何管理口守卫**，
>    文档只是"声明约束"；候选里也会出现管理口（发现 #7）。**先用内核事实确认哪个不能碰**：
>    `ip route show default`、`echo $SSH_CONNECTION`、`ip -o -4 addr show`。
> 2. **先 `modprobe vfio-pci`**（无 IOMMU 时还要 `echo Y > /sys/module/vfio/parameters/enable_unsafe_noiommu_mode`），
>    否则 `bind-dpdk` 直接失败（发现 #6；手册把它放在"CLI 不可用时"的方式 B，实际是**前置条件**）。
> 3. **绑定成功后端口不会自动进 VPP**：需把它**声明进配置**（`set interfaces <口>` + `set vpp dpdk dev <口>`
>    → commit → `request vpp restart`），见 §3.3 与用户手册 §3.3。
>    ✅ **2026-09-18 决策 #100 起产品路径已走通**（此前只能带外手写 startup.conf、且 `request vpp restart`
>    会把口弄丢）：口名→PCI 记在 `/var/lib/nfvis/dpdk-bindings.json`；`request vpp restart` 现在会按
>    committed + 该记录重生成 startup.conf，且对「已接管但未声明」的口**先告警**（掉口防呆）。
>    当前这台机器上 ens192/ens224 **都已由产品声明**（committed 的 `vpp.dpdk.per_dev`）。
> 4. 一旦失联：**带外 `reboot`** 即可恢复（vfio 绑定不跨重启）。

```bash
# 产品命令（会中断流量，须 --yes）
nfvis-cli -c "request interfaces 0000:13:00.0 unbind-dpdk to-driver vmxnet3 --yes"
nfvis-cli -c "request interfaces ens224 bind-dpdk --yes"
```
带外手工（CLI 不可用时；M3 交接文档有原文）：

```bash
modprobe vfio-pci; echo Y > /sys/module/vfio/parameters/enable_unsafe_noiommu_mode
for d in 0000:0b:00.0 0000:13:00.0; do
  echo $d > /sys/bus/pci/drivers/vmxnet3/unbind 2>/dev/null || true
  echo vfio-pci > /sys/bus/pci/devices/$d/driver_override
  echo $d > /sys/bus/pci/drivers/vfio-pci/bind
done
systemctl restart vpp; sleep 8; vppctl show interface
```
**实测平台行为**：① 绑定后内核网卡消失 → 只能按 PCI 定位，且须**按 PCI 回读驱动**；
② 清空 `driver_override` + `rescan` **不足以**让内核重探原生驱动 → 必须给 `to-driver`。

### 3.5 ⚠️ 默认 HTTPS（决策 #72，易踩）

- nfvisd **默认自动生成自签证书并启用 HTTPS**（`/var/lib/nfvis/tls/`）。
  - 开发/测试要明文：**必须显式 `-allow-plaintext`**（该开关是权威的，会忽略已装/自签证书）。
  - `curl` 用 https 需 `-k` 或 `--cacert /var/lib/nfvis/tls/server.crt`。
  - **`nfvis-cli` 零参数即可连**（缺省 `https://127.0.0.1:443` 且自动固定本机证书，决策 #78）；
    口令可用 `-p` / `NFVIS_PASSWORD` / **交互式提示**（决策 #80⑤）。
  - `make e2e` 用 `NFVIS_API=https://…`（证书经 `NFVIS_E2E_CA` 固定，缺省同上路径）。
- **监听地址收敛**：`-listen` 为通配且已配置管理口地址时收敛为管理口地址（未配在本机时不收敛）。

### 3.6 ⚠️ 跑集成测试前必须重置 VPP（踩过两次）

真机验证若在 VPP 里建过对象（BD/VRF/端口），**跑 `make integration` 前必须 `systemctl restart vpp`**，
否则残留拓扑会污染用例（曾遗留 3 个 bridge domain 导致 `TestE2EFlow` 的 VRF ping 0 received；
决策 #37 亦记过"同挂一个 BD 致 NAT 不回 ICMP"）。

### 3.7 ⚠️ cross-connect 环路（真机踩到，整机失联）

`cross-connect` 是**二层直通、无 MAC 学习、无环路保护**。把**同一广播域**内的两个端口直通
（本环境 ens192/ens224 与 ens160 同在 VMnet8）会形成**物理二层环路/广播风暴**——实测导致
**整机 load 21、内核 soft lockup、网络完全不可达**（详见 §5.6）。两端必须属于**不同**广播域。

### 3.8 其它环境事实

- VPP **不自启**（`systemctl start vpp`）。
- nfvis-vm 直连 proxy.golang.org 不通；`git push` 需 ssh key（开发机已配）。
- **开发机（Windows/Git Bash）内存限制**：commit 限额约 2 GB，`go test ./...`（并行编译）会
  `fatal error: out of memory`；用 `go test -p 1 ./...`。CI 不受影响。
- 开发机装了 `python`（无 `python3`）；`ssh-keygen -R <host>` 清陈旧 host key（VM 重装/回滚后会出现）。
- 部分命令输出中文在 Git Bash 下可能乱码（终端编码），不影响判定。
- ⚠️ **Git Bash（MSYS）的 `sed` 会吞掉 `\r`**，Linux 上不会——所以"CRLF 未剥"这类缺陷**在开发机上复现不出来**
  （round36 实测：同一段 `printf … | sed` 在 Windows 得 6 字符、在 VM 得 7 字符带 `\r`）。
  **脚本/工具的最终判定一律以 VM（Linux）为准**；自校准脚本里也把 `\r` 显式渲染成 `\\r` 再打印，
  否则两侧输出看起来一模一样（正是"藏证据"）。
- 开发机的 `/tmp` 与 Python 眼里的 `/tmp` **不是同一个目录**（Git Bash 的 `/tmp` 是 MSYS 临时目录，
  Python 会把 `/tmp/x` 解析成当前盘根下的 `D:\tmp\x`）——临时文件用**仓库内相对路径**（如 `build/`），
  别跨工具传 `/tmp` 路径（round36 踩中，红-绿验证因此白跑一次）。

### 3.9 ⚠️ 动包管理**之前先备份 `/var/lib/nfvis`**（2026-09-16 事故后的新流程）

2026-09-16 发布 v1.1.4/v1.1.5 期间，已装实例的配置库 `/var/lib/nfvis/nfvis.db` **被重置**：
日志显示 10:19:48 走了「首次启动」并生成了新的一次性口令，原配置与 admin 口令随之丢失，
**当时没有备份、无法恢复**。事后核对 `prerm`/`postrm` 只碰 `/run/nfvis`、**不改数据**；
发布 v1.1.7 时按新流程实测「升级 → purge → 重装」三次，配置库**全程未变**
（revision / 用户 / 审计条数逐项与备份一致），并**顺带修好了此前「DB 被重置」的归因**：
不是打包脚本干的。**事故直接原因未查明**（当时有其他会话在操作同一台机），故流程上按「必须先备份」执行：

```bash
BK=/root/nfvis-db-backup-$(date +%Y%m%d-%H%M); mkdir -p "$BK"
python3 -c "import sqlite3;s=sqlite3.connect('/var/lib/nfvis/nfvis.db');d=sqlite3.connect('$BK/nfvis.db');s.backup(d);d.close();s.close()"
# 之后才 dpkg -i / --purge；每步后用 sqlite 读 revision/用户/审计 与备份比对
```

> 另：VM 上 admin 口令是**用户自己的**（事故后由用户重设），新会话不要臆测口令；
> 需要开发态实例时请用 `/tmp` 下的独立 DB + 自设口令（如 `-init-admin-password`）。

---

## 4. 机制速查

```bash
make check            # 提交前/CI 统一入口：vet + 全量测试 + 覆盖率门槛 + 依赖方向 + 决策条数 + openapi 同步 + 原型
make integration      # 真机集成（nfvis-vm；无 VPP socket 自动跳过，CI 不跑）
make e2e              # 端到端验收（需 NFVIS_API 指向已装环境）
make deb VERSION=x.y.z SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)   # 打包 deb（需 Linux/dpkg-deb；见 §2.7 可复现）

# 判定「cloud-init 到底有没有消费 seed」（round56 立的方法；只读、不动运行中的 VM）：
#   ① 停机态才挂盘：virsh shutdown <vm>（超时才 destroy）
#   ② qemu-img convert -O qcow2 <vm>/disk.qcow2 /tmp/copy.qcow2     # 用副本，原盘不动
#   ③ qemu-nbd --read-only --connect=/dev/nbd0 --format=qcow2 /tmp/copy.qcow2 && partprobe /dev/nbd0
#   ④ mount -o ro /dev/nbd0 /mnt  → 看 guest 的 /etc/hostname、/var/lib/cloud/instances/、
#      /var/log/cloud-init.log（成功=「Using data from /dev/vdb」；失败=「Failed to mount」+「Datasource DataSourceNone」）
#   ⑤ umount 后**必须** qemu-nbd --disconnect /dev/nbd0（红线 7）
#   ⚠️ 判"失败"前先做一次阳性对照（同一份 seed 给已知可用的镜像，如 debian-12-generic-amd64），
#      确认取证方法本身能测出"成功"——否则可能是"方法永远说没有"的假红

# 真机手动三件套（互补，CI 都跑不了；改动后至少跑前两个）
bash contrib/scripts/cli-fulltest.sh        # ① 命令能不能用：256 条契约命令，见 %/%% 即失败
#   ⚠️ 想拿"套件本身是否全绿"的结论，跑**干净开发态基线**（§3.3）；对已装实例跑会因池已满等前置成片级联
bash contrib/scripts/cli-semantic-check.sh  # ② 结果对不对：与 VPP/内核/libvirt 独立事实源对照 + 扰动判别（决策 #84/#85）
#   ⚠️ 产品装在系统上时须给 CLI 路径（脚本已加前置自检，缺了会明确报错、不再甩假失败）：
#      CLI_BIN=/usr/bin/nfvis-cli bash contrib/scripts/cli-semantic-check.sh
#   ⚠️ 收尾会还原扰动（跑前跑后 show configuration 应逐字节一致）——不一致就是工具又留了残留
bash contrib/scripts/cli-pty-smoke.sh [CLI路径] [server] [user] [password]   # ③ 交互行为 pty 冒烟（决策 #81）

# 工具自校准（桩式、不需要真机、CI 可跑；`make check` 的 toolcheck 已含）
bash contrib/scripts/cli-fulltest-selftest.sh     # 失败判定模式（发现 #9）
bash contrib/scripts/cli-semantic-selftest.sh     # 语义校验 oracle（round36）；SEM_SCRIPT=<副本> 做红-绿
```

- **契约先行**（AGENTS 规则 1）：改 CLI 命令 / API 端点 / 资源模型 → 先改 `docs/` 下契约 + 附录 A 追加决策，
  再写代码；命令树、OpenAPI 路径、附录 B 三处同步。
- **守护**（改动即红）：API↔契约（`internal/api/routes_contract_test.go`，**只判路径**）、
  **响应形状**（`internal/api/shape_contract_test.go`，决策 #116：按契约 schema 逐字段核对实际响应，
  字符串还须非空；会合法缺席的进显式白名单并写明理由——白名单**只收"会合法缺席"的字段**，
  把已实现字段列进去等于关掉断言）、CLI 语句↔模型（`cli_mapping_test.go` 的 `contractStatements`）、
  CLI 命令覆盖（`cli_contract_coverage_test.go`）、依赖方向（`internal/archtest`，含操作者可见文本
  `.go/.sh/.service/Makefile/deploy/*` **与 `.html/.js/.css`**）、决策条数（`check_decisions_count.sh`）、
  **契约 YAML 无重复键**（`check_openapi_no_duplicate_keys.sh`，决策 #116：PyYAML 对重复键静默取最后一个，
  会让发布的规范与文件所见不一致——R37-1 的 VppStatus 就是这么藏住的）、
  `openapi.json` 同步（`check_openapi_json_sync.sh`）。
- **提交约定**：LF 换行；提交信息中文 `<type>: <摘要>`，type ∈ docs/feat/fix/chore/test；引用 FR-xxx。
- **工作流**：**先建分支再改** → `make check` → 真机验证 → PR → 合并（合并后按惯例删分支）。
- **发布/装机前**：先按 §3.9 **备份 `/var/lib/nfvis`**（sqlite 一致性快照）再动包管理——
  2026-09-16 曾出现配置库被重置且无备份可恢复。备份后每步都用 sqlite 读 revision/用户/审计 与备份比对。
- **发布**：`make deb VERSION=x.y.z SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)` → 校验（元数据/内容/解包跑
  `-version`/实装+purge）→ `gh release create vX.Y.Z --target main --notes … <deb>` → **下载回来比对 sha256**。
  deb 现已**可复现**（决策 #88），哈希可用于溯源「这份包是否出自该 commit」；⚠️ 但 `git archive` 导出的
  工作树没有 `.git`，**必须显式传 `SOURCE_DATE_EPOCH`**，否则包内时间戳成 1970（仍可复现，但难看且易误判）。
  ⚠️ **随包文档（手册/契约/规格书/命令全表）是发布件的一部分**，发布前要核对它们是否为当前版本——
  round48 的 R48-5 就是这么抓到的（手册 §10.12 落后两个增量、仍写「只读视图」，而控制台已能改配置）。
  **发布后若必须改随包文档，照 round29 先例重切**：重建 → `dpkg-deb -x` 逐文件对照证明"只有文档不同、
  二进制逐字节相同"（这样装机链路结论不必重跑）→ 删原 release/tag 重建 → 回下载比对；
  另**核对 asset 名**（round48 首传把本地文件名后缀 `.recut` 带进了 asset 名，已重传修正）。

---

## 5. 踩坑记录（勿重蹈）

1. **口令哈希泄露**：commit 的审计详情是完整 diff，而 `/audit-logs` 只需 `ClassReadOnly` →
   read-only 账号可取他人哈希。**教训**：脱敏要覆盖"日志/审计/视图"全链路，且须在**渲染层**
   （放进 `Flatten` 会让"仅改口令"被判为无变更）。
6. **`pkill -x nfvisd` 杀不掉改名后的进程**：开发态实例若叫 `/tmp/nfvisd-old`（进程名 `nfvisd-old`），
   `-x` 精确匹配**不命中**，于是「杀掉旧的、起新的」实际是「旧进程还在占用 18443，新进程绑定失败」——
   后续所有请求都打到**上一个**实例上。本轮因此得到过一组「新旧二进制结果完全一样」的**假对比**
   （还据此差点写错结论）。**教训：换二进制前先 `for p in $(pgrep -f nfvisd); do readlink -f /proc/$p/exe; done`
   确认到底谁在服务**；`pgrep -f` 连路径一起匹配。
7. **管道会吞掉退出码，量退出码必须不经管道**：`$CLI -c ping … | grep -v 连接; echo $?` 拿到的是 **grep 的** 0，
   不是 CLI 的；`OUT=$(cmd | grep …)` 之后 `$?` 同理是 grep 的。本轮据此误判过「ping 退出码 0」。
   **教训：量退出码就单独执行、重定向到文件，别进管道。**（假绿之外，还有一类「假证据」。）
8. **上一轮自己造成的回归要回头查**：决策 #88 那次编辑把 `## 附录 B：…` 标题当成锚点替换掉了（新串里没带回来），
   于是 **v1.1.8 的包内规格书少了附录 B 标题**——守护全绿、测试全过，因为**没人检查文档结构**。
   本轮已补回。**教训：拿标题/锚点做替换时，改完要 `grep` 一次锚点还在不在。**
2. **"声明了但无实现/静默无效"反复出现**（最高频缺陷类型）：`sriov.vf-count`、`max-sessions`、
   `facility/severity`、`management ip address`、物理口 link 告警、**8 处 CLI 语句**都属此类。
   **教训**：命令树/契约里声明的东西若执行器未映射会**静默无效**，比缺功能更危险。
   决策 #79 后由 `DisallowUnknownFields` + `contractStatements` 清单双重兜底——
   **新增契约语句必须同时补进该清单**，否则它永远不会被执行到。
3. **契约与实现漂移**：`/audit-logs` 声明 `offset` 却静默忽略；LLDP `system_name` 在 govpp 里取不到；
   `request api token revoke` 在契约里被放在顶级（决策 #76⑧）。
4. **复核要用"证伪"而非采信标签**：15 条降级里 6 处措辞/证据不准，1 处是真缺陷
   （"无真机证据"的 IPv6 实际藏着 v6 路由不可见的 bug）。
5. **`pkill -f` 会杀掉自己的 ssh 会话**（命令行含匹配串时）。括号技巧（`pkill -f "[u]dp_listen"`）
   **只保护 pkill 自己的 argv，保护不了调用它的那个 shell**：2026-09-15 我在**同一个** `ssh host '<脚本>'`
   里既 `pkill -f "[1]27.0.0.1:18443"` 又写着 `-listen 127.0.0.1:18443`，远端 bash 的 cmdline 含该串
   → 整个会话被自己杀掉，**命令零输出**（排查了半天）。
   可靠做法：① 把脚本落成文件再跑（`bash /tmp/x.sh`，远端 bash 的 cmdline 就只有文件名）；
   ② 或按监听端口反查 PID 再 kill：`pid=$(ss -ltnp | sed -n 's/.*127.0.0.1:18443.*pid=\([0-9]*\).*/\1/p')`。
6. **⚠️ 我自己踩的：`git checkout <file>` 会丢掉该文件的全部未提交改动**。验证红-绿时我用它撤临时改动，
   结果把 `cliexec.go` 的整份改动退回 HEAD。**教训**：红-绿要撤的是"注入的坏代码"，
   应该 `cp` 备份后用 `sed` 改回，别用 `git checkout`。
7. **6 核 VM 上跑重负载会触发内核 soft lockup 而整机失联**：`watchdog: BUG: soft lockup - CPU#3 stuck for 52s`
   （`drain_vmap_area_work` → `flush_tlb_kernel_range` → `smp_call_function_many_cond`，
   即 CPU 饱和下 vmap 回收的跨 CPU TLB flush 长时间自旋）。当时 VPP 轮询线程占着隔离核，
   又在上面跑 Docker/镜像/容器定义。**教训**：验证时避免让 VPP 轮询与重负载同时在 6 核机上跑满；
   失联后可从 `journalctl -b -1` 取证据（重启不会丢上一 boot 的日志）。
8. **CLI 前端与服务端是两条路径**：守卫/校验只加在 HTTP handler → CLI 能绕过
   （`api.cliExecutor` 直调 Provider）。新增任何前置校验必须落在**两侧共同依赖处**（决策 #75）。
9. **⚠️ 交互行为只用管道（非 TTY）演示 → 交互缺陷全部漏网**（决策 #81）。`printf 'show ?\n' | nfvis-cli`
   这类演示里 `term.MakeRaw` 失败、退化 `readPlain`，于是「raw 模式下裸 `\n` 不回车导致的错位」与
   「`?` 必须回车」**都不会出现**——M2 的 `?` 演示记录正是这么跑的，`V1-验收检查表` 据此把
   FR-CLI-001/002/003 判了「通过」，而 Tab **从未实现过列出候选**、`?` **必须回车**。
   **教训**：交互行为必须在 **pty** 下验证——`script -qec '<cmd>' /dev/null`，并用 `cat -A`
   核对行尾是 `^M$`（CRLF）而不是裸 `$`（LF）；且「响铃并列出」这类措辞要**逐词**对照契约，不能只看「补全能用」。
   可复跑的手动冒烟：`contrib/scripts/cli-pty-smoke.sh`（10 项断言，修复前失败 7 项、修复后全绿）。
10. **⚠️ heredoc 会吞掉反斜杠与中文**（本会话被咬三次）：`python - <<'PY'` 里的 `'\\'` 变成 `'\'`（直接语法错误）、
   含 CJK 的匹配串被改坏（**`grep` 匹配不到却报 0 命中，看着像「已清理干净」**）。
   **教训**：脚本/提交信息一律**先 Write 落文件再执行**（`python build/x.py`、`git commit -F 文件`），
   别把带转义或中文的逻辑塞进 heredoc；发现「异常干净的 0 命中」要换个工具复核。
11. **⚠️ 工具/脚本自身出错会制造假红**（比假绿更伤信任，决策 #85）：语义校验脚本先后踩了三次——
    终端文本解析脆弱（把 `ens192` 截成 `192`）、`show l2fib` 空表按行数计得 1、
    **前置条件没校验**（`CLI_BIN=/tmp/nfvis-cli` 已被删 → 所有经 CLI 的断言退化成空输出 → 3 通过/6 失败，
    看着像产品大面积回归，实际完全正常）。**教训**：这类脚本必须**先校验前置条件并 `exit 1` 说清原因**，
    且结论采信前先做「已知正确 → 判 PASS、已知错误 → 判 FAIL」的自校准。
12. **deb 不是逐字节可复现的**：同一棵源码树连续 `make deb` 两次，哈希就不同（打包层 tar 成员 mtime + 压缩）。
   **教训**：别用「重打包 → 比哈希」来验证源码一致；发布校验只能以**发布件自身**的 sha256 为准
   （构建端与回下载件一致即可）。要做可复现构建需给 `dpkg-deb` 设 `SOURCE_DATE_EPOCH`（见 §2.7）。
13. **测试会把缺陷固化成期望**（决策 #84/#86 各出现一次）：`TestDynamicCandidatesEndpoint` 曾断言
    「已配置的接口名**应成为候选**」（正是 #83 的 bug 本身）；5 处断言曾要求消息里**含 `FR-NET-021`** 等内部编号。
   **教训**：断言写**契约语义**（「修改需先关机」「跨 NUMA」），不写实现细节；改行为时若既有测试大面积变红，
   先分辨「产品或测试谁错了」。
14. **收尾必须还原环境**：本会话多次起开发态 `nfvisd`（`/tmp` DB）并扰动 VPP。收尾动作：
    `kill` 开发态实例 → 删 `/tmp` 中间产物 → `systemctl restart vpp` → （若做过装机）`systemctl restart nfvis`，
    最后核对「VPP 里只剩配置声明的对象、服务 active、18143/18443 端口已释放」。
15. **防呆/守卫要验证它"真的生效"，不能因为"没触发"就假定存在**：2026-09-18 有会话要绑管理口，
    第一次因 `vfio-pci` 未加载而失败，**被误当成"守卫挡住了"**，第二次直接把自己锁在门外
    （`bind-dpdk` 把承载 SSH 的网卡交给 DPDK，SSH 当场断，只能带外重启）。**并且永不拿管理路径做试验**：
    先用非管理口验证守卫与流程，或先用内核事实确认哪个口不能碰。
16. **裸机/新装走一遍应作为发布前门槛**：2026-09-18 从零装一遍才暴露三条既有守护**完全看不到**的缺陷
    （#8 业务口交 VPP 的路走不通、#9 冒烟对 `show log audit` 的判定依赖环境历史、
    #10 自签重签后 CLI 因证书缺 IP SAN 而全断），而单测/守护/256 条冒烟在旧环境里全绿——
    **"装一遍"与"跑测试"不是同一件事**。另：**手工验 `ping` 一类命令时不要相信管道后的 `$?`**
    （那是 grep 的退出码），量退出码要重定向到文件再读。