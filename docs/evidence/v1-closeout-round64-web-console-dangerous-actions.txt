===== V1 收尾第六十四轮：控制台刀 4b 第二段（危险动作接入 + uiNotWired 收口到 6）=====
日期：2026-09-24；分支 console-ia-4b2；决策 #141 第 4 刀第三段

----- 1. 交付 -----
按 §5.4/§7 接入危险动作并**逐个走三档确认**：
- `#/system`（系统域入口，只读回显）★、`#/system/kernel`（三方对照：运行实际/配置期望/差异；apply/rollback=中危）★、
  `#/system/users`（用户与权限，**高危**：确认词＝用户名）★、`#/system/tls`（粘贴 PEM 上传=**高危**（词 `install-cert`）；
  重签=中危）★、`#/system/interfaces/:name`（DPDK/SR-IOV=中危，影响面写明"该口会离开内核平面"，管理口守卫在服务端）★；
- 运维页：软件升级/回退（**高危**，词 `upgrade`/`rollback`）、恢复配置（**高危**，词＝归档文件名）、
  恢复出厂（**高危**，词 `zeroize`）；
- QoS/SPAN 删除（中危，纯 candidate 写）；`GET /vpp/config`、`/virtual-switches/{name}/ports` 只读接入（为收口缺口）。
**核实结论**：login-users 的写路径既非纯 candidate 也非直连——服务端**取编辑锁 → 写候选 → 立即提交**；
故界面确认框写明"立即生效、取编辑锁"，并在动手前查本会话有无未提交候选（有就先拒绝并指路，避免把配置页改动一并提交）。
restore/zeroize 直连 + 立即提交；qos/span 删除纯 candidate（提示"提交后才生效"）。

**覆盖数字：已接 93 / 未接 6 / 契约 99**（原 76/23/99）——**达到设计 §5.5 的机器判据**；
剩余 6 条全是非界面读物：`/metrics`、`/openapi.json`、`/ui`、`/ui/`、`/cli/execute`、`/cli/candidates`。

**顺带查出**：`POST /system/kernel:apply|:rollback` **契约声明但服务端从未注册（实测 404）**——与 R51-2 的
`/system/kernel` 同类"幽灵端点"（`routes_contract` 只查反方向）。界面已如实报"本机服务端没有提供这个接口，
请在命令行执行"，**不谎报成功**；已登记待办 §2.3（修需服务端补注册）。

----- 2. 浏览器操作验收（决策 #141 口径）-----
**高危闸门（用「恢复出厂」验形态，全程只取消、绝不执行）**：

| 步骤 | 断言 | 结果 |
|---|---|---|
| 点「恢复出厂」 | 有确认词输入框（提示词 `zeroize`）、**影响面 5 条**、主按钮标红、初始**禁用**且写着「执行（10 秒后可点）」 | ✓ |
| 输错词 `zeroiz` | 仍禁用 | ✓ |
| 输对词但倒计时未完 | **仍禁用** | ✓ |
| 词对 + 倒计时走完（约 10 秒） | 才变可点（文案变「执行」） | ✓ |
| 点「取消」 | 弹框关闭；**未执行**（独立源：审计无 `system.zeroize`；用户表仍只有 admin） | ✓ |

> **未验如实登记（两项）**：
> ① **高危动作的"真执行"没跑成**——我按验收步骤去建临时用户时，探测没能打开建用户的模态框（大概缺了必填项如
> class，`#modal-word` 未出现），随即我的上下文耗尽，未再硬凑。**故"高危动作真的改到了底座"这一层本轮只有
> 桩式自校准（断言请求序列）与闸门形态的浏览器证据，没有真执行证据**；精确步骤（建 `netop-tmp` → 闸门 → 对照
> `show users`/`/system/login-users`/审计/`revision +1` → 再删除 → 验 409 原文）已写在子代理报告里，下一刀开头补。
> ② §7 要求的"高危审计记意图+结果两条"**服务端未实现**（各动作只写一条结果行），界面无法补——已登记。

----- 3. 环境（前后）-----
配置库逐项未变（rev 55 / audit 104 / cfgsha 084c3a64…）；我的探测**无副作用**（用户表仍只有 admin、审计未变）；
收尾停开发态实例 → restart vpp → 起回已装服务；临时件与源码树已清。
