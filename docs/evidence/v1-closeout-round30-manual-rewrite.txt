===== V1 收尾第三十轮：用户手册重写（细化版）+ 命令准确性机器验证 =====
日期：2026-09-19
范围：仅 `docs/NFViS-用户手册.md`（945 行 → 1359 行）；产品代码零改动。

----- 1. 重写要点 -----
在既有手册（逐条真机核验过的内容全部保留）基础上按章节细化扩写：
- 新增「快速上手（5 分钟版）」与「1.3 平面划分」（管理面/数据面/计算面 + 三条铁律）；
- §2 安装扩为「安装与卸载」：postinst 行为逐条（含基线自动补厂商参数）、升级/降级、
  purge 逐项处置表（含 #28 修复后的自动撤销）；
- 新增 §4「CLI 基础」独立成章：模式导航、事务模型（含 confirmed 语义与会话锁）、
  compare/rollback/load/save/annotate、管道、配置层次、配置态 vs 运行态；
- §5 wizard 向导成章：问题与默认值表、输出样例（真机 pty 记录）、非 TTY 行为、完成后动作；
- §6 内核基线：三路径、自动补全规则表、护栏报错样例、低延迟代价表、规划速查表；
- §7 数据面：管理口守卫、绑定/解绑详解、startup.conf 与掉口防呆、**重启后固定动作清单**；
- §8~§9 配置任务每节补核对命令与约束说明（NAT 派生规则、bond 引用等价、cloud-init
  三条实测注意事项：seed 驱动要求/网卡命名/验证方法）；
- §10 运维扩为 11 节：日志审计（时钟标记）、告警、备份/zeroize、证书密钥、监控接入表、
  software add；§11 故障排查按「安装登录/基线数据面/资源负载」分组扩到 20+ 条；
- §12 附录：文件目录表补 dpdk-bindings.json/SSH drop-in、无鉴权端点、高频十条速查。

----- 2. 守卫与准确性验证 -----
- archtest 操作者读物守卫（禁 FR-xxx/决策 #n/M 编号/附录 A；允许 § 导航与圈号步骤）：
  `make check` 全绿（决策 108 项一致）。
- **show 语句真机执行**：手册中 39 条去重 show 语句逐条在 nfvis-vm 执行，0 条未知命令。
- **set/delete 语句 schema 验证**：手册代码块按顺序解析（跟踪 edit 层级展开全路径、
  跳过多行 user-data），101 条语句在单会话 candidate 中顺序执行，schema 级错误 0 条
  （`nfvis-cli -c "configure; <全部语句>; discard"`，%% 错误 0）。
  语义级校验依赖对象间引用（示例中的 vs-dmz/fw-vm 互指），不在本验证范围——
  其由 commit check 在真实使用时给出。
- 验证脚本：build/verify-manual.sh（show）/ build/verify-manual-set.py（set）。
