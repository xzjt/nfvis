===== V1 收尾第七十六轮：发布 v1.1.32（四条待裁收口：决策 #147~#150）=====
日期：2026-09-24；main `68e149a`（发布点）；发布件 `nfvis_1.1.32_amd64.deb`

----- 1. 本版内容（交接文档"仍未裁"的四条，逐条关闭）-----
| 条 | 决策 | 一句话 | 浏览器/真机验收的落点 |
|---|---|---|---|
| ① 控制台「我的账号」自助改密 | #147 | 只读账号此前在**界面上**无处改口令（表单在 super-user 页上，决策 #145 起隐藏）；顶栏加入口（所有角色可见、**不带 `data-write`**），复用既有处理器与高危闸门 | 真机浏览器（round72 证据 §3）：只读账号改密成功、**新口令登录 200 / 旧口令 401**、审计留痕、未自动登出 |
| ② 备份导出目标路径校验 | #148 | `request system configuration backup to <path>` 目标此前无校验（手误 `to /etc/fstab` 会截断系统文件）；现 5 条判据拒（绝对路径 / **目标不得已存在**（`O_EXCL`）/ 父目录须存在 / 目录拒 / 符号链接按已存在拒） | 真机（round73 证据 §3）：`/etc/fstab` 被挡住且**哈希不变**；合法路径 0600 成功 |
| ③ 诊断归档脱敏 | #149 | `GET /system/tech-support/{file}` 只要 read-only，而归档里 `config.json` 含**口令哈希**、`logs.txt`（journalctl 原文）含**首启引导一次性口令**；裁决＝**脱敏 + 保持 read-only** + 归档 0600/目录 0700 | 真机（round74 证据 §5）：归档内 0 命中、独立事实源有值、read-only 仍能下载 |
| ④ 高危审计「意图 + 结果」两条 | #150 | 设计 §7 的要求此前只有一条结果行；现 `result` 增设 `intent`（动作前落库，校验与确认守卫之后），覆盖 zeroize/软件升级回退/恢复配置/证书上传/建删用户/改口令策略；**非高危仍一条** | 真机（round75 证据 §3）：每条 +2 行、坏归档走 failure、文件不存在 0 条 |

----- 2. 发布校验（round48 口径，逐项）-----
| # | 项 | 结果 |
|---|---|---|
| ① | 元数据 | `Package nfvis / Version 1.1.32 / amd64 / Section net / Depends libc6`；control 4 件齐 |
| ② | 内容 | **23 个成员** |
| ③ | 解包运行 | `nfvisd -version` / `nfvis-cli -version` = **1.1.32** |
| ④ | **可复现** | 同 commit + 同 VERSION **连打两次 sha256 逐字节一致**（`2b8029ff78eb306bcaa9e079c6cbda2cb6b21d793998328e325d2b29b17818ad`，7,945,486 字节） |
| ⑤ | 随包文档是当前版本 | 规格书 **150 条决策**（含 #147~#150）；用户手册 1488 行，写明导出路径三条判据（§10.7）、**诊断包"配置是脱敏视图"**（§10.7）、审计「意图 + 结果」两条（§10.5/§10.12）；OpenAPI 含脱敏口径（2 处）；附录 B 标题仍在 |
| ⑥ | **随包前端 = 仓库源码** | `/api/v1/ui/{index.html,app.js,style.css,router.js,routes.json}` 与仓库源码**逐字节一致**（`cmp` 全过）——即"内嵌进二进制的前端 = round72 浏览器验收过的那份" |
| ⑦ | 升级实装 1.1.31 → 1.1.32 | prerm/postinst 正常；服务 **active**；`show version` = 1.1.32；**既有 VNF 拓扑存活**（vnf-a/vnf-b running、BD 6252701）；**配置库未变**（rev 55 / audit 104 / cfgsha `084c3a64…`）；`wizard` 非 TTY 明确返提示 |
| ⑧ | REST/Web 内容实测 | `/ui/` 200（48433 字节）、`/ui` 302、未认证 401；`POST /login` 回 `user={name,class}`；`/system/status` 契约字段齐全；`/system/version` = 1.1.32 + 5 组件；`/configuration` **0 处 `password_hash`**；`/configuration/history` 51 条；`/configuration/check` 未持锁 **409**；`/system/logs?last=5` 200；`POST /logout` **204** 且旧 token 立即 **401** |
| ⑨ | **本版新内容实测**（已装二进制 + 生产库副本） | **②**：相对路径被拒；已存在文件被拒且哈希不变；合法路径 `600` + 归档头；`/etc/fstab` 被拒且哈希不变（`9eebf9ed…`）。**③**：归档 `600`、目录 `700`；`config.json` 的 `password_hash/pbkdf2` **0 命中**（`hostname` 仍在 2 处）；`logs.txt` 的「一次性口令」**0 命中**；read-only 下载诊断归档 **200**、下载配置备份 **403**。**④**：删用户 `config.commit` 两条（intent「删除本地用户 del-g（原权限类 operator）」→ success）；非高危提交**仍一条**；审计里 `pbkdf2$` **0 次**。**①**：read-only 本人改口令 **200**、新口令登录 **200** / 旧口令 **401**；内嵌前端含 `#account-btn` 与 `openAcct` |
| ⑩ | 决策 #143 口径未回退 | read-only 下载配置备份 **403**、诊断归档 **200**（两者分工明确） |
| ⑪ | purge | postrm 自动撤销 SSH 强化（drop-in 消失、`sshd -t` 通过）；GRUB 片段/fstab 保留并打印手工步骤；`/var/lib/nfvis` 保留（1.7G）；二进制与单元移除；`/run/nfvis` 清理；配置库未变 |
| ⑫ | 重装 | SSH 强化重新落地；**配置库逐字段一致**；启动日志有「管理口守卫事实」「恢复收敛完成」、**30 分钟内无「一次性口令」** |
| ⑬ | 重装后拓扑 + 流量 | BD 6252701 四口、vnf-a/vnf-b running；`vppctl ping` 双向 **10/10 0% 丢包**（首发 5 发各 4/5 是首包 ARP 预热）；宿主经 DPDK 物理口 **4/4、0.34~1.35ms** |
| ⑭ | 发布与回验 | tag `v1.1.32` 指向 main、非 draft/prerelease；assets = `nfvis_1.1.32_amd64.deb`（7,945,486）+ `.sha256`；**自 GitHub 回下载 sha256 与构建端逐字节一致**（`cmp` 通过） |

----- 3. 校验中撞到的一条**已登记**行为（不是本版引入）-----
本轮校验脚本里**三次**撞到「`login-users` 写路径提交后仍持有全局编辑锁」（round72 验收首次查出、已登记在交接 §2.3）：
用 REST 建完账号的会话没登出 ⇒ 随后的 CLI `configure` / 删账号 / 自助改密被 **409「candidate 会话锁被占用」** 挡住；
按决策 #119 的口径 `POST /logout` 释放锁后即通过（补跑的 G3/G4 即以此方式完成）。
**本版未改该行为**；它是这条登记的第三次现场复现，建议下一轮优先处置（修法见 §2.3）。

----- 4. 环境（本轮前后逐项核对）-----
- 生产库：**rev 55 / audit 104 / cfgsha `084c3a64e4cc0d43a16cbced28cb0742aaa0ef1783c75065eb7966b091eba726`**
  ——本轮开始前 = 升级后 = 本版内容实测后 = purge 后 = 重装后 = 收尾，**逐字段一致**
- 已装服务：**nfvis 1.1.32 active**（443 在听）；无开发态实例（8443 无监听）；临时件已清
- VPP：只剩配置声明的 **BD 6252701（bvi0）**；`vnf-a`/`vnf-b` running
- 宿主 GRUB 片段与 `/etc/fstab`：指纹与本轮开始时一致（②的校验**没有**改动它们）
- 口令：`WBF81vOA4M8GM28f@Aa1` **按 §3.1 哈希比对法**核对有效（全程未做登录试探）

----- 5. 仍未裁（交接 §2.3 / §0 入口块）-----
1. **`login-users` 写路径提交后仍持有全局编辑锁**（本版校验第三次复现，建议优先）。
2. **operator 级角色粒度**（界面仍会显示声明为 super-user 的入口，点下去被服务端拒；需服务端下发权威 class）。
3. 建议做一次「**秘密出口**」全量复查（本轮把诊断归档脱敏了，但同类出口是否还有别的，没有系统扫过）。
