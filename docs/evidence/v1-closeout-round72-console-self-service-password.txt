===== V1 收尾第七十二轮：控制台顶栏「我的账号」（只读账号可用的自助改密入口，决策 #147）=====
日期：2026-09-24；分支 `feat/console-self-service-password`（main `bca77ff` 之上）

----- 1. 起因与交付 -----
起因（round70 登记、本轮处置）：改口令的表单原先只在「用户与权限」页（`#/system/users`），而该页自决策 #145
起对 `read-only` 角色隐藏（导航不列、页内写入口隐藏）⇒ **只读用户在界面上无处改自己的口令**。服务端本来就
允许：`POST /api/v1/system/login-users/{name}:change-password` 注册为 **ClassReadOnly**（本人 + 验旧口令，
见 `internal/api/server.go` 与 `resources_w5.go` 的 `handleChangePassword`）——即缺口纯在界面侧。

| 文件 | 改了什么 |
|---|---|
| `internal/api/ui/index.html` | 顶栏加 `#account-btn`「我的账号」（`topbar-right`，**不带 `data-write`**）；新增只含「当前口令 / 新口令」的小对话框 `#acct`（`#acct-who`/`#acct-old`/`#acct-new`/`#acct-msg`/`#acct-close`/`#acct-submit`，同样**整块不带 `data-write`**），置于 `#modal` 之前（确认框叠在它上面） |
| `internal/api/ui/app.js` | 新增「我的账号」段：`acctMsg`（消息落在本框）、`openAcct`（每次打开先清空口令控件）、`closeAcct`；绑定 `#account-btn` → `openAcct`、`#acct-close` → `closeAcct`、`#acct-submit` → 读值后调 `usrMyPassword` 并随即清空；`#acct` 点遮罩与 Esc 也算取消（Esc 先给叠在上面的确认框）。`usrMyPassword` / `usrPrecheck` 各加一个**消息去处**参数（缺省＝用户页的 `#usr-msg`），顶栏这条的提示因此落在自己的框里——**一条处理器两个入口**，没有第二份实现 |
| `internal/api/ui/style.css` | 一条规则：`.modal-box .acct-forms { grid-template-columns: 1fr; }`（两个口令字段竖排，复用 `.cfg-forms` 的控件外观） |
| `docs/NFViS-用户手册.md` | §10.12 补两处**操作者可见**的说明（随包文档 = 发布件的一部分，R48-5 的教训：新交付的界面入口必须写进手册）：顶栏右上有「我的账号」；以及一段「改自己的口令」——**所有账号可见**（含只读账号）、高危档确认、改完用新口令重新登录（不会自动登出）、用户页那条表单仍保留。**未改**其余段落 |

设计要点（逐条对应本轮要求）：
1. **免构建**：原生 HTML/CSS/JS，无 npm、无打包步骤（Go `embed`）。
2. **入口与对话框都不带 `data-write`**——这正是本条的**目的**：只读账号要看得见、用得上（带了就会被
   `body.role-readonly [data-write]` 的 CSS 规则隐藏，本入口白加）。入口给了稳定 id `account-btn` 便于守护断言。
3. **分级确认按高危档**（与用户页那条一致）：确认词＝**当前登录用户名**、默认 10 秒倒计时、只读回显
   `request system password change`、影响面四条（立即生效 / 服务端验旧口令 / 新口令按本机策略 / 可回退）。
4. **改完不自动登出**：只提示「口令已修改——请用新口令重新登录。」；框可取消（点「取消」、点遮罩、按 Esc）。
5. **口令控件不在别处留值**：只在提交那一刻读值、随后立即清空；开框时也先清空（不把上次的输入带进来）。
6. **契约与服务端一字未改**（REST 路径、注册 class 都不动）；用户页那条表单**保留**（两个入口同源、行为一致）。

----- 2. 守护与验证 -----
2.1 桩式自校准（真实 `app.js` + 最小 DOM 桩；`node contrib/scripts/web-console-confirm-selftest.js`）

新增**高危**用例（与既有用例同形，但走的是顶栏这条**两步**路径：先开表单框、填好再点提交——开框会清空
口令控件，故口令值在开框之后才填，与真人操作同序）：

```
— ⓪b 顶栏「我的账号」（决策 #147）：骨架在不在 + 入口有没有被标成写入口 —
  ✓ 骨架 index.html 里有 #acct（「我的账号」框）
  ✓ 骨架 index.html 里有 #acct-old（「我的账号」框）
  ✓ 骨架 index.html 里有 #acct-new（「我的账号」框）
  ✓ 骨架 index.html 里有 #acct-msg（「我的账号」框）
  ✓ 骨架 index.html 里有 #acct-close（「我的账号」框）
  ✓ 骨架 index.html 里有 #acct-submit（「我的账号」框）
  ✓ 骨架 index.html 里有 #account-btn（顶栏入口）
  ✓ #account-btn 没有 data-write（只读账号也要能看见/能用）
  ✓ #acct-old 没有 data-write（只读账号也要能看见/能用）
  ✓ #acct-new 没有 data-write（只读账号也要能看见/能用）
  ✓ #acct-close 没有 data-write（只读账号也要能看见/能用）
  ✓ #acct-submit 没有 data-write（只读账号也要能看见/能用）
  ✓ app.js 里顶栏入口接的是 openAcct（不是直接发请求）
```

```
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认框弹出且标题是动作名
  ✓ 修改我的口令（高危，顶栏「我的账号」）：逐条列出影响面（≥ 2 条）
  ✓ 修改我的口令（高危，顶栏「我的账号」）：主按钮标红
  ✓ 修改我的口令（高危，顶栏「我的账号」）：出现确认词闸门（输入框真的在）
  ✓ 修改我的口令（高危，顶栏「我的账号」）：闸门里写明确认词是 "admin"
  ✓ 修改我的口令（高危，顶栏「我的账号」）：初始「执行」不可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：默认倒计时 10 秒（提示里写明）
  ✓ 修改我的口令（高危，顶栏「我的账号」）：按钮上标注还要等几秒
  ✓ 修改我的口令（高危，顶栏「我的账号」）：只读回显对应命令
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认之前一条写请求都没发
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认词为空：仍不可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认词不匹配（多打一个字符）：仍不可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认词正确但倒计时没走完：仍不可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：倒计时差 1 秒（9/10）：仍不可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：倒计时走完（10 秒）：按钮才可点
  ✓ 修改我的口令（高危，顶栏「我的账号」）：取消之后仍然没有写请求（取消 = 什么都没做）
  ✓ 修改我的口令（高危，顶栏「我的账号」）：取消之后框收起、倒计时没有留在后台
  ✓ 修改我的口令（高危，顶栏「我的账号」）：确认后发出 POST /\/system\/login-users\/admin:change-password$/
  ✓ 修改我的口令（高危，顶栏「我的账号」）：执行完框已收起
```

整轮结论：`全部符合预期`（rc=0）——既有的全部用例（低/中危档位表、高危两道闸门、候选脏数据拦截、
不许退回 `window.confirm`、直连 fetch 的失败文案）**仍全绿**。

2.2 红-绿（变异验证）

`bash contrib/scripts/web-console-confirm-selftest.sh`（本轮新增变异体 `acctgate`：把顶栏这条路径的确认
闸门拆掉——只对"消息落在账号框里"的调用直接放行）：

```
✓ 拆掉 acctgate 之后同一套用例报 ✗：✗ 修改我的口令（高危，顶栏「我的账号」）：确认框弹出且标题是动作名：visible=false title="修改我的口令"
全部符合预期
```

手跑变异体（`--mutate acctgate` 生成后 `WEB_CONSOLE_APP_JS=<变异体> node …`）——报 ✗ 的正是**新用例**，
且关键一条是"确认之前不许发写请求"被击穿（即"没弹框就直接发了请求"，正是要拦的假绿）：

```
  ✗ 修改我的口令（高危，顶栏「我的账号」）：确认框弹出且标题是动作名：visible=false title="修改我的口令"
  ✗ 修改我的口令（高危，顶栏「我的账号」）：初始「执行」不可点
  ✗ 修改我的口令（高危，顶栏「我的账号」）：默认倒计时 10 秒（提示里写明）："倒计时已结束，现在可以执行（确认词仍需匹配）。"
  ✗ 修改我的口令（高危，顶栏「我的账号」）：按钮上标注还要等几秒："执行"
  ✗ 修改我的口令（高危，顶栏「我的账号」）：确认之前一条写请求都没发：已发写请求：["POST /api/v1/system/login-users/admin:change-password"]
  ✗ 修改我的口令（高危，顶栏「我的账号」）：确认词正确但倒计时没走完：仍不可点：label="执行"
  ✗ 修改我的口令（高危，顶栏「我的账号」）：倒计时差 1 秒（9/10）：仍不可点：label="执行"
  ✗ 修改我的口令（高危，顶栏「我的账号」）：取消之后仍然没有写请求（取消 = 什么都没做）：已发写请求：["POST /api/v1/system/login-users/admin:change-password"]
有不符合预期的用例
```

Go 守护的红-绿（`TestUIConsoleRoleGating` 新增的"入口不许带 `data-write`"一条）：临时给 `#account-btn`
加上 `data-write` 后——

```
--- FAIL: TestUIConsoleRoleGating (0.00s)
    ui_routes_test.go:255: 顶栏「我的账号」入口被打上了 data-write：<button id="account-btn" data-write type="button" class="ghost">——只读账号会看不到它，自助改密入口就没了
FAIL
FAIL	github.com/xzjt/nfvis/internal/api	0.047s
```

（已还原；`git diff --stat internal/api/ui/index.html` 只剩本轮 32 行新增。）

2.3 决策条数与自检

```
$ bash contrib/scripts/check_decisions_count.sh
✓ 决策条数一致（147 项）且决策记录表格结构完整
```

`make check`（宿主 Windows，为避开内存紧张导致的 `errno=1455` 用 `GOFLAGS=-p=1` 串行跑）**全绿**（rc=0）：

```
ok  github.com/xzjt/nfvis/internal/api       250.007s
ok  github.com/xzjt/nfvis/internal/archtest  1.895s      （含 user_text 守护：本轮的注释/文案无内部引用）
./internal/config/ 82.5%  model 83.8%  schema 90.8%  network 74.7%  compute 87.8%  container 76.3%  images 75.1%
✓ 决策条数一致（147 项）且决策记录表格结构完整
toolcheck：4 项桩式自校准全过（含本轮 web-console-confirm-selftest.sh 的 acctgate 变异体）
rc=0
```

（首次跑 `make check` 时 `TestUserVisibleTextHasNoInternalRefs` 报了两处——`selftest.js` 的 `console.log`
文案里写了决策编号、`index.html` 顶栏注释的**续行**里写了决策编号（HTML 注释按**行**剥离，续行算可见文本）
——均已改到注释首行/去掉编号后复跑全绿。这正是那条守护该抓的东西。）

----- 3. 浏览器验收（**占位：由主会话在真机浏览器里执行后填写**）-----
（由主会话在真机浏览器里执行后填写——本轮**没有**做任何浏览器操作，也没有编造结果。下面是待执行的
步骤与断言，按决策 #141 口径：界面显示对了不算完，要证明它真的改到了底座。）

1. **只读账号可见性（本条的存在理由）**：以 `read-only` 账号登录 `GET /api/v1/ui/` → 顶栏**看得见**
   「我的账号」按钮（`#account-btn` 非隐藏），且导航里仍看不到「用户与权限」等写页。
   断言：`document.getElementById('account-btn').offsetParent !== null`；页内 `[data-write]` 可见数为 0。
2. **两步流程与分级确认**：点「我的账号」→ 框弹出且写着当前登录用户名；填入当前口令与新口令 → 点「修改口令」
   → 确认框标题「修改我的口令」、**影响面 ≥ 2 条**、只读回显 `request system password change`、确认词要求
   输入**当前登录用户名**、按钮显示「执行（10 秒后可点）」。
   断言：倒计时期间按钮 `disabled === true`；输入错误确认词仍 `disabled`；倒计时走完 + 确认词正确才可点。
3. **真的改到了底座（独立事实源对照）**：确认执行后，用**新口令**重新登录成功、用**旧口令**登录失败
   （或按待办 §3.1 的口径读配置库哈希比对，避免反复试登录锁号）；界面提示「口令已修改——请用新口令重新登录。」
   且**没有**被自动登出。断言：`POST …:change-password` 返回 2xx；审计里出现一条改密记录
   （`show log audit` 或控制台审计页）；改完把口令改回原值（真机基线不被扰动）。
4. **取消路径**：重开框 → 点「取消」/点遮罩/按 Esc，三种方式都要能收起框；框收起后**没有**任何写请求
   （浏览器 Network 面板或服务端审计：无新增记录）。
5. **只读账号端到端**：用 `read-only` 账号完成一次「改自己的口令」（第 2~3 步），确认服务端**放行**
   （这正是 `ClassReadOnly` 的自助语义）；同时对照：同一账号点写页动作仍被拒（403），界面不谎报成功。
6. **super-user 对照**：`super-user` 账号下顶栏入口同样可用；用户页那条「修改我的口令」表单**仍在**
   （本轮有意保留），两条路径行为一致。

----- 4. 环境 -----
- 宿主：Windows 10（10.0.19044，git bash）；`go1.26.3 windows/amd64`；`node v24.15.0`
  （node 只用于桩式自校准，产品构建与其余检查都不依赖它）。
- 分支 `feat/console-self-service-password`，基线 main `bca77ff`（2026-09-24，v1.1.31 发布提交）。
- **未碰真机 / VM**：未跑 `make integration`、未连 nfvis-vm、未做浏览器操作（§3 留给主会话）；
  未改 REST 契约、未改服务端 class（服务端本来就对）。
- 本轮新增/修改的守护：`contrib/scripts/web-console-confirm-selftest.js`（新用例 + 变异体 `acctgate`）、
  同名 `.sh`（变异体列表）、`internal/api/ui_routes_test.go`（`#account-btn` 无 `data-write` 等 4 组断言）。
