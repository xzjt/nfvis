# NFViS V1 验收检查表（FR 逐条对照证据）

| 文档属性 | 内容 |
|---|---|
| 用途 | **A-1**：把规格书 FR 逐条对照证据（代码 / 守护测试 / 真机记录），作为 V1 收尾签字依据 |
| 基线 | main `d83b46f`（PR #57）；决策 67 项；`make check` 全绿 |
| 检查日期 | 2026-09-14 |
| 判定口径 | **通过**＝代码 + 测试证据齐备，且关键路径有真机/集成证据；**未验**＝代码与单测齐备但无真机证据；**降级**＝能力受限（附理由）；**移 V2**＝规格书已声明范围外 |
| 配套文档 | `docs/NFViS-系统产品需求与目标架构规格书.md`（需求源）、`docs/M5-验收记录.md`、`docs/M4-验收记录.md`、`docs/M3-人工演示记录.md`、`docs/M5-11-端到端与基准报告.md`、`docs/evidence/` |

## 0. 结论摘要

规格书共 **109 条 FR**（按 FR 编号族：CLI 7、CFG 12、NET 18、CMP 23、API 7、OPS 20、SEC 8、SYS 14），
本表**逐条覆盖、无遗漏**。§4.3 的 ACL/NAT/SPAN/QoS 无 FR 编号，另计 4 条功能行。

| 判定 | 条数 | 说明 |
|---|---|---|
| 通过 | **101**（+4 条非 FR 功能） | 代码 + 单测/守护 + 真机/集成证据齐备（FR-SYS-012 为"通过（环境受限）"，降级路径已验，见 §5.1）。**FR-SEC-006 由降级转通过**：该 FR 拆两半后 SSH 半已实现（决策 #96），底座最小化半不在 deb 交付范围（见 §5.2/§5.3） |
| 未验 | 4 | 有代码与单测、**无真机证据**：SR-IOV（FR-NET-004、FR-NET-021）、LLDP 邻居（FR-NET-018）、真人 `Ctrl-]`（FR-CMP-014） |
| 降级 | **2** | FR-NET-022（容器侧 memif 通流）、FR-SEC-007（脱敏的有意例外）——逐条见 §5.2 |
| 移 V2 | 2 | tap 接入（FR-NET-024）、外部镜像仓库（FR-CMP-034） |
| 合计 | **109** | |
| 另记（非 FR 行） | — | §10 吞吐/容量类基准未测（见 §4）；`show vpp runtime`、NFR-005/006 见 §5.2 |

> **2026-09-14 第七轮（决策 #75）**：**FR-CMP-015 快照磁盘内容级回滚**在 guest 内实证（T0-5 关闭，
> 由降级转**通过**）——cloud-init 写 KNOWN → 串口改 LATER → 关机快照 → 改 CHANGED → 关机回滚 →
> 重引导回读 = LATER。**同时证伪**原交接文档的猜测：运行中回滚**不报 libvirt 错**，但会
> **替换 QEMU 进程**（静默重启该 VM）；故产品侧 create/rollback 改为**显式要求关机态**（运行中 409）。
> 守卫落点初版错放在 HTTP handler，真机实测 **CLI 仍能绕过**（独立执行路径），已移至
> 两侧共同依赖的 `snapshotController`——AGENTS.md「命令树与执行器必须同源」的又一实例。
> 证据见 `docs/evidence/v1-closeout-round7.txt`。

> **2026-09-14 V1 收尾（决策 #69）已关闭 5 条降级**（15 → 10）：FR-API-002（openapi.json 端点）、
> FR-SYS-004（syslog 远程转发）、FR-OPS-022（告警转发）、FR-OPS-030（日志级别联动）、
> FR-NET-013（IPv6 真机，A-3 完成且**顺带发现并修复 v6 路由运行态不可见的缺陷**）。
> 证据见 `docs/evidence/v1-closeout.txt`。
>
> **2026-09-14 第六轮（决策 #74）**：**列表端点分页补齐**——`api.paginate` 应用于 14 个列表端点、
> 契约同步声明 `limit`/`offset`（FR-API-007 由降级转**通过**）。
> 证据见 `docs/evidence/v1-closeout-round6.txt`。

> **2026-09-14 第五轮（决策 #73）**：**FR-NET-003 物理业务口链路告警**落地（此前唯一链路告警是 vhost-user 的
> `VNF_PORT_DOWN`）——新增 `INTERFACE_LINK_DOWN`（warning），接入恢复收敛与 15s 巡检；真机验证
> 「置 down → 出警 → 恢复 → 消警」全链路。另修 `-allow-plaintext` 被已存在自签证书压过的缺陷。
> 证据见 `docs/evidence/v1-closeout-round5.txt`。

> **2026-09-14 第四轮（决策 #72）**：实机部署闭环——**FR-NET-001 网卡 DPDK 驱动接管**落地并真机跑通
> 完整周期（unbind→内核驱动回归并重现网卡→bind→vfio-pci→VPP 正常）；**FR-SEC-001 管理面仅监听管理网卡**
> （通配监听按管理口地址收敛）；**FR-SEC-004 默认 HTTPS**（自动自签 + CLI 证书固定）。三条均由**降级转通过**。
> 真机实测两点平台行为已处理（rescan 不足以重新探测原生驱动 → `to-driver`；接管后须按 PCI 回读驱动）。
> 证据见 `docs/evidence/v1-closeout-round4.txt`。

> **2026-09-14 第三轮（决策 #71）**：用户提问「CLI 能否把网卡绑到 VPP / 能否指定管理网卡」引出三项核查——
> ① 新增**管理网卡身份** `system.management.interface` + 数据面隔离 commit 强制（FR-NET-002 由降级转**通过**）；
> ② 修复 `set system management ip address|gateway` **未映射**（CLI 上根本设不了，FR-SYS-001 长期未真正可用）；
> ③ 修复 Flatten/Diff 对**嵌套对象**误用身份字段语义（影响 `port-mirroring.source.interface` 等既有缺陷）。
> 另清理两处「声明了但无实现」：`max-sessions` 限流（FR-SYS-006 转**通过**）、URL 拉取强制 sha256（FR-SEC-004 收窄）。
> **如实新登记**：**FR-NET-001 驱动接管产品侧完全未实现**（全仓无 `vfio-pci`/`driver_override` 写入）→ 由通过改判**降级**。
> 证据见 `docs/evidence/v1-closeout-round3.txt`。

> **2026-09-14 第二轮复核（决策 #70）**：对剩余项**逐条回代码证伪**，修正 6 处断言不准，
> 并修复 1 处**安全缺陷**与 2 处功能缺陷（详见 §5.2 与决策 #70）：
> ① 口令哈希经 `GET /audit-logs` 泄露给 read-only 账号（真机复现，**已修复 + 全路径守护测试**）；
> ② 声明式 `interfaces[].sriov.vf_count` 静默无操作（**已修复**）；
> ③ LLDP 标识忽略 subtype 导致 MAC 型对端乱码（**已修复**）；
> ④ `/audit-logs` 的 `offset` 契约漂移（**已实现**）。
> 措辞修正：FR-SEC-001（出厂默认全网卡监听）、FR-SEC-004（默认明文 HTTP）、FR-SEC-006（证据指针误标）。
> 真机证据见 `docs/evidence/v1-closeout-round2.txt`。

> 「降级」3 条：FR-NET-022（容器 memif 通流）、FR-SEC-006（SSH 强化）、FR-SEC-007（脱敏守护）。
> 另见 §5.2 的非 FR 条目（`show vpp runtime`、NFR-005/006）与已定论的 ACL 命中计数（降 V2）。
> `format-data`/`password change` 按决策 #65 延期 V2（见 §5.3）。

> **签字前必须处理的 2 项**（详见 §4）：§10 吞吐/容量口径（A-2）、「降级」项的书面确认（§5.2，勿默认通过）。
> A-3（IPv6 真机）已作为 FR-NET-013 计入「降级」，补验后可由降级转为通过。

> **2026-09-18 收口（决策 #93~#98）**：本轮把「验收口径与实际不符」的地方逐条对齐，并按验收方指示执行：
> - **NFR-005**：原文「错误消息**英文**为主」与产品（全程中文、且 #86 专门守护）不符 → 措辞按**决策 #95** 改为「**中文为主**」 → **转通过**。
> - **NFR-006**：审计记录随事件落库 `time_synced` 三态（真机双向实测：已同步无标记 / 未同步带 `[时钟未同步]` / 老记录未知）→ **转通过**（决策 #97）。
> - **FR-SEC-006**：拆两半——SSH 禁 root 口令登录**已实现并真机验证**（决策 #96）→ **转通过**；底座最小化属镜像/安装器范畴、**不在 deb 交付范围**，移入 §5.3 登记。
> - **§10 吞吐/容量**：从「未验」改为「**设计目标 + 测量方法**」逐行列明（决策 #93 同批）。
> - **`ping` 语义收紧**：未通即失败（0 发包 / 无应答都报错，#89/#93）；`delete-vfs` 明说编号不参与定位（#94）。
> - **本次确认的正式签字仍由验收方在交付说明中落笔**——本节记录的是**处置口径已确认**（2026-09-18，按推荐意见执行）。

---

## 1. CLI 需求（§3）

### 1.1 交互行为

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-CLI-001 | `?` 显示上下文命令 | `internal/cli/rawmode.go`（`helpLine`）、`internal/schema/node.go:342` | `schema/node_test.go:TestCandidatesKeywords`、`cli/session_test.go:TestRootForContext`、**`cli/rawmode_test.go:TestQuestionMarkListsCandidatesImmediately`** | 通过（原判「通过」为**误判**，2026-09-15 修复后重判，见下方说明） |
| FR-CLI-002 | 任意位置 `?` 显示补全项（含参数值） | `internal/cli/rawmode.go`（`helpLine`）、`internal/cli/session.go:86`、`internal/api/cli_support.go:40` | `schema/node_test.go:TestCandidatesDynamicAndEnum`、`cli/session_test.go:TestDynamicCandidatesStub`、**`cli/rawmode_test.go:TestQuestionMarkListsCandidatesImmediately`** | 通过（同上，修复前 `?` 须回车才显示，属**未实现**） |
| FR-CLI-003 | Tab 补全（唯一自动/多匹配列出） | `internal/cli/rawmode.go`（`completeLine`）、`internal/cli/session.go`（`Complete`） | `cli/session_test.go:TestCompleteLineUniqueAndCommonPrefix`、**`cli/rawmode_test.go:TestTabListsCandidatesOnMultiMatch`** | 通过（原判「通过」为**误判**：「列出」此前从未实现，见下方说明） |
| FR-CLI-004 | 无歧义缩写 | `internal/schema/node.go:201`、`internal/api/cliexec.go:218` | `schema/node_test.go:TestMatchAbbreviation`、`api/cliexec_test.go:TestCLIAbbreviationExecutable` | 通过 |
| FR-CLI-005 | 管道过滤 `match/except/count/last/display` | `internal/api/pipes.go:22,84` | `api/pipes_test.go`（8 个用例） | 通过 |
| FR-CLI-006 | 历史/Ctrl-R/空闲超时自动登出 | `internal/cli/history.go:43,72,106` | `cli/history_test.go:TestHistoryUpDownNavigation`、`TestCtrlRSearch`、`cli/repl_test.go:TestREPLIdleTimeoutLogsOut` | 通过 |
| FR-CLI-007 | 配置模式按 schema 补全 set/delete | `internal/schema/tree_config.go:9`、`internal/cli/session.go:130` | `schema/node_test.go:TestCandidatesConfigMode` | 通过 |

> 说明：FR-CLI-001/002/007 的行编辑器级 `?` 行为主要经 `Schema.Candidates`/`Session.Candidates`
> 单测覆盖（含 REPL 后缀识别），**无 rawmode 内联 `?` 按键的专门单测**——覆盖充分但为间接证据。
>
> **⚠️ 更正（2026-09-15，决策 #81）**：上一段的结论「覆盖充分」是**错的**，三条已判 FR 中有三条
> 实为**误判**（FR-CLI-001/002/003）。真机交互报告发现：`?` **必须回车才显示候选**（与 FR-CLI-002
> 「输入过程中任意位置输入 `?`，显示该位置…」不符）、Tab **多匹配只响铃、从不列出候选**
> （与 FR-CLI-003「多匹配列出候选」不符）、且 raw 模式下**候选列表与提示符阶梯错位**。
> **误判根因**：① 原证据指针（`TestCandidatesKeywords`/`TestDynamicCandidatesStub`/`TestCompleteLineUniqueAndCommonPrefix`）
> 只覆盖 `Candidates` 计算与公共前缀，**从未覆盖「列出」与「按键即时」**；② M2 的 `?` 演示记录是用
> `printf … | nfvis-cli`（**非 TTY**）产生的——`term.MakeRaw` 失败退化 `readPlain`，既显不出错位、
> 也显不出「需回车」。**教训**：交互行为必须在 **pty** 下验证（`script -qec`），管道演示不能充当交互证据。
> 修复与新增测试见决策 #81；证据 `docs/evidence/v1-closeout-round12.txt`。
> 判定总数不变（仍为通过），但「通过 100」中这三条的依据已由间接证据改为直接证据。

### 1.2 配置事务模型

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-CFG-001 | set/delete 作用于 candidate | `internal/config/engine.go:201`、`api/cliexec.go:483` | `api/cliexec_test.go:TestCLIConfigTransaction`、`config/engine_test.go:TestEngineLifecycle` | 通过 |
| FR-CFG-002 | commit 校验（schema+语义）全通过才下发 | `internal/config/engine.go:350,376` | `config/engine_test.go:TestEngineValidationAggregatesErrors`、`TestEngineApplyFailure` | 通过 |
| FR-CFG-003 | `commit confirmed` 超时自动回滚+告警 | `internal/config/engine.go:444,641` | `config/engine_test.go:TestEngineCommitConfirmedTimeout`、`TestEngineConfirmedPersistenceAcrossRestart` | 通过 |
| FR-CFG-004 | 等待期确认；超时回滚入审计 | `internal/config/engine.go:356,476,662` | `config/engine_test.go:TestEngineCommitConfirmedConfirm`、`TestEngineNewCommitConfirmsPending` | 通过 |
| FR-CFG-005 | `rollback <n>`，保留 50 个历史 | `internal/config/engine.go:495`、`store_sqlite.go:19` | `config/engine_test.go:TestEngineRollbackAndCompare`、`store_sqlite_test.go:TestStorePruneRevisions` | 通过 |
| FR-CFG-006 | JunOS 风格 diff | `internal/api/cliexec.go:321,517`、`model/diff.go:138` | `api/cliexec_test.go:TestCLIConfirmedRollbackAndCompare`、`model/diff_test.go` | 通过 |
| FR-CFG-007 | 层级子树 delete；annotate 注释 | `internal/api/cli_annotate.go:19,119` | `api/cliexec_test.go:TestCLIAnnotateAndRender`、`TestCLISetNestedDeleteAndValidation` | 通过 |
| FR-CFG-008 | `load override/merge`、`save`（JSON） | `internal/api/cli_annotate.go:71,99`、`config/engine.go:263,278` | `api/cliexec_test.go:TestCLILoadSaveRoundTrip`、`model/merge_test.go` | 通过 |
| FR-CFG-009 | 并发写保护 + 会话列表 | `internal/config/engine.go:201,313` | `config/engine_test.go:TestEngineLockConflict`、`TestEngineLockIdleTimeout` | 通过 |
| FR-CFG-010 | 全程审计（user/time/diff/结果） | `internal/config/engine.go:379`、`store_sqlite.go:312` | `api/aliases_audit_test.go:TestAuditLogsEndpoint`、`store_sqlite_test.go:TestStoreAudit` | 通过 |
| FR-CFG-011 | 11 条 commit 语义校验规则 | `internal/model/validate.go`（①-⑧）、`resources.go:108`（⑨⑪）、`engine.go:396`（⑤）、`engine.go:422`（⑩） | `model/validate_test.go:TestValidateFRConfig011Rules`、`model/resources_test.go`（5 用例）、`config/engine_test.go:TestEngineImageTypeRule`、`TestEngineNumaWarning` | 通过 |
| FR-CFG-012 | 管理口自锁保护（SSH 须 commit confirmed） | `internal/config/engine.go:392`（`ErrConfirmRequired`） | `config/engine_test.go:TestEngineMgmtPortSelfLock`、`TestEngineCommitWarnings` | 通过 |

**FR-CFG-011 规则逐条对照**（11/11 齐备）：

| # | 规则 | 落点 | # | 规则 | 落点 |
|---|---|---|---|---|---|
| ① | vhost-user 须大页 | `validate.go:788` | ⑦ | hugepage-preference 一致 | `validate.go:714` |
| ② | IP 冲突/网段重叠 | `validate.go:878` | ⑧ | dpdk dev 须物理口 | `validate.go:720` |
| ③ | VNF MAC 不重复 | `validate.go:810` | ⑨ | 配额缺口明细 | `resources.go:130` |
| ④ | 端口 VLAN 一致 | `validate.go:391` | ⑩ | NUMA 警告 | `engine.go:422` |
| ⑤ | 镜像类型匹配 | `engine.go:714` | ⑪ | hugepage-size 池容量 | `resources.go:121` |
| ⑥ | vpp 核 ⊆ 隔离池 | `validate.go:700` | | | |

---

## 2. 网络功能需求（§4）

### 2.1 物理网卡接入

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-NET-001 | 业务网卡 DPDK 接管 + `show interfaces physical` | **`network/dpdkbind.go`（`driver_override`+`bind`/`unbind`，决策 #72①）**、`network/startup.go:205,228`、`api/cli_iface_vpp_show.go:62` | **`network/dpdkbind_test.go`（6 用例：绑定序/幂等/驱动缺失/PCI 定位/解绑 to-driver/未自动探测报错）**、`api/dpdk_bind_test.go`（确认语义、503、错误透传、命令树覆盖）；**真机全周期** `unbind-dpdk to-driver vmxnet3` → 网卡回到内核并重新出现 → `bind-dpdk` 回到 vfio-pci → VPP 正常起 | 通过（决策 #72①；真机实测两点平台行为已处理，见证据） |
| FR-NET-002 | 管理网卡不交 VPP，管理/业务隔离 | **`model.MgmtConfig.Interface`（新增，决策 #71①）+ `validate.go:checkManagementIsolation`（commit 强制）**、`api/cli_iface_vpp_show.go`（显示真实网卡名） | **`model/mgmt_isolation_test.go`（7 类数据面引用逐项拒绝）**、**`api/cli_management_test.go`** | 通过（决策 #71①：管理网卡身份 + 数据面引用强制校验；**驱动层**「该网卡确未被绑到 vfio-pci」仍属安装期，见 FR-NET-001） |
| FR-NET-003 | 启用/禁用/MTU/描述 + **链路状态变化告警** | `api/cli_iface_sriov_ops.go:19`、`network/services.go:167`；**`network/interface_alarm.go:CheckInterfaceLinks`（决策 #73）** | `services_test.go:TestQosApplyBindUnbind`、**`interface_alarm_test.go`（5 用例：up 不告警 / link down 告警并消警 / admin 未启用 / 显式 disable 不告警 / 缺失不重复告警）**；**真机**：`set interface state ens192 down` → 15s 内出 warning，恢复后 18s 内消警且 `state=all` 见 `resolved` 与时间戳 | 通过（决策 #73） |
| FR-NET-004 | SR-IOV VF 创建/回收与直通 | `network/sriov.go:35`（sysfs）、`api/vpp.go:100`、`compute/domain_xml.go:216`；**声明式路径 `provider.go:ApplyInterface`（决策 #70 新增）** | `network/sriov_test.go:TestSRIOVSetVFCount`、`TestApplyInterfaceAppliesDeclarativeVFCount`、`TestApplyInterfaceVFCountErrorNotSilent`、`api/vpp_test.go:TestPutSRIOVEndpoint` | **未验**（VF 硬件不在环境中）。注：**声明式 `vf_count` 此前静默无操作，已在决策 #70 修复**；`delete-vfs` 忽略 `vf <n>` 参数的缺陷仍待修 |

### 2.2 虚拟交换机（L2/L3）

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-NET-010 | L2 BD + MAC 学习，物理/vhost/memif/子接口成员 | `network/l2.go:140,374` | `l2_test.go:TestL2ApplyBridgeDomainAndDetach`、`test/integration/e2e_test.go:TestE2EFlow`（真机） | 通过 |
| FR-NET-011 | access/trunk VLAN | `network/l2.go:336,347` | `l2_test.go:TestL2AccessVlanCreatesSubif`、`TestL2TrunkVlanSubifs` | 通过 |
| FR-NET-012 | L2 cross-connect（限 2 口） | `network/l2.go:311` | `l2_test.go:TestL2CrossConnect`、`TestL2CrossConnectDetach` | 通过 |
| FR-NET-013 | L3 VRF + 静态路由（v4/v6、默认路由） | `network/l3.go:101,141,389`（+`Routes` 合并 v4/v6，决策 #69④） | `l3_test.go:TestL3ApplyVRF`、`TestL3RoutesIncludeIPv6`；真机 v4 `TestE2EFlow`；**v6 `test/integration/a3_ipv6_test.go`**（地址/静态路由/默认路由 + ICMPv6 往返） | 通过（A-3 已完成，决策 #69④；跨设备 v6 通流受环境限制，见 §5.1） |
| FR-NET-014 | L2 BVI 三层网关 | `network/l3.go:268`、`DeleteGateway:347` | `l3_test.go:TestL3Gateway`、`TestL3GatewayReuseExistingBVI`；真机 `TestE2EFlow`/M4-11 主链路 | 通过 |
| FR-NET-015 | 按名引用 + 删除前校验引用 | `api/resources.go:242`、`model/validate.go:809` | `api/resources_test.go:TestVirtualSwitchesEndpoint`（被引用→409） | 通过 |
| FR-NET-016 | 计数/状态经 show 与 API 实时呈现 | `network/stats_linux.go`、`api/vpp.go:184` | `api/vpp_test.go:TestMacTableEndpoint`、`TestInterfaceStatistics`；真机 `m511-e2e-bench.txt` | 通过 |
| FR-NET-017 | bond/LACP + 静态聚合 | `network/bond.go:63`、`model/validate.go:295` | `bond_lldp_test.go:TestBondStaticAndLacp`、`TestBondErrors`、`network/recovery_test.go:TestEnsureConsistentRebuildsStaleBond` | 通过 |
| FR-NET-018 | LLDP 全局/按口启停 + 邻居表 | `network/lldp.go:57,110`；**标识解码 `lldpIDBySubtype`（决策 #70 修复）** | `bond_lldp_test.go:TestLldpApplyAndDisable`、`TestLldpIDBySubtype`、`TestLldpIDMacNotRawString` | **未验**（环境无 LLDP 对端）。注：**标识按 subtype 解码的缺陷已修复**（旧实现把 MAC 型标识当字符串 → 乱码）；契约中无法填充的 `system_name` 已移除 |

### 2.3 高级网络功能（§4.3，无 FR 编号）

| 功能 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|
| ACL | `network/acl.go:86,166` | `acl_test.go`（5 用例）；**命中计数缺失**（见 §5） | 通过（计数降级） |
| NAT44 | `network/nat.go:92,253`（跨 VRF） | `nat_test.go`（6 用例）；真机 T0-1 D4 端到端（`d4-nat-cross-vrf.txt`） | 通过 |
| 端口镜像 SPAN | `network/services.go:64` | `services_test.go:TestQosApplyBindUnbind`；真机 T0-7（`t07-span-lldp.txt`，tcpdump 抓到镜像帧） | 通过 |
| QoS 限速 | `network/services.go:112` | `services_test.go:TestQosApplyBindUnbind` | 通过 |

### 2.4 VM / 容器接入

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-NET-020 | vhost-user（VM 默认，须大页） | `network/vhost_user.go:61`、`validate.go:790` | `vhost_user_test.go:TestVhostUserApplyCreatesNamesMacUp`；真机 `test/integration/m4_vnic_test.go` | 通过 |
| FR-NET-021 | SR-IOV VF 直通，占用 VF 时禁 PF 进 BD | `network/provider.go:122`、`validate.go:800,828` | `model/validate_test.go`（PF/BD 约束）、compute domain XML 单测 | **未验**：XML 组装与校验有单测；**无 VF 硬件，真机未验** |
| FR-NET-022 | memif（容器），socket 挂载进容器 | `network/memif.go:40`、`container/docker.go:320` | `test/integration/m4_container_test.go`（endpoint/socket/生命周期，真机） | **降级**：VPP 侧与容器生命周期真机通过；**容器侧通流未验**（T0-3，离线无自带 memif 的镜像，见 §5） |
| FR-NET-023 | vNIC 删除/迁移同步；断连置 down + 告警 | `network/provider.go:130`、`vhost_user.go:147`、`vnf_alarm.go:26` | `vhost_user_test.go:TestVhostUserLinkState`、`m4_vnic_test.go`（真机 link down + 告警） | 通过 |
| FR-NET-024 | tap 接口接入 | — | — | **移 V2**（规格书 §12、§4.4 明示） |

---

## 3. 计算、镜像、API、运维、安全需求（§5~§9）

### 3.1 资源池与 VM 生命周期

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-CMP-001 | 全局资源池（大页/隔离核，NUMA） | `model/model.go:273,361`、`api/resources_w5.go:29` | `api/resources_w5_test.go:TestResourcePoolsEndpoint`、`model/resources_test.go:TestLedgerDefaultMainPool` | 通过 |
| FR-CMP-002 | 分配 vCPU 绑核 + NUMA + 大页，缺口明细 | `model/resources.go:108,164`、`compute/domain_xml.go:190` | `resources_test.go`（5 用例含缺口明细）、`domain_xml_test.go:TestBuildDomainXML_NumaTune` | 通过 |
| FR-CMP-003 | 删除自动归还；`show resource-pools` | `model/resources.go:43`、`api/cli_compute_show.go:299` | `api/cli_compute_test.go:TestCLIShowContainersImagesPools` | 通过 |
| FR-CMP-004 | 池缩减校验存量占用 | `model/resources.go:75,108` | `resources_test.go:TestLedgerPoolShrinkBlockedByExistingVNF` | 通过 |
| FR-CMP-005 | 底座优化安装期预置 + CLI 可查 | `system/kernel.go:97,123`、`deploy/installer/nfvis-baseline.sh` | `api/cli_kernel_baseline_test.go:TestKernelBaselineApplyAndShow`、`system/kernel_test.go` | 通过 |
| FR-CMP-010 | VM 创建（镜像/vCPU/内存/vNIC/cloud-init） | `api/vm.go:67`、`compute/provider.go:86` | `provider_test.go:TestDefineVM_PreparesDiskSeedAndDefines`、`compute/lifecycle_integration_test.go`（真机） | 通过 |
| FR-CMP-011 | start/stop/restart + 状态实时查询 | `api/vm.go:140`、`compute/provider.go:148` | `provider_test.go:TestStartVM_IdempotentAndNotFound`、`TestStopVM_Graceful`、`m4_main_chain_test.go`（真机） | 通过 |
| FR-CMP-012 | 修改（关机态生效，热调整 V2） | `api/vm.go:88`（running→409） | `api/vm_test.go:TestVMLifecycleEndpoints`；真机 M4-11（vCPU 1→2） | 通过 |
| FR-CMP-013 | 级联删除 + 二次确认 | `api/vm.go:119`、`compute/provider.go:123` | `api/cli_compute_test.go:TestCLIRequestVMDeleteRequiresConfirm`、`provider_test.go:TestDeleteVM_CascadeActiveDomain` | 通过 |
| FR-CMP-014 | 串口 console + `Ctrl-]` 退出 | `api/vm_console.go:82,119`、`cli/console.go:22` | `api/vm_console_test.go:TestConsoleTicketAndWebSocketBridge`、`cli/console_test.go`；真机 `m4_console_test.go` | **未验**（真人 `Ctrl-]` 需真实 TTY，T0-4）。注：另有未记缺陷——**WS/串口断开时空闲阻塞在 `Read`，需按键才退出**（决策 #70⑤，待 V2 修） |
| FR-CMP-015 | 快照创建/列出/回滚/删除 | `api/vm_snapshot.go:48,65,93,114`、`cmd/nfvisd/main.go`（`snapshotController.requirePoweredOff`） | `api/vm_snapshot_test.go:TestSnapshotEndpoints`、`TestSnapshotRequiresPoweredOff`；真机 `m4_snapshot_test.go`、`m5_snapshot_content_test.go:TestSnapshotContentLevelRollbackRealLibvirt`（**内容级**：guest 内写 LATER→CHANGED→回滚→回读=LATER） | **通过**（决策 #75：内容级回滚已实证；运行中 create/rollback 改为显式拒绝，依据见 `TestSnapshotRevertOnRunningVMRealLibvirt`） |
| FR-CMP-016 | cloud-init（NoCloud seed ISO） | `compute/cloudinit.go:23,96`、`domain_xml.go:282` | `domain_xml_test.go:TestBuildDomainXML_DataDisksAndSeedISO`；真机 `m4_console_test.go`（串口回读标记） | 通过 |
| FR-CMP-017 | VM 异常退出 critical 告警 | `compute/domain_xml.go:165`、`provider.go:419` | `provider_test.go:TestCheckVMAlarms`；真机 `test/integration/m4_alarms_test.go` | 通过 |
| FR-CMP-018 | 附加 virtio 数据盘 | `compute/domain_xml.go:266,299`、`provider.go:297` | `domain_xml_test.go:TestBuildDomainXML_DataDisksAndSeedISO`、真机 `m4_snapshot_test.go` | 通过 |
| FR-CMP-019 | 内存 backing hugepage/normal | `compute/domain_xml.go:376,396`、`model/resources.go:196` | `resources_test.go:TestLedgerNormalBackingSkipsHugepagePool`、`domain_xml_test.go:TestHugepageSpec` | 通过 |
| FR-CMP-020 | 容器创建（Docker，CPU/内存/memif/env） | `container/docker.go:110,294` | `docker_test.go:TestBuildCreateSpec`；真机 `m4_container_test.go` | 通过 |
| FR-CMP-021 | 容器 start/stop/restart/删除 + 日志 | `api/container.go:98,174`、`docker.go:135` | `docker_test.go:TestApplyContainerLifecycle`、`api/cli_compute_test.go:TestCLIRequestContainerLifecycleAndLog` | 通过 |
| FR-CMP-022 | 容器异常退出告警；重启策略可配 | `docker.go:253,299` | `docker_test.go:TestCheckContainerAlarms`；真机 `m51-events-metrics.txt`（docker kill→critical） | 通过 |
| FR-CMP-030 | 本地镜像仓库（容量/使用率可查） | `images/images.go:43`、`api/cli_system_show.go:28` | `images/images_test.go:TestImportIncoming`、`TestRefCountAndList` | 通过 |
| FR-CMP-031 | 上传（incoming）+ URL 拉取（进度/sha256/断点续传） | `images/images.go:233,265`、`download.go:78-184` | `images_test.go:TestDownloadWithSHA256AndFailure`、`TestDownloadResumeFromPartial`；真机 T0-6（真实 cloud image 187MB + sha256 独立复核） | 通过 |
| FR-CMP-032 | 镜像元数据（名/类型/大小/sha256/格式/时间） | `images/images.go:47,306` | `images_test.go:TestImportIncoming`、`api/images_test.go:TestImagesEndpoints` | 通过 |
| FR-CMP-033 | 删除前校验引用；容器镜像经 Docker 删除 | `images/images.go:196,290`、`api/images.go:129` | `images_test.go:TestDeleteReferenceAndDocker`、`api/images_test.go:TestImagesDeleteReferenced`（409） | 通过 |
| FR-CMP-034 | 外部镜像仓库对接 | — | — | **移 V2**（规格书 §12） |

### 3.2 API（§6）

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-API-001 | HTTPS + Bearer Token（TTL 可配、可吊销） | `api/server.go:34,353`、`aaa/service.go:242,364` | `api/server_test.go:TestLoginFlow`、`aaa/service_test.go:TestTokenLifecycle`、`TestTokenTTLFromConfig` | 通过 |
| FR-API-002 | `GET /api/v1/openapi.json` 随产品发布 | `internal/api/openapi.go`（`go:embed` 嵌入 `openapi.json`）、`server.go` 注册；契约文件仍随 deb 打包 `/usr/share/doc/nfvis/` | `api/openapi_test.go:TestOpenAPISpecEndpoint`（无鉴权 + JSON 可解析 + 关键路径）；守护 `contrib/scripts/check_openapi_json_sync.sh`（接入 `make docscheck`）；真机 200/140509B/88 路径 | 通过（决策 #69①） |
| FR-API-003 | 资源模型与 CLI 一一对应 | `api/server.go:139-295`（路由↔CLI 语句映射） | `api/routes_contract_test.go:TestRoutesRegisteredInContract`、`cli_mapping_test.go`、`cli_contract_coverage_test.go` | 通过 |
| FR-API-004 | 事务语义（candidate + commit / Auto-Commit 头） | `api/configuration.go:72-107` | `api/configuration_test.go`、`cliexec_test.go:TestCLIConfigTransaction` | 通过 |
| FR-API-005 | 统一错误 JSON（code/message/detail[]） | `api/handlers_auth.go:12-30`、`configuration.go:45` | `api/vm_test.go:TestVMActionErrors`、`routes_contract_test.go` | 通过 |
| FR-API-006 | 事件订阅 SSE/WebSocket | `api/events.go:31,85`、`events/bus.go:51,89` | `api/events_test.go:TestEventsSSE`、`events/bus_test.go`（3 用例）；真机 `m51-events-metrics.txt` | 通过 |
| FR-API-007 | `/api/v1/` 版本化 + 分页 limit/offset | `api/server.go:25`；`cli_support.go` + `config/store_sqlite.go:ListAudit(limit,offset)`（决策 #70④）；**`api/pagination.go` + 14 个列表端点（决策 #74）** | **`api/pagination_test.go`（纯函数语义 11 组 + `/images` HTTP 层分页）**、`api/redact_test.go:TestAuditLogsOffsetEffective`、`config/store_sqlite_test.go:TestStoreListAuditOffset`；契约 14 条路径声明 `limit`/`offset` | 通过（决策 #70④ + #74） |

### 3.3 运维（§8）

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-OPS-001 | `software add` 校验+升级+失败回退 | `system/software.go:76,235,289` | `system/software_test.go:TestSoftwareAddLocalDeb`、`TestSoftwareAddRejectsNonNfvisPackage`；真机 `m57-deb-upgrade.txt` | 通过 |
| FR-OPS-002 | `software rollback` | `system/software.go:133` | `software_test.go:TestSoftwareRollbackPicksHighestNonCurrent`；真机 `m57` | 通过 |
| FR-OPS-003 | 升级不触碰配置；reboot/shutdown 需确认 | `system/software.go:257,263`、`api/system_sw.go:86` | `software_test.go:TestSoftwarePowerAndNTP`、`api/capture_test.go:TestSoftwareEndpoints` | 通过 |
| FR-OPS-004 | `backup` 导出 JSON 归档可下载 | `system/backup.go:93`、`api/system_ops.go:64` | `system/backup_test.go:TestBackupAndList`；真机 `m56-backup-restore-zeroize.txt` | 通过 |
| FR-OPS-005 | `restore` 导入并作为 candidate 提交 | `system/backup.go:162`、`api/system_ops.go:80` | `backup_test.go:TestRestoreAppliesConfigAndReturnsImageManifest`；真机 `m56` | 通过 |
| FR-OPS-006 | 镜像本体不进备份；存储监控告警 | `system/backup.go:103`、`system/hardware.go:95` | `backup_test.go:TestBackupAndList`、`hardware_test.go:TestEvaluateThresholds` | 通过 |
| FR-OPS-007 | `zeroize` 双重确认 + 重启进初始化 | `system/backup.go:195`、`api/system_ops.go:128` | `backup_test.go:TestZeroizeClearsConfigAndImages`；真机 `m56`（zeroize→重启→重新引导 admin） | 通过 |
| FR-OPS-010 | 启动收敛（补建/修正，不阻塞） | `network/recovery.go`、`compute/provider.go:262`、`container/docker.go:232`、`cmd/nfvisd/main.go:322` | `network/recovery_test.go`、`compute/provider_test.go:TestEnsureConsistent_DefinesAllAndCollectsErrors`；真机 `m4_recovery_test.go` | 通过 |
| FR-OPS-011 | VPP 重启自动重放 + vhost-user 重连 | `cmd/nfvisd/main.go:432`、`compute/domain_xml.go:328` | `network/recovery_test.go`、`m4_vnic_test.go`（真机） | 通过 |
| FR-OPS-012 | 整机重启后 systemd 拉起并恢复 | `deploy/nfvis.service`、`cmd/nfvisd/main.go:432` | 真机 `m4_recovery_test.go`、`m5` systemd 证据 | 通过 |
| FR-OPS-013 | systemd `Restart=always` + 崩溃退避 + watchdog | `deploy/nfvis.service:10-18`、`internal/systemd/notify_linux.go`、`cmd/nfvisd/main.go:486` | 真机 `m5`（`Type=notify`、`WatchdogUSec=30s`、日志「看门狗已启用」）；**无该包单测**（平台文件，build tag 隔离） | 通过（证据为真机） |
| FR-OPS-020 | 事件模型（severity/source/code/message/time/clear） | `network/alarm.go:45,88,111` | `network/alarm_test.go`、`api/alarms_test.go` | 通过 |
| FR-OPS-021 | 触发源（link/VNF 退出/资源耗尽/阈值/confirmed 超时/版本） | `vnf_alarm.go`、`provider.go:419`、`docker.go:253`、`hardware.go:95`、`config/engine.go:28` | 各 `*_test.go`；真机 `m51`/`m55`/`m4_alarms_test.go` | 通过（**物理口 link 告警缺失**，见 FR-NET-003） |
| FR-OPS-022 | `show alarms` + API/SSE + **可转发 syslog** | `api/alarms.go:32,46`、`api/events.go:31`、`cmd/nfvisd/main.go`（notifier 内转发告警） | `api/alarms_test.go`、`events_test.go`；真机捕获 `<132>` `[HARDWARE_THRESHOLD] …` 与 `/alarms` 同源 | 通过（决策 #69②） |
| FR-OPS-030 | 分级日志入 journald，**级别可配** | `cmd/nfvisd/main.go`（`slog.LevelVar` 由 committed 配置驱动，启动 + 每次 commit 重载） | `system/syslog_test.go:TestSeverityOfSlog`；真机 `level=DEBUG` 生效（debug 记录产生并转发） | 通过（决策 #69③） |
| FR-OPS-031 | 审计独立通道（登录/配置/生命周期/API） | `config/store_sqlite.go:87,313`、`api/vm.go:188`、`container.go:160` | `api/cli_compute_test.go:TestCLIRequestVMLifecycleAudits`、`store_sqlite_test.go:TestStoreAudit` | 通过 |
| FR-OPS-032 | console 访问入审计 | `api/vm_console.go:139`、`api/cli_compute_oper.go:223` | `api/vm_console_test.go`、`cli_compute_test.go:TestCLIRequestVMConsoleIssuesTicket` | 通过 |
| FR-OPS-040 | tech-support 一键打包/列出/下载 | `system/techsupport.go:50`、`api/diag_ops.go:34,58` | `system/diag_test.go:TestTechSupportArchiveSections`；真机 `m54-tech-support-coredump.txt` | 通过 |
| FR-OPS-041 | core dump 收集/容量滚动/导出 | `system/coredump.go:89,113,125,147`、`api/diag_ops.go:73` | `diag_test.go:TestCoreDumpsListDeletePrune`；真机 `m54`（真实 SEGV 转储 5.1 MiB） | 通过 |
| FR-OPS-042 | 数据面抓包导出 pcap | `network/capture.go:92,136,182`、`api/capture.go:51` | `network/capture_test.go`、`api/capture_test.go:TestCaptureEndpoints`；真机 `m53-capture.txt`（`file` 识别 pcap 2.4，tcpdump 读出 ARP） | 通过 |

### 3.4 系统级配置（§7）

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-SYS-001 | `[edit system]` hostname/时区/NTP/DNS/管理口 IP | `model/model.go`、`api/cli_aliases_system.go`（DNS 数组映射 #67；**管理口三条语句 #71 补齐**） | `api/resources_test.go:TestSystemEndpoint`、**`api/cli_management_test.go:TestCLIManagementStatements`**；真机 `m59-cli-closeout.txt` | 通过（**修正**：管理口 `ip address`/`gateway` 此前是未映射语句，CLI 上根本设不了，决策 #71② 已补） |
| FR-SYS-002 | 大页安装期预留 + 运行期调整（需 reboot 提示） | `system/kernel.go:123`、`deploy/installer/nfvis-baseline.sh` | `system/kernel_test.go:TestGenerateBaseline`、真机 `kernel-baseline-*.txt`（三方对照） | 通过 |
| FR-SYS-003 | 绑核/隔离核（isolcpus + 动态划分） | `model/resources.go:164`、`system/kernel.go:164` | `model/resources_test.go:TestLedgerAllocateHappyPath`、`api/cli_kernel_baseline_test.go` | 通过 |
| FR-SYS-004 | syslog：本地 journald + 远程目标可配 | `system/syslog.go`（RFC 5424 转发）、`model/syslog.go`（facility/severity 词汇）、`api/cli_aliases_system.go`（facility/severity 映射补齐） | `system/syslog_test.go`（12 用例）、`api/cli_syslog_alias_test.go`、`model` 校验测试；真机 UDP 捕获 `<134>`/`<135>` 报文、severity 阈值过滤 | 通过（决策 #69②） |
| FR-SYS-005 | Prometheus 指标端点（系统/VPP/VNF/告警） | `internal/metrics/`、`api/metrics.go` | `metrics/metrics_test.go`；真机 `m51-events-metrics.txt`、`m511-e2e-bench.txt` | 通过（buffer 池已由决策 #68 修复，见 §5.2） |
| FR-SYS-006 | API 服务：HTTPS 端口/token TTL/并发限制可配 | `api/server.go`（`listener` 按 committed `max_sessions` 包 `netutil.LimitListener`，决策 #71④）、`aaa/service.go:364` | `api/max_sessions_test.go`（配置透传 / 上限生效 / 缺省不限）、`api/server_test.go:TestLoginFlow`、`aaa/service_test.go:TestTokenTTLFromConfig`、`model` 校验（不得为负） | 通过（决策 #71④：`max-sessions` 由此前「声明了但无实现」改为真正生效） |
| FR-SYS-007 | `show version` 汇报 nfvis/Ubuntu/VPP/DPDK/libvirt/qemu/docker | `api/cli_*`、`system/techsupport.go`（version.json） | `api/cli_system_show_test.go`；真机 `m511-e2e-bench.txt`（CLI show 1ms） | 通过 |
| FR-SYS-008 | `[edit vpp]` → startup.conf 生成，纳入事务 | `network/startup.go:205,228`、`api/vpp.go` | `network/startup_test.go:TestGenerateStartupFull`、`TestGenerateStartupOverrideFallback`、`api/vpp_test.go:TestVppConfigEndpoint` | 通过 |
| FR-SYS-009 | VPP 变更需重启（pending-restart 告警） | `network/startup.go`（段哈希判定）、`api/vpp.go` | `network/startup_test.go`、`api/vpp_test.go` | 通过 |
| FR-SYS-010 | VPP 核从隔离池保留分配，与大页偏好一致 | `model/validate.go:700,714`、`model/resources.go:164` | `model/validate_test.go:TestValidateFRSys010Rules`、`resources_test.go:TestLedgerCoreShortfallDetail` | 通过 |
| FR-SYS-011 | TLS 证书安装/重签 + SSH host key + 到期告警 | `system/tls.go`、`api/system_ops.go` | `system/tls_test.go:TestTLSRegenerateAndInfo`、`TestTLSInstallValidatesPair`；真机 `m58-tls-logs.txt`（热生效） | 通过 |
| FR-SYS-012 | 硬件健康监控 + 越限告警 + 阈值可配 | `system/hardware.go:95`、`api/hardware.go` | `system/hardware_test.go:TestEvaluateThresholds`；真机 `m55-hardware.txt`（降级路径 + 真实越限告警） | 通过（环境受限，见 §5.1） |
| FR-SYS-013 | 日志轮转与保留策略可配 | `system/tls.go`（`ApplyLogRetention`） | `system/tls_test.go:TestApplyLogRetentionWritesDropIn`；真机 `m58`（drop-in + journald 热重载） | 通过 |
| FR-SYS-014 | 内核启动基线托管（三方对照 + apply/rollback） | `system/kernel.go:97,123,164`、`api/cli_kernel_baseline.go` | `system/kernel_test.go:TestReadActualAndCompare`、`api/cli_kernel_baseline_test.go:TestKernelBaselineApplyAndShow`；真机 `kernel-baseline-*.txt` | 通过 |

### 3.5 安全（§9）

| FR | 需求摘要 | 代码落点 | 测试证据 | 判定 |
|---|---|---|---|---|
| FR-SEC-001 | 管理面仅监听管理网卡 | **`system/listen.go:ResolveListenAddr`（决策 #72②）**、`deploy/nfvis.service`（注释已更新） | **`system/listen_test.go`（9 组用例：通配收敛/地址非本机不收敛/显式不一致告警/IPv6）**；**真机** `-listen :18443` + 管理口地址 → 收敛绑定 `192.168.155.129:18443` 且可达 | 通过（决策 #72②：通配监听按 committed 管理口地址收敛；显式指定他址仍尊重但告警） |
| FR-SEC-002 | CLI/API 经 login class 授权；token 继承权限 | `aaa/service.go:242,260`、`api/server.go:353` | `aaa/service_test.go:TestAuthorizePresets`、`api/server_test.go:TestClassAuthorization` | 通过 |
| FR-SEC-003 | 口令策略/加盐哈希/失败锁定 | `aaa/service.go:43,101,325` | `aaa/service_test.go:TestHashAndVerifyPassword`、`TestCheckPasswordPolicy`、`TestLockout`、`api/server_test.go:TestLockoutHTTP423` | 通过 |
| FR-SEC-004 | TLS 1.2+；**镜像 URL 默认要求 sha256** | `api/server.go:323`（MinVersion）；`images/download.go`（URL 缺 sha256 即拒，决策 #71⑤）；**`system/tls.go:EnsureSelfSigned` + `nfvisd` 默认启用 HTTPS（决策 #72③）**、`pkg/cliclient` 证书固定 | `images/images_test.go:TestDownloadRequiresSHA256`、`system/tls_test.go:TestEnsureSelfSigned`、`pkg/cliclient/client_test.go:TestNewWithTLS`；**真机**：默认启动自动生成自签证书并 HTTPS 200、明文请求被拒、nfvis-cli 经固定证书直连成功 | 通过（决策 #71⑤ + #72③） |
| FR-SEC-005 | 会话空闲超时 + token TTL | `cli/history.go:126`、`cli/repl.go:20`、`aaa/service.go:364` | `cli/repl_test.go:TestREPLIdleTimeoutLogsOut`、`aaa/service_test.go:TestTokenTTLFromConfig` | 通过 |
| FR-SEC-006 | 底座最小化；SSH 禁 root 口令登录 | **SSH 半：已实现**——`deploy/installer/nfvis-ssh-harden.sh`（写 `/etc/ssh/sshd_config.d/99-nfvis.conf`：`PermitRootLogin prohibit-password`，只写 drop-in、`sshd -t` 校验、**不 reload**、无密钥登录路径则拒绝加固）+ `postinst` 第 7 段调用；**底座最小化半：不在 deb 交付范围**（属镜像/安装器） | `deploy/installer/nfvis-ssh-harden.sh` （真机：`--apply` 后 `sshd -T` 生效值 `prohibit-password`、不 reload、幂等、防锁死分支跳过、临时端口以密钥登录 root 成功） | **拆两半判定（已确认）**：SSH 半 **通过**（决策 #96，已落实现与真机证据）；底座最小化半 **不在 deb 交付范围**，移 §5.3 登记（需镜像/安装器） |
| FR-SEC-007 | 敏感信息脱敏 | **渲染层：`model/diff.go`（`IsSensitiveKey`+`maskSensitive`）；配置视图：`api/redact.go`（`redactConfigView`）**；`api/cliexec.go:1399`、`handlers_auth.go:67` | **`api/redact_test.go:TestSecretsNeverEchoed`（全路径守护：审计/候选/diff/login-users/CLI）+ `TestRedactConfigView`、`model/diff_test.go:TestDiffMasksPasswordHash`** | **降级（已修复主要缺陷）**：决策 #70① 前**口令哈希经 `GET /audit-logs` 泄露给 read-only 账号（真机复现）**，现已在渲染层与配置视图两处脱敏并加全路径守护；`save`/备份归档为有意例外（0600、恢复所需） |
| FR-SEC-008 | 口令自助修改（验证旧口令） | `aaa/service.go:299`、`api/resources_w5.go:308,320` | `aaa/service_test.go:TestChangePassword`、`api/resources_w5_test.go:TestChangePasswordEndpoint` | 通过 |

---

## 4. 非功能需求与性能（§10、§11）

| 项 | 目标 | 实测 | 判定 |
|---|---|---|---|
| NFR-001 | 核心模块单测覆盖率 ≥70% | config 78.6% / model 80.7% / schema 90.4% / network 73.0% / compute 89.8% / container 76.3% / images 75.8% | 通过 |
| NFR-002 | CLI/API 模型不依赖 VPP 实现细节 | `internal/archtest`（依赖方向守护）、Provider 接口隔离 | 通过 |
| NFR-003 | 支持矩阵随产品文档发布 | `docs/` + deb 包内文档 | 通过 |
| NFR-004 | V2 扩展不破坏 V1 数据模型 | 架构分层 + 附录 A 决策 | 通过 |
| NFR-005 | 错误消息**以中文为主**（措辞按决策 #95 修正，原为「英文为主」） | 全链路中文且已被守护（决策 #86/#87：文案不得含内部引用/剥离残渣）；第三方原文（如 VPP 的 `Failed: no egress interface`）按原样保留并在旁给中文解释（决策 #89） | **通过**（口径已按 #95 修正，待签字） |
| NFR-006 | 时间戳依赖 NTP，未同步带标记 | **已实现**（决策 #97）：审计记录随事件落库 `time_synced` 三态（true/false/**NULL=未知**），`show log audit` 在未同步时追加「[时钟未同步]」，API 省略该字段表示未知；探针 `internal/system.ClockSynced()`（`NTPSynchronized` → `chronyc`，判不出按未同步）；存储 v1→v2 纯增量迁移、老记录保留为未知 | **通过**（待签字）；**告警侧未加同款字段**（如实登记，建议单列） |
| §10 响应类 | 列表/详情 ≤500ms；CLI show ≤1s；commit ≤5s | `/interfaces` p95 1ms、`/metrics` 4ms、CLI show 1ms、commit p95 13ms | 通过 |
| §10 吞吐类 | 10GbE ≥14Mpps；vhost-user ≥8Gbps | **未测** | **未验**（A-2，见 §5） |
| §10 容量类 | ≤10 VM/≤20 容器；恢复 ≤30s/≤3min | **未测** | **未验**（A-2，见 §5） |

---

## 5. 降级与未验项汇总（需书面确认，勿默认通过）

### 5.1 环境受限（非缺陷，无法在产品内解决）

| 项 | 原因 | 建议 |
|---|---|---|
| SR-IOV（FR-NET-004/021） | nfvis-vm 为 vmxnet3，无 PF/VF | 目标硬件上补验 |
| LLDP 邻居（FR-NET-018） | 无 LLDP 对端 | 接交换机后补验；启用与命令已验证 |
| 硬件健康极端场景（FR-SYS-012） | VM 无 IPMI/温度传感器/smartctl | 降级路径已验（`m55-hardware.txt`） |
| 容器侧 memif 通流（FR-NET-022） | 离线无自带 memif 客户端的镜像 | 自建镜像后单立项（T0-3） |
| 真人 `Ctrl-]`（FR-CMP-014） | 需运行中 VM + 真实 TTY | 人工演示一次（T0-4） |

### 5.2 能力降级（状态截至 2026-09-14 V1 收尾后；**2026-09-18 复核改写见下**）

> **2026-09-18**：本节表格中 9 行的「未实现 / V2 补」已过期（对应决策关闭后未更新，与 §0 及 §1~§4 主表矛盾），已按源码逐条复核改写；剩余 2 条降级项（FR-NET-022、FR-SEC-007）的**处置口径已确认**，正式签字仍由验收方在交付说明中落笔。

> **第四轮已关闭（决策 #72）**：FR-NET-001（驱动接管）、FR-SEC-001（管理面监听收敛）、FR-SEC-004（默认 HTTPS）。
>
> **第一轮已关闭（决策 #69）**：远程 syslog 转发（FR-SYS-004）、告警转发 syslog（FR-OPS-022）、
> 日志级别联动（FR-OPS-030）、`GET /api/v1/openapi.json`（FR-API-002）、IPv6 真机（FR-NET-013）。
> 详情与真机输出见 `docs/evidence/v1-closeout.txt`。
>
> **第二轮修复但判定不变（决策 #70）**：FR-SEC-007（哈希泄露已修 + 全路径守护）、
> FR-NET-004/021（声明式 vf-count 已落地）、FR-NET-018（LLDP subtype 已修）、
> FR-API-007（audit offset 已实现）——**这几项的功能缺口已关闭，但因其主线仍受环境或范围限制**
> （无 VF 硬件 / 无 LLDP 对端 / 其余列表端点仍无分页 / 备份与 save 为有意例外），判定维持不变，
> 措辞已按复核结果修正。
>
> **待决**：console 空闲断开需按键才退出（FR-CMP-014，代码可改、真机验证需运行中 VM + TTY）。
> **已处置**：SSH 禁 root 口令登录**已实现**（决策 #96，见主表；底座最小化半不在 deb 范围）；
> SR-IOV `delete-vfs` 已**诚实化**（决策 #94）——V1 按数量回收，回显明确写出「`vf <n>` 不参与定位」，
> 让编号真正生效需 PF/VF 硬件（本环境无，见 §5.1）。
> 已关闭：物理口 link 告警（FR-NET-003，#73）、列表端点分页（FR-API-007，#74）、
> 快照运行中回滚风险（FR-CMP-015，#75——已查明并改为显式拒绝）。

| 项 | 现状（**已逐条在源码中复核**） | 判定 |
|---|---|---|
| ~~buffer 池统计~~ | **已解决**（决策 #68）：`statsclient` 对 26.06 的 buffer 池值类型解码全为 0；已加 `vpp_get_stats` 同版本工具回退源 + 来源标注（`statsclient\|vpp_get_stats`）与「不可用+原因」。真机实测 `available=430184`、`source=vpp_get_stats`（`docs/evidence/m5/d1-stats-fallback.txt`） | ✅ 已完成 |
| ~~FR-SYS-004 远程 syslog~~ | `internal/system/syslog.go`（`SyslogForwarder`：`net.DialTimeout` + RFC 5424）、`cmd/nfvisd/main.go:1008 applySyslogSettings` 从 committed 装载 host/port/facility/severity | ✅ 已关闭（#69）——**原行写「未实现」，已过期** |
| ~~FR-OPS-022 告警转发 syslog~~ | `cmd/nfvisd/main.go:140 alarms.SetNotifier` → `syslogFwd.Forward(alarmSyslogSeverity(…), "nfvisd", "alarm", …)` | ✅ 已关闭（#69）——**原行写「未实现」，已过期** |
| ~~FR-OPS-030 日志级别可配~~ | `cmd/nfvisd/main.go:95 slog.LevelVar` + `applySyslogSettings`（启动与每次 commit 重载）；`model/validate.go:239` 校验取值 | ✅ 已关闭（#69）——**原行写「未实现」，已过期** |
| ~~FR-NET-003 物理口 link 告警~~ | `internal/orchestrator/network/interface_alarm.go:CheckInterfaceLinks`（`INTERFACE_LINK_DOWN`，admin+link 双标志、自动 Resolve），由 `cmd/nfvisd/main.go:415/442` 巡检调用 | ✅ 已关闭（#73）——**原行写「仅 vhost-user 有」，已过期** |
| ~~FR-API-002 `openapi.json` 端点~~ | `internal/api/openapi.go`（`go:embed`）+ `internal/api/server.go:250` 注册 `GET /api/v1/openapi.json` | ✅ 已关闭（#69）——**原行写「端点未注册」，已过期** |
| ~~FR-API-007 列表分页~~ | `internal/api/pagination.go`（`api.paginate`，14 个端点）+ `APIPrefix = "/api/v1"`（`server.go:29`）+ `audit-logs` 的 `limit/offset` | ✅ 已关闭（#74）——**原行写「分页仅 audit limit」，已过期** |
| ~~FR-SYS-006 并发连接限制~~ | `internal/api/server.go:334 maxSessions()` 以 `netutil.LimitListener` 包装 listener（决策 #71④）；`max_sessions_test.go` 覆盖；`model/validate.go:236` 拒绝负值 | ✅ 已关闭——**原行写「无任何代码使用」，已过期** |
| ~~FR-SEC-001 管理面隔离~~ | `internal/system/listen.go:ResolveListenAddr`（通配收敛到管理口地址，决策 #72②）+ `listen_test.go` | ✅ 已关闭（#72）——**原行写「靠约定」，已过期** |
| ~~FR-SEC-004 sha256 默认强制~~ | `internal/images/download.go:171`「URL 拉取**默认强制** sha256——不提供即拒绝」（决策 #71⑤）；TLS 下限 `server.go:323` | ✅ 已关闭——**原行写「可选」，已过期** |
| **ACL 逐规则命中计数** | **正式降 V2**（决策 #68）：`acl_stats_intf_counters_enable` 无法经 govpp v0.13.0 调用——应答被 VPP 当作 `acl_del`（ID 108）执行，**不但无效且可能误删 ACL**，故不接线；绑定后 stats 段亦无逐规则计数（仅 `/err/acl-plugin-*`） | 降 V2（规格书 §12）；V2 需升级 govpp 或自解析 stats segment |
| `show vpp runtime` | govpp runtime 未接入；CLI 给出**明确提示**而非静默空值 | 另记（未接入，已提示） |
| ~~NFR-006 NTP 未同步标记~~ | ✅ **已实现**（决策 #97）：`internal/system/ClockSynced()` 探针 + 审计记录随事件落库 `time_synced` 三态（true/false/**NULL=未知**），`show log audit` 未同步时带 `[时钟未同步]`；存储 v1→v2 纯增量迁移；**真机双向实测** | **通过**（**告警侧**内存态未加同款字段，如实登记） |
| **NFR-005 国际化口径** | 原文要求「错误消息**英文为主**」，而产品**全程中文**（CLI/文档/命令全表），且决策 #86 专门**守护**中文文案不得混入内部引用。**这不是缺陷，是口径与产品定位不符**——以决策 #95 把措辞改为「**中文为主**」。⚠️ **更正**：我此前写的「降级理由已由 #86/#87 消除」**是错的**——#86/#87 消除的是「文案混内部引用」，与语言无关；按原文措辞，中文报错仍算未满足。 | 按决策 #95 修正措辞后转**通过**（处置口径已于 2026-09-18 确认；正式签字由验收方落笔） |

**本节此前的问题（已修）**：上表这些行在对应决策关闭后**没有跟着改**，仍写着「未实现 / V2 补」，
与同一文件 §0 的关闭注记、以及 §1~§4 主表（那里早已给出代码与测试落点）**自相矛盾**——
会让人把已完成项当降级接受、或反复复核同一批条目。本次按源码逐条复核后改写。

> **D-1 进展**：第一层（buffer 池回退源 + 来源标注）已完成并真机验证；
> 第二层（ACL 命中计数）经实测**不可安全落地**，按 D-1 既定规则正式降 V2；
> 第三层（升级 govpp / 自解析 stats segment）留 V2。

### 5.3 明确移 V2（规格书已声明）

tap 接口接入（FR-NET-024）、外部镜像仓库（FR-CMP-034）、`request system storage format-data`、
`request system password change`（决策 #65）。详见规格书 §12。

**另有 FR-SEC-006 的「底座最小化」半**（Ubuntu 最小集 + 固定版本组件清单 + 关闭无关服务，决策 #96）：
属**镜像/安装器**范畴，deb 装在有既定底座的机器上无法回溯地「最小化」它。出路二选一：
(i) V2 做最小镜像构建配方（packer/cloud-init）；(ii) 把该半句从 FR 拆出，改由**安装文档** +
`postinst` 的底座探测报告承担（现有：检测 vpp/libvirtd/dockerd 并提示）。

---

## 6. 签字建议

1. **可签字通过**：判定「通过」的 **100 条**（+4 条 §4.3 非 FR 功能行）——逐条证据链
   （代码落点 → 单测/守护 → 真机）见 §1~§4 与 §0。**别再引用旧数字**：本节此前写的
   「§1~§3 中判定通过的 71 条」与 §0/主表不符（全表通过实为 100），已更正。
2. **降级项处置已确认（2026-09-18）**：**只剩 2 条**——FR-NET-022（容器侧 memif 通流，需自建带 memif 客户端的镜像）、FR-SEC-007（脱敏的**有意例外**：`save <file>` 与备份归档 0600 + super-user）；FR-SEC-006 已拆两半并处置：**SSH 半实现并转通过**（决策 #96）、**底座最小化半移 §5.3**（属镜像/安装器）；
   另有 4 条「未验」是**环境受限**（SR-IOV 无 PF/VF、LLDP 无对端、硬件健康极端场景、真人 `Ctrl-]`），
   需在目标硬件/环境上补验。**用户可感知的剩余缺口只有 ACL 逐规则命中计数**（已正式降 V2，原因见 §5.2）。
3. **签字前建议完成**：A-2 §10 性能口径（要么实测，要么改为「设计目标，以目标硬件验收（附测量方法）」）。
   ~~A-3 IPv6 真机验证~~ **已完成**（决策 #69，且顺带发现并修复 v6 路由运行态不可见的缺陷）。
4. **建议重判 1 条（口径措辞变更，需签字）**：NFR-005 的**原文**要求「错误消息**英文为主**」，而产品
   全程中文——按原文它确实算未满足。但产品定位与全链路（CLI/文档/命令全表、且 #86 专门守护中文文案）
   都选择了中文，故以**决策 #95** 把措辞修正为「**中文为主**」；修正后该项由「降级」转「通过」
   （**待签字确认**；若转，§0 的通过数 100 → 101）。
   ⚠️ **更正我自己此前的建议**：我先前说「降级理由已由决策 #86/#87 消除」——**那是错的**，
   #86/#87 消除的是「文案里混内部引用」，与**语言**无关。
5. **技术债**：~~D-1 第一层 + 第二层可当天消除两项降级~~ **已结案**——第一层（buffer 池回退源 + 来源标注，
   决策 #68）已完成；第二层（ACL 计数）实测**不可安全落地**（会被 VPP 当成 `acl_del` 执行），
   已按 D-1 既定规则正式降 V2。

---

## 附：本表生成方法

- FR 清单取自 `docs/NFViS-系统产品需求与目标架构规格书.md` §3~§9、§11（含 §4.3 无编号功能）。
- 代码落点与测试名经全仓检索核对（`internal/`、`test/`）；真机证据取自 `docs/evidence/` 与各验收记录。
- 未决项均经**独立复核**（非直接采信子代理结论）：`openapi.json` 路由、分页 offset、
  `slog.LevelInfo` 硬编码、syslog 转发、物理口 link 告警、sha256 可选、IPv6 集成测试缺失，
  均已逐一在源码中确认现状。
- 基线自检：`make check` 全绿（覆盖率达标、依赖方向守护通过、决策 **91 项**一致、`openapi.json` 同步、
  操作者文本口径守护、原型全绿）。
- **2026-09-18 复核**：§5.2 的能力降级表此前**未随决策关闭而更新**（仍写「未实现 / V2 补」），
  与 §0 的关闭注记及 §1~§4 主表**自相矛盾**；已按源码逐条复核改写——9 行标为已关闭并给出 `文件:行` 依据。
  同批更正 §6：通过数（71 → 100，对齐 §0）、A-3（已完成）、D-1（已结案）。
  **总量复核**：按表格判定列逐行统计 = 通过 100 / 未验 4 / 降级 3 / 移 V2 2 = **109**，与 §0 一致。
